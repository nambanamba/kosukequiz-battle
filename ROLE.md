# このフォルダのセッションの役割

> ## 📍 起動する場所は `G:\マイドライブ\四谷大塚` です（例外なし）
>
> **セッションは全部、`四谷大塚` から開きます**（ユーザーの方針・共通 `CLAUDE.md` 第0節）。
> **役割ごとにフォルダを移動して開くと、指示を出す場所が分散して管理できなくなるため**です。
>
> - **このフォルダは「あなたが書き込む場所」であって、「そこから起動する場所」ではありません**
> - **担当フォルダがカレントディレクトリでなくても、それが正常です。**戸惑わないでください
> - **役割は最初のメッセージで決まります**（例:「あなたは◯◯担当です」）
> - **パスは絶対パスで書いてください。**カレントが担当フォルダでないため、相対パスは事故のもとです
> - ⚠️ **もし担当フォルダをカレントにして開かれていたら、それは想定外です。**
>   作業はできますが、**司令塔に一報してください**


**kosukequiz-battleアプリ本体の実装担当、かつ複数セッションのとりまとめ役**です。

対戦型暗記カードアプリ（社会・理科）の実装（index.html）・data.jsへのクイズデータ取り込み・GitHubへのコミット/push・GitHub Pagesへのデプロイを担当します。

- GitHubリポジトリ: `nambanamba/kosukequiz-battle`
- データはクイズ作成担当セッションから受け取って取り込みます:
  - 社会（通常回）: `G:\マイドライブ\四谷大塚\5年下\quiz_csv`
  - 理科: `G:\マイドライブ\四谷大塚\5年下\quiz_csv_理科`
  - 社会（夏期講習の復習）: `G:\マイドライブ\四谷大塚\5年下\quiz_csv_夏期講習`
- 元の単体アプリ（対戦なし、社会のみ）は `G:\マイドライブ\四谷大塚\kosukequiz` フォルダで別セッションが担当（そちらのROLE.md参照）
- **他セッションと同じファイル（index.html/data.js）に同時に書き込むと競合するので、コミット・push前には必ず一声かけ合うこと**（過去に実際に競合が発生済み）

セッション名（claude-XX）は再起動のたびに変わります。自分の役割が分からなくなったら、まずこのファイルを読み直してください。

---

# 担当とルール（2026-09-06 ユーザー決定）

2026-09-06に、司令塔の行きちがいで同じ役に2つのセッションが任命され、
あやうく同じファイルを同時に書きかえるところだった。以下はその再発防止のルール。

## 誰が本番（master）に入れるか

| セッション | 役割 | masterへのpush |
|---|---|---|
| **claude-ed（自宅PC）** | **本番担当。**data.jsへの取り込み、アプリ実装、他セッションのとりまとめ | **○ ここだけが直接pushしてよい** |
| **クラウドセッション**（例: `kosukequiz-battle-e1`。スマホからの窓口） | 相談・調査・実装 | **✕ 直接pushしない。**ブランチ＋プルリクで出し、claude-edが確認してマージする |

- クラウド側は環境が使い終わると消えるので、**作ったものは必ずpush（またはPR）まで済ませる**こと。
- 役割の変更はユーザーが決める。セッション同士や司令塔の判断だけで担当を移さないこと。

## 作業の進めかた（全セッション共通・必ず守る）

1. **始める前に必ず最新を取り込む**
   `git fetch origin master && git status -sb` で差がないか確認し、遅れていれば `git pull origin master`。
   いきなり編集を始めない。

2. **長い作業に入る前に「いま作業中」を宣言する**
   `作業メモ.md` の先頭の「## いま作業中」に、`セッション名 / 触るファイル / 内容` を1行書いてpushする。
   終わったらその行を消す。これが他のセッションから見える唯一の「札」になる。
   作業前にこの欄を読み、**他のセッションが同じファイルを触っていたら手を出さない**。

3. **pushの直前にもう一度 `git fetch origin master` する**
   他セッションのコミットが入っていたら、取り込んでから push する。
   競合したときは、勝手に片方を捨てず、内容を見て統合する。

4. **作業が終わったら必ず `作業メモ.md` に追記してから push**
   「何をしたか」「なぜそうしたか」「次にやること」を書く。
   セッション同士は直接会話できないので、**作業メモ.mdとコミットメッセージが唯一の引き継ぎ手段**。
   コミットメッセージも、1行目に何をしたか、本文になぜそうしたかを書く。

5. **data.js / index.html は特に注意**
   - data.jsを編集するときは、**元の整形を保ったまま該当ブロックだけ**を触る。
     ファイル全体を書き直すと巨大な差分になり、競合の温床になる。
   - 同じファイルを2セッションが同時に触らない（上の「いま作業中」で防ぐ）。

6. **仕様を作り直すときは、過去にその仕様にした理由を確認する**
   作業メモ.mdを検索してから変える。
   （実例：苦手判定を「累計の正誤比較」に戻してしまい、同じ指摘を2度受けた）

## 問題文の修正はプルリクで自動化ずみ

アプリの「保存してGitHubに報告する」で出た修正報告Issueから、GitHub Actionsが
自動でdata.jsを直すプルリクを作る（`.github/workflows/apply-quiz-fix.yml`）。
手でdata.jsを直す前に、この仕組みが使えないか確認すること。

---

## 作業を終えるときは
このフォルダに `作業メモ.md` を作る/更新し、「今日どこまで進んだか」「次にやること」を簡潔に書き残してください。ROLE.mdは役割（変わらないもの）、作業メモ.mdは進捗（毎回変わるもの）の担当です。次に別のセッションがこのフォルダを開いたとき、両方を読めば迷わず続きから動けます。

---
最終更新: 2026-09-06
