# 作業メモ

## 2026-09-03 にやったこと（続きのその4：アプリ分割）

ユーザーから「理科（計算問題）と社会（漢字プリント）だけ別アプリに分けたい」との依頼。早押し対戦本体（社会・理科共通）は分割せず維持。

### Part A：社会「漢字プリント」→ 既存の`kanji-print-app`リポジトリを刷新
- `kanji-print-app`（`G:\マイドライブ\四谷大塚\kanji-print-app`）は、8/31時点の古い実装のまま止まっていたが、**私の知らないところで別セッションが並行して更新を進めていた**（印刷余白の調整、第1回・第2回への実データ同期など、コミット履歴あり）。着手前にコミットされていない作業中の変更（印刷CSSの途中編集）を発見し、`git stash`で退避（破棄はしていない）。
- kanji-data.jsをquiz_csv作成の正式な83問データ（第1回_漢字.json・第2回_漢字.json）に総入れ替え。id体系をkosukequiz-battle側`KANJI_DATA`と同じ`k{回}_{連番}`に統一。
- 進捗記録をkosukequiz-battleと同じlocalStorageキー（`kq_kanji_stats_v1`）・同じデータ形式に変更。GitHub Pagesの同一オリジン（`nambanamba.github.io`）のため、localStorageが自動的に共有され、既存の進捗がそのまま引き継がれる（エクスポート/インポート等の手動移行は不要）。
- 〇✕はタップした瞬間に確定・保存、苦手優先の並び替え、印刷レイアウトの拡大など、kosukequiz-battle側で確定した仕様をすべて反映。README.mdも刷新。
- ヘッドレスブラウザで動作確認、コミット・push済み。

### Part B：理科「計算問題」→ 新規リポジトリ`keisan-print-app`
- `gh repo create`で新規作成、GitHub Pages有効化。
- kosukequiz-battleのdata.js内`kind:"calc"`の項目（77問）と参照画像（39枚）を移植した最小限のスタンドアロンアプリ（stats/localStorageは使わない無状態機能）。
- ヘッドレスブラウザで動作確認、初回コミット・push済み。

### Part C：kosukequiz-battle本体の縮小
- `screen-kanji`・`screen-calc-setup`・`screen-calc`一式（HTML/CSS/JS）と、data.js内の`KANJI_DATA`・`kind:"calc"`の項目（77問）を削除。
- ホーム画面の「今日の漢字プリント」「今日の計算問題」ボタンを、それぞれ姉妹アプリへの外部リンク（新しいタブで開く）に置き換え。
- ヘッドレスブラウザで、対戦・ソロ・問題一覧・復習ミックスなど残る機能に影響がないことを確認（QA_DATA件数1147→1070に減少、想定通り）。README.md更新、コミット・push済み。

### 他セッションとの連携
- kanji-73（新規セッション、国語「漢字と言葉」の新アプリ検討中）から相談があり、「対戦機能はkosukequiz-battleのみ、印刷・練習系は姉妹アプリに分ける」方針を共有・推奨した。
- quiz-csv-9a（社会・通常回の作成担当）から、第3回_奈良時代.json（112問）・第3回_漢字.jsonの取り込み依頼が届いている（次にやること参照）。

## 次にやること
- quiz-csv-9aから届いている第3回_奈良時代.json（対戦クイズ本体）・第3回_漢字.json（kanji-print-app側）を取り込む
- kanji-print-appでstashした「作業中だった印刷CSS変更」は、今回の刷新で内容ごと上書きしたため、基本的にもう不要のはず。ただし念のため残してある（`git stash list`で確認可能）

## 2026-09-03 にやったこと（続きのその3）

### 対戦の正誤カウント修正：「まちがえた問題だけもう一勝負」の扱い
- ユーザー報告「正解不正解のカウントがおかしい。1回目正解で正解+1、スキップ・誤り・最後の誤りのやり直しのときは誤り+1にしたい」に対応。場所は二人対戦、「最後の誤りのやり直し」は「まちがえた問題だけもう一勝負」ラウンドのことと確認。
- 現状：スキップした問題を後で答え直したときはホスト側のみ強制的に「誤り」記録される仕組みが既にあったが、「まちがえた問題だけもう一勝負」ラウンドでは、そこで正解すると普通に「正解+1」されてしまっていた（元々間違えた問題なのに正解扱いになる）。
- 修正：`inMissRetryRound`（もう一勝負ラウンド中かどうか、host/guest共通）を新設し、このラウンド中の判定は常に「誤り」として永続的な正誤記録に残すように変更（今回だけ「まちがえた問題」リストから外れるかどうかの判定は、これまで通り実際の正誤で行う）。ホストからゲストへの`startAction`送信に`isRetryRound`フラグを追加してゲスト側にも伝播、対戦セッションの再開（`resume-battle-btn`）時も保持されるようにした。
- 実際の2人対戦（WebRTC）はヘッドレス環境で直接テストできないため、コードレビューと基本的な動作確認（ソロ・漢字プリントに影響がないこと）で確認。次に実機で対戦する際に、まちがえた問題のやり直しで正解しても苦手記録が残ることを確認してほしい。

## 2026-09-03 にやったこと（続きのその2）

### 復習ミックスの選び方をシンプルな仕様に作り直し
- ユーザーから「複雑にお願いしすぎたかも」とのフィードバックとともに、復習ミックスのやりたいことを明確化：①メイン単元選択（既存のまま）、②復習単元選択＋各種フィルタ＋「苦手な問題から選ぶ」＝正解より失敗が多い問題を正解が一番昔の問題から優先→残り（失敗経験ありだが失敗より正解の方が多い問題）も同様に正解が一番昔の問題から、という2段階の選び方。
- 旧来の間隔反復（box/nextDue）ベースの選定ロジックを廃止し、上記の2段階＋「一度も失敗していない問題」を最後に追加する3段階選定に置き換え（`orderReviewCandidatesByWeakness()`関数を新設）。「正解したのが一番昔」の判定用に`stats`へ`lastCorrectAt`（直近正解時刻ではなく、正解時のみ更新される専用フィールド）を追加。
- 復習単元のフィルタを、メイン単元選択と同じ「出題タイプ・優先度・難易度」の3種類に拡張（従来は優先度のみだった）。復習専用の独立した状態（reviewType/reviewPriority/reviewLevel）として持たせ、メイン側のフィルタとは干渉しない。
- ヘッドレスブラウザで、実際にstatsを仕込んで一人プレイを進め、①失敗過多の問題→②失敗経験ありの問題→③未失敗の問題、の順で正しく出題されることを確認（出題順どおりモードで検証）。フィルタのトグル→ラベル反映→リロード後の保持も確認ずみ。

### 復習ミックスの「復習する単元」バグ修正（このあと上記の作り直しで選定ロジック自体は差し替え済み）
- ユーザー報告「①どの単元を選んでいるのかわかりにくい／②選んだ単元から出ていない」に対応。原因は2つ：(1) チップの選択状態表示に存在しないCSSクラス`on`を使っていた（正しくは`selected`）、(2) メイン単元を1つずつ外していく操作で、2つ目以降に新しく復習候補になった単元が自動でonにならないロジックバグ。修正しヘッドレスブラウザで確認・コミット・push済み（0ab9ade）。

### 漢字データを対戦クイズから完全独立させる大改修
- quiz_csv（社会通常回）セッションが、ユーザーからの直接指示「漢字問題と通常問題は分けたい。漢字は完全に独立して進捗・正誤を管理したい」を受けて、対戦クイズ本体（`第1回_旧石器縄文弥生.json`等）から`kanjiWord`フィールドを削除し、代わりに完全に独立した`第1回_漢字.json`（40問）／`第2回_漢字.json`（43問、いずれもクイズ本体とは別id・読み仮名＋ヒント形式）を新規作成していたことが判明（当セッションへの返信待ちだった）。
- ユーザーに方針を確認：「独立（データは別ファイル）、ただし正誤管理の“方法”はクイズ本体と同じ、単元は共通の文字列で連動」という結論。
- 対応：
  1. `第1回_漢字.json`／`第2回_漢字.json`（計83問）を、data.js内に**QA_DATAとは別の新配列`KANJI_DATA`**として追加。id接頭辞は`k{回}_{連番2桁}`（例：k1_01）。`u`（単元名）は対戦クイズ側と完全に同じ文字列（例：`"第1回.旧石器時代・縄文時代・弥生時代"`）になっており、単元の自動連動はそのまま機能する。
  2. index.html側：`getKanjiCandidates()`を、旧来の`kanjiWord`/`kanjiSkip`自動抽出ロジックから、`KANJI_DATA`をそのまま使う形に全面書き換え。
  3. 漢字専用の正誤記録（`kanjiStats`、localStorageキー`kq_kanji_stats_v1`）を新設し、クイズ本体の`stats`とは別ストレージで管理するように変更（昨日実装した「一本化」を撤回）。間隔反復のスケジューリングロジック（`scheduleNext()`）はクイズ本体と共通の関数をそのまま再利用し、「管理方法は同じ」を担保。
  4. ヘッドレスブラウザで、KANJI_DATA件数（83件）・QA_DATA非破壊・漢字保存後もクイズ側stats（ホームの苦手/正解ずみ件数）が変化しないこと・印刷レイアウトを確認ずみ。
- README.mdにKANJI_DATAのスキーマを追記。
- **次にやること**：quiz_csv（社会通常回）セッションに、①今回の取り込み完了、②今後の回も同じ形式（`第{回}回_漢字.json`）で対戦クイズと合わせて作成してほしい旨、③旧ドリルPDF（`第1回・第2回_社会漢字_5回練習ドリル`等）はアプリ本体の印刷機能に統合されたため再生成不要、を連絡する（担当セッションの特定ができておらず、次回セッション起動時に対応）。

## 2026-09-03 にやったこと
- 漢字プリントの印刷を、姉妹アプリ`kanji-print-app`（G:\マイドライブ\四谷大塚\kanji-print-app）のレイアウトを参考に「①テストの穴埋め欄→②お手本＋なぞり書き＋4回練習マス」の1枚構成に変更。従来は「問題用紙（穴埋めのみ）」「解答用紙（答えのみ）」を別々に印刷する方式だったが、1枚に両方まとめ、印刷ボタンも4個（今日の分・まとめ印刷それぞれの問題用紙／解答用紙）から2個（今日の分・まとめ印刷それぞれ1個）に整理
- ヘッドレスChromeで印刷プレビュー（`@media print`エミュレート）のスクリーンショットを確認し、レイアウト・件数（テスト欄・練習マス目とも一致）を確認ずみ

## 2026-09-02 にやったこと

### アプリ本体（index.html）の機能追加
- 漢字プリントの対象単元を、ホーム画面（クイズ）の社会の単元選択と常に自動連動するように変更。漢字画面独自の単元選択UIは廃止し、「ホーム画面で単元を変更」リンクに置き換え
- 漢字プリントの正誤記録を、クイズ本体の正誤記録（苦手判定・間隔反復）と一本化。漢字プリントで記録した結果がクイズ側の「苦手な問題」にもそのまま反映されるようになった（従来はkanjiState.statsという別管理だった）。あわせてホームと同じ「正解ずみを除く」「苦手な問題だけ」フィルターを漢字画面にも追加
- 復習ミックスに「復習する単元」の個別選択（メインで選んでいない単元の中からさらに絞り込み）と、優先度（高/中/低）フィルターを追加
- 対戦に「こどもどうし公平モード」を新設（既存の親子戦フローはそのまま）。ONにすると、ホストによる問題の先読み・スキップ（考える時間のアドバンテージ）を行わず問題を両者に同時配信。ゲスト参加後、ゲスト側でも出したい単元を選べ、ホストの単元選択と合算して出題される
- ヘッドレスChromeでの自動操作確認（漢字プリント作成→記録→ホーム統計反映、単元/復習ミックス操作、公平モードトグル、ソロ開始）でJSエラーなしを確認ずみ

### 見つかった問題（未対応・要フォローアップ）
- 漢字プリントで実際に抽出される405問（すべてkanjiWord指定＝厳選ずみ）を全件確認したところ、約4割（162問）で「こたえ」が空欄の正解ではなく、問題文中の別の単語になっている疑いがある（特に公民分野で顕著。例：「国会議員は満何歳以上の選挙で選ばれますか」→こたえ「国会」など）。data.jsのkanjiWordはクイズ作成担当セッションの管轄のため、本セッションでは修正せず指摘のみ。ユーザー確認済み、対応は「クイズは別途作ってもらう」ため保留中

### データ取り込み
- quiz-csv-5a（理科・第2回レビュー担当）からの依頼を受け、理科第2回「てこ・滑車・輪軸」（87問：暗記r2m01〜17、計算r2c01〜70、画像r2_01〜40.jpg）をdata.jsに取り込み、コミット・push・デプロイ済み

## 次にやること
- 漢字プリントのkanjiWord誤り（上記162問）を、クイズ作成担当セッションと共有・修正する必要がある（現時点ではリスト化のみ保留、着手時はkanji_full_list_suspicious.jsonの生成ロジック（このセッションの一時ファイル、再生成可）を参考にできる）

## 2026-08-30〜31 にやったこと

### データ取り込み
- 夏期講習・復習編1（日本の食料生産v2）: 地図問題15問に丸数字を追加、レビュー指摘6件（八郎潟・利根川・境港・おうとう・港の記号問題3問）を反映
- 理科第1回「生物のつながり」を80問版（本文由来の設問を除去したもの）に差し替え、その後の細かい修正（no.53画像追加、no.66答えの先出し修正、no.68答え不足の修正）も反映
- 理科・夏期講習の両方に優先度（priority）・難易度（level）フィールドを追加（取り込み時に一度落としてしまっていたのを追加し直した）

### アプリ本体（index.html）の機能追加・修正
- 問題一覧画面（screen-list）にイラスト画像・出典（ページ数/問題番号）を表示するようにした
- 一人で解くとき（screen-solo）に「前の問題にもどる」機能を追加（通常モード・チェックのみモード両方対応）
- 計算問題の印刷で、画像の読み込みを待ってから印刷するように修正（印刷が空白になる不具合の対策）
- ホーム画面に優先度・難易度フィルターを追加
- 復習ミックスを間隔反復（スペースドリピティション）方式に変更：①未正解の問題→②間隔が来た問題→③まだ間隔が来ていない問題、の順で優先。苦手判定も「直近2回連続正解」で解消するように変更（累計正解数ベースだと解消しにくい問題があったため）
- 社会の「今日の漢字プリント」機能を新規追加。既存の問題データ（答えの括弧内など）から漢字語を自動抽出し、毎日数問印刷→手書き→〇✕入力→まちがえた分を翌日優先、というサイクルを実装
- 漢字プリントの単元フィルター＆印刷機能強化（2026-08-31）:
  - 中学受験社会の頻出漢字・記述指定重要語（歴史・地理・公民）を精選し、data.jsの全問題にkanjiWord（439問）/kanjiSkip（541問）を設定して平易な語（米・肉牛など）の混入を完全排除
  - 漢字画面（screen-kanji）内に「単元フィルター」（全単元/個別単元の選択・問題数バッジ）を追加し、単元ごとの絞り込みに対応
  - 「選んだ単元の漢字をすべて印刷（まとめテスト用）」ボタンを追加（問題用紙・解答用紙）
  - A4印刷時に手書きしやすい解答記入枠（四角ボックス）とタイトル・日付・氏名・得点欄ヘッダーを配置
- 二人対戦（ゲスト側）: ホストが「わかった／スキップ」を考えている間は問題文を表示せず「つぎの問題の準備中…」に変更し、前の問題のカードも非表示に（スキップした瞬間にはスキップされた問題を表示）
- 二人対戦の結果画面: 「まちがえた問題だけもう一勝負」を3秒カウントダウンで自動開始するように変更（ゲスト側にも同じカウントダウンを表示）

### 環境まわり
- このセッションの環境にgit・GitHub CLI（gh）が入っていなかったので、winget経由でインストールし、gh認証（デバイスコード方式）を完了させた。以降のpushはこのセッションから可能

### 複数セッションの役割整理
- quiz_csv（社会・通常回）フォルダにレビュー担当の役割をROLE.mdに追加（従来レビュー担当が不在だった）
- レビュー担当の運用を「指摘のみ、修正は作成担当が行い再確認する」方式に統一（理科・夏期講習の両ROLE.mdとクイズデータ作成ルール.mdに反映。レビュー担当自身の修正案に誤りがあった実例があったため）
- 社会（通常回）の自動チェックで、107問中20問が本文由来（2026-08-30ルールでは対象外）の可能性が高いことを発見、quiz_csv\ROLE.mdに記載済み

## 次にやること
- images/フォルダに未取り込みの画像が増えている（kai1_28.jpg, kai1_29.jpg, r2_01〜08.jpg）。理科第2回または社会の新しいデータの可能性が高いが、対応する取り込み依頼はまだ来ていないので、担当セッションから連絡が来たら対応する
- quiz_csv（社会通常回）のレビュー担当が本文由来20問の要否を判断する予定（まだ未着手のはず）
