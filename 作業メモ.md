# 作業メモ

## いま作業中
（作業を始めるときに「セッション名 / 触るファイル / 内容」を1行書いてpush、終わったら消す。
　作業前に必ずここを読み、他のセッションが同じファイルを触っていたら手を出さないこと）

- （いまは誰も作業していません）

## 担当（2026-09-06 ユーザー決定）
- **masterへ直接pushするのは claude-ed（自宅PC）だけ。**
- クラウドセッション（スマホからの窓口、例: `kosukequiz-battle-e1`）は**ブランチ＋プルリクで出す**。
- 詳しいルールは `ROLE.md` の「担当とルール」を参照。

## 2026-09-08 起動を速くするため、対戦の通信ライブラリを「あとから読む」形にした

### やったこと
`https://esm.run/trystero`（対戦のWebRTC）を、module 先頭の `import` から
**対戦を始めるときに読み込む**形に変えた。先頭の `import` は読み終わるまで
続きが動かないので、**ひとり練習しかしない日でも毎回待たされていた**
（esm.run → trystero → core → nostr → secp256k1 と4回続けて取りに行く）。

押してから待たされないように、**画面が落ち着いたころに裏で先読み**もする
（`preloadJoinRoom`、`requestIdleCallback`）。

### ★ここでハマった：一度失敗したURLは、ブラウザが二度と取りに行かない
裏の先読みが失敗すると、**そのあと「二人で始める」を押しても対戦が始められなくなった。**
電波が戻っても直らず、アプリを開き直すまでずっと始められない。

原因は、ブラウザが「このURLのモジュールは読み込みに失敗した」ことを覚えていて、
**まったく同じURLで頼み直しても、通信すら発生しない**こと（実測で確認）。

| 頼み方 | 結果 | 通信したか |
|---|---|---|
| 1回目（わざと失敗させた） | 失敗 | した |
| 2回目・同じURL | **失敗（取りに行きすらしない）** | していない |
| 3回目・`?r=1` を付けた別URL | 成功 | した |

なので、**やり直すときは必ず前と違うURLにする**ようにした（`importTrystero`）。
`esm.run` と `cdn.jsdelivr.net` を交互に使い、2回目以降は `?r=回数` を足す。
さらに、失敗したら**1回だけ黙って自動でやり直す**ので、先読みが1回こけただけなら
ユーザーには何も見えない。

**教訓（E-1）: `import()` の再試行は、同じURLでは効かない。URLを変えること。**
リトライを書いたつもりで書けていない、という形で静かに壊れる。

### ★教訓（E-2）: 「画面がそうなった」は「動いた」の証拠にならない
最初のテストは「部屋のコードが出て『相手を待っています』になったか」で判定していた。
これは**通らないはずの場面でも通ってしまった**。理由は、この2つが
`await`（読み込み待ち）の**前**に画面へ出るため。読み込みが失敗しても画面はそうなる。

いまは **trystero が中継サーバーへ実際に接続したか**（WebSocketが開いたか）で判定している。

### テスト `tools/test_battle_start.mjs`（新規・17件）
スタブを使わず、**本物の trystero をインターネットから読み込ませる**。
smoke-test は WebRTC をスタブに差し替えているので、読み込みそのものを見ていない。

1. 先読みずみ（数秒おいてから押す）
2. 起動直後に急いで押す（先読みが終わる前）
3. 読み込みが1回失敗（自動でやり直せるか）
4. 先読みが完全に失敗（押したときにやり直せるか）← ここが実際に落ちた

**このテストが空振りでないことも確認した**：直す前のコードに戻すと5件失敗し、
直すと17件全部通る。

### 流したテスト（すべて成功）
- 素早い確認 123件（16/20/21/21/23/22）終了コード0
- 対戦開始 17件 終了コード0
- smoke-test 11件 終了コード0

### ★実機で確認ずみ（2026-09-08 ユーザー、司令塔 claude-1c 経由）
> 対戦はできました。単元は消えています。

- **対戦が実機で始められた。**機械では確認できなかった「2台目をつないだ実際の対戦」も動いている
- **旧単元289問の削除も実機に反映されている**（9b14f5b〜8a1b780 の分）

配信前にモジュールの読み込み失敗のキャッシュを見つけて直したのが効いた。
あのまま出していたら、**電波が不安定になった時点で対戦不能になり、
アプリを開き直すまで直らない**という形で表に出ていた。

### 【依頼を受けた】5編・38問の取り込み（claude-45 から、claude-18 レビュー通過ずみ）
「20秒で解けない問題」への対応。**問題数は増減せず、id も1つも動かない**（消えた/重複/新規すべて0件）。

| 編 | ファイル | 差異 |
|---|---|---|
| 復習編4 | 復習編4_中国四国地方.json | 2問 |
| 復習編5 | 復習編5_近畿地方.json | 22問 |
| 復習編6 | 復習編6_中部地方.json | 3問 |
| 復習編7 | 復習編7_関東地方.json | 9問 |
| 復習編8 | 復習編8_東北北海道地方.json | 2問 |
| | | **38問** |

フィールド別: q=29 / sol=29 / img=25 / a=15 / priority=9 / level=1。
**この数字は作成担当・司令塔・claude-18 の三者で一致ずみ**。取り込み後の照合基準にする。
復習編1・2・3 は差異0件で取り込み不要。

注意点（依頼元の申し送り）:
1. **id をそのまま使う**（全8編に付与ずみ）
2. **画像2枚が未参照になるが消さない**（C-3）: `kaki_r5_table01.jpg`（17問）・`kaki_r7_table02.jpg`（8問）。
   `kaki_r5_table02.jpg` は kaki5_83 が、`kaki_r7_table01.jpg` は kaki7_68 が引き続き参照する
3. **`update_img_sizes()` を通す**（司令塔の申し送り）
4. **q に改行が入っている。取り込みで落とさないこと**（`white-space: pre-wrap` 対応ずみ）
5. **記録の引きつぎは不要**（id 不変のため）。移行テーブルは触らない

### 取り込み完了（38問・data.js は 1342件のまま）
`python tools/import_quiz.py --apply 4/5/6/7/8`。件数・id とも増減なし。

**自分の照合（元JSONと data.js を id キーで突き合わせ）: 8編765問すべて不一致0件。**
q・a・sol・img・priority・level を1件ずつ比べた。取り込み担当の道具まかせにせず、
**別の道具で独立に確かめた**（import_quiz.py のバグがあっても気づけるように）。

- 改行を含む q は43件。**落ちていない**
- **図表を指しているのに画像が無い問題は0件**（kaki4_79 と同じ「解答不能」の型が無いか確認）
- 丸数字を含むのに画像が無い問題も0件
- 未参照になった `kaki_r5_table01.jpg`・`kaki_r7_table02.jpg` は**残してある**（C-3）。
  `kaki_r5_table02.jpg` は kaki5_83 が、`kaki_r7_table01.jpg` は kaki7_68 が引き続き参照
- 画像は新規コピー0枚・欠落0件。IMG_SIZES の追加も0件（画像自体は変わっていないため）
- テスト: 素早い確認123件・smoke-test 11件、すべて成功

### ★教訓（E-3): 打ち切られた一覧を「全部」と読み違えた
照合の途中で「こちら36件・依頼元38件」と食い違い、**依頼元が間違っていると疑った。**
実際は `import_quiz.py` が**変更点の一覧を先頭20件で打ち切って表示**していただけで、
件数の行は正しく22件と出ていた。**打ち切りの印が無かったので、20件で全部だと思い込んだ。**

- **数える対象は「一覧」ではなく「件数の行」。**一覧は表示の都合で減ることがある
- 打ち切るなら「…ほか2件」と必ず書く。**黙って切るのが一番あぶない**
- そもそも打ち切りをやめた（取り込み後の照合の土台になる箇所なので全部出す）

### 作成担当への回答: アプリは答えの文字列を照合していない
「表記ゆれで不正解になるか」と聞かれたので調べた。**照合していない。**
答えを入力する欄が無く、正誤は人が押して決める。

- ひとり練習 …「答えを見る」→ 自分で 〇わかった / ✕まちがえた
- 対戦 … 早押し「わかった！」→ 口で答える → **相手が**〇✕を押す

なので `a` は「答え合わせの見本」として働く。「（兵庫県）です。」の形はむしろ判定側に親切。
**表記ゆれのための `a` の書き換えは不要**と伝えた。

#### ただし、対戦の判定は10秒で自動的に「正解」になる（要観察）
`judgeTimeSec = 10`（設定「二人対戦用: 〇✕判定の時間」で変更可）。時間切れは `judgeOpponent(true)`。
答える側が相手のもたつきで損をしないための作りだが、**複数を答えさせる問題**では
判定する子が10秒で照合しきれず、**黙って正解になる**おそれがある
（例: kaki7_90 「（東京都・埼玉県・群馬県）です。」）。記録が実力より甘くなる方向なので気づきにくい。

いまは**データもアプリも変えていない。**まず実機で「複数答えの問題で時間が足りているか」を見て、
足りなければ設定を延ばすか、アプリ側で手当てする（例: 複数答えの問題だけ長くする）。

### a の点検は「複数項目か」と「説明がくっついているか」を別々に見る
判定が10秒で自動正解になる件から、作成担当が `a` を点検してくれた。そのとき
**走査条件のちがいで、拾えるものが変わった。**

- 作成担当の条件「（）の中に `・` がある」→ 複数項目の答えは拾えるが、
  **kaki1_79 のように「（近郊農業）です。正しくないのは（ウ）です（理由…）」**は拾えない
- こちらの条件「`です。` のあとにまだ文が続く」→ **答えのうしろに説明がくっついているもの**を拾う。
  該当は kaki2_73 と kaki1_79 のちょうど2問（記述問題は除外して走査）

**どちらか一方では取りこぼす。** `a` を点検するときは両方の条件で見ること。

走査から**記述問題（「説明しなさい」「なぜ〜ですか」など）は外す。**
kaki1_93（69字）・kaki8_110（57字）・kaki3_86・kaki2_33 は `a` が長いが、
**その長い文自体が正解**なので直す対象ではない。

判定に時間がかかる問題（実機で様子を見る対象）:
- 3項目を答える: kaki7_90 / kaki7_91 / kaki6_72 / **kaki1_74**（「・」区切りでないので一覧から漏れやすい）
- 記号2〜3個: kaki2_59 / kaki2_81 / kaki5_85 / kaki7_55 / kaki8_97 / kaki8_104 / kaki6_104 / kaki8_118（照合は一瞬なので実害小）

### ★【保留中】kaki1_79・kaki2_73 の2問が取り込み待ち（2026-09-08〜）
**元データ（JSON）はもう直っている。data.js にはまだ入れていない。**
**待っているのは司令塔の「配信してよい」の一言だけ**で、作業自体はいつでも1分で終わる。

- 元データ: `5年下\quiz_csv_夏期講習\復習編1_日本の食料生産_v2.json` / `復習編2_工業資源輸送機関.json`
- 取り込み方: `python tools/import_quiz.py --apply 1` と `--apply 2`
- `--check` で確認ずみ: 復習編1＝`kaki1_79 a,sol`、復習編2＝`kaki2_73 a,sol` の各1件のみ。
  消えたid 0件・新規id 0件・重複0件・画像の増減なし
- 中身: 答えのうしろにあった説明を `sol` へ移した（`a` は 69字→22字、43字→15字）

**なぜ今すぐ配信しないのか**: 2026-09-08 に大きな変更（対戦の遅延読み込み・旧単元の整理・38問の
書き換え）を続けて配信したため、司令塔と「しばらく実機で使ってもらって様子を見る」方針で合意した。
**この2問だけのために配信はしない。**次のまとまった変更と一緒に出す。作成担当も催促しない旨を了解ずみ。

**このセッションが止まったら、次の担当はここを見て取り込んでください。**

### 次の予定（2026-09-08 時点）
- 作成担当が29問の書き換え中（復習編5の21問・復習編7の8問＝表を外して1段階に、
  別に9問を level=発展 / priority=低 に）。**claude-18 のレビュー通過後に取り込み依頼が来る。**
  それまで当セッションは手が空いている
- 一覧を開くときの4.9秒（案A・100件ずつ）… **急がない。**ユーザーの体感待ち
- data.js のパース4秒 … **触らない**（司令塔判断。リスクに見合わない）
- 今日は大きな変更を続けて配信したので、**しばらく実機で使ってもらって様子を見る**

## 2026-09-07 claude-4c [8cc457]（自宅PC）が claude-ed から引き継ぎ、保留分を push 完了

前任 `claude-ed` [7768c9] が「push 保留中」の状態で応答を停止したため、司令塔 `claude-1c` [e3df7b]
経由でユーザー決定として当セッションが引き継いだ。**取り込み作業はやり直していない**（検証ずみのため）。

### 引き継ぎ時に master が behind 1 だった → リベースで解消（force push はしていない）
`git fetch` したところ cloud セッションが `78bc26a`（複数セッションの担当分けと作業ルールの明文化）を
push 済みで behind 1 になっていた。リベースし、コンフリクトした `作業メモ.md` は
**cloud側の記述（担当分け・競合防止ルール）と claude-ed の記述（取り込み結果・役割重複の経緯）の
両方を残して統合**した（司令塔の指示と一致）。片方も捨てていない。

**`78bc26a` は `ROLE.md` と `作業メモ.md` しか触っておらず `data.js` は無変更**だったため、
claude-18 による取り込み検証はそのまま有効と判断し、再検証は依頼しなかった（司令塔も同判断）。
根拠: リベース前後で blob ハッシュが完全一致（1バイトも変わっていない）
- data.js `d9e9ab6878e4a58dd16a68e332f4f9be7f3a23c6`（前後同一）
- index.html `f31495d7991b331602a8b5d2467a79e2ec0eb890`（前後同一）

### push 完了（`78bc26a..fbf834b`、fast-forward）
ユーザー本人の承認を得てから push。**force push はしていない。**

#### push 前に当セッションでも独立に再確認した内容（すべて一致）
- 総件数 **1411** / ユニークid **1411**（重複0）
- 各編の件数 93 / 81 / 86 / 89 / 85 / 111（復習編1〜6）
- 画像参照 **273種すべて実ファイルあり、欠落0**
- 旧画像4枚（`kaki_r4_graph02` / `map01` / `map02` / `shapes01`）は**残してある**（C-3）

#### ★最優先だった `kaki4_79` の解答不能を解消、本番配信も確認ずみ
- `img` が `kaki_r4_graph02.jpg`（グラフ）→ **`kaki_r4_shapes02.jpg`（A〜Hの分県図）** に修正されている。
  設問「A〜Hから選んで、記号で答えなさい」・答え「（H）です。」と整合し、**解答できる状態になった。**
- GitHub Pages で実配信を確認:
  - `data.js` HTTP 200 / 配信中の総件数 **1411** / `kaki4_79` の img が `kaki_r4_shapes02.jpg`
  - `images/kaki_r4_shapes02.jpg` HTTP 200 / 95,947バイト（ローカルと一致）
  - ※配信サイズがローカルより 13,418バイト小さいのは、行数がちょうど 13,418行で、
    ローカルが CRLF・配信が LF のため。**内容は同一**（差はすべて改行コード）。

### ⚠️ 上の「claude-ed への引き継ぎ」節の記述について（誤解の元なので補足）
cloud側が書いた「復習編4は `1496a94` で既にmasterに入っているので、取り込みに含まれていたら外して」
という注意書きが下に残っているが、**今回は C-1 の「上書き更新」で処理済みであり、二重登録は起きていない。**
根拠: 件数が `1411 = 1215 − 89 + 285` で計算一致、id重複0。**対応不要。**

### 2026-09-07 仕事2 完了: 問題数・フィルタなどの出題設定を科目別にした

**ユーザーの訴え「理科と社会を共通とせず別にしてほしい」の原因は単元選択ではなかった。**
単元選択 `unitsBySubject` は `fd828e2` で科目別になっており正しく動いていた。
`saveSettings()` で **`unitsBySubject` だけが科目別で、出題設定はすべて科目共通の単一値**
だったのが原因（司令塔が特定）。理科で問題数を10問にすると社会も10問になっていた。

#### やったこと
`unitSelectionBySubject` と同じ形で `settingsBySubject` を追加し、
`collectSubjectSettings()` / `rememberSubjectSettings()` / `restoreSubjectSettings()` を用意。
`switchSubject()` と `saveSettings()` で覚え、`switchSubject()` と `loadSettings()` でもどす。

- **科目別にした9項目**: `count`（問題数）/ `filterUnmastered` / `filterWeak` / `type` /
  `priority` / `level` / `shuffle` / `reviewMixCount` / `reviewMode`
- **科目共通のまま残した項目**: `fairMode` と時間設定5つ（`headStartSec` / `answerTimeSec` /
  `judgeTimeSec` / `nextTimeSec` / `skipNextTimeSec`）。科目ではなく「その端末での好み」のため
- **復習ミックスの対象単元（`reviewSelectedUnits` / `reviewUnitsKnown`）は触っていない。**
  単元名そのものが科目ごとに違うので1つの集合で両科目ぶんを持てる作りになっており、
  `syncReviewUnits()` が他科目の分を消さないよう明示的に配慮している（コメントに理由あり）。
  ROLE.md の「仕様を作り直すときは過去にその仕様にした理由を確認する」に従い、現状維持とした

#### 後方互換（既存の設定を消さないための処理）
既存ユーザーのlocalStorageはフラット形式なので、`loadSettings()` でフラットな値を読んだあと、
**まだ記録のない科目にはその値を初期値として入れる**（社会・理科の両方に引きつぐ）。
これがないと、この版に上げたとたんに今まで使っていた問題数やフィルタが初期値へ戻る。
保存側もフラットなキーを今までどおり書き続けているので、
**旧バージョンのindex.htmlがキャッシュから開かれても読める**（`units` を残しているのと同じ考え方）。

#### 検証したこと
`kosukequiz-battle\tools\test_settings_by_subject.mjs` に自動テストを置いた（**16件すべて成功**）。
このテストは **index.html から実物の関数を名前で切り出して読む**ので、コピーではなく実コードを検証する。
実行: `node test_settings_by_subject.mjs [index.htmlのパス]`（パス省略可）
1. 科目を切りかえたとき、問題数・フィルタがそれぞれの科目の前回値になる
2. **閉じて開き直したあとも、両方の科目の設定が残る**（localStorageを差しかえて再現）
3. 旧フラット形式が社会・理科の両方に引きつがれ、時間設定・公平モードは共通のまま
4. 新形式で保存したものを旧バージョン（フラットしか読まない）が読める
5. 保存データが壊れていても落ちず、既定値で動く

あわせて実ブラウザ（ヘッドレスChrome + ローカルHTTPサーバ）でも描画を確認:
JSエラーなし、単元バッジは **93 / 81 / 86 / 89 / 85 / 111問**（復習編1〜6）と正しく表示。

#### 追記: 復習ミックス側の絞り込みも科目別にした（司令塔の承認ずみ）
`reviewPriority` / `reviewLevel` / `reviewType` は当初「司令塔の指定リストに無い」として共通のまま
にしていたが、**メイン側の `priority`/`level`/`type` を科目別にしたので扱いが分かれてしまう**点を
確認に上げたところ承認された。ユーザーの要望も「問題数や**そのたのフィルタ**」だったので、
メイン側とそろえて科目別にした。

### 2026-09-07【重要】中断したひとり練習を「添字」ではなく「id」で保存するようにした

**問題を配列の途中に挿入できるようにするための前提条件。これが入るまで途中挿入は不可だった。**

#### 見つかった不具合
司令塔から「アプリに id の順序に依存したコードは無いか」と確認依頼があり、全体を調べたところ
**`kq_battle_solo_session_v1`（中断したひとり練習）だけが `QA_DATA` の「添字」を保存していた。**
```
旧: { quizQueue: [添字...], quizPos, quizResults: [{idx: 添字, ...}] }
```
配列の途中に1件挿入すると、それ以降の添字が全部ずれる。
→ 中断中のひとり練習がある状態でデータ更新が入ると、
**「前回の続きから再開する」で別の問題が出て、成績表示も別の問題に紐づく。**
末尾追加なら添字がずれないので、これまで表面化していなかった。

#### 調べた結果（司令塔へ報告ずみ。ここ以外に順序依存は無い）
- **id でソートしている箇所は0件。** `sort()` は2箇所だけで、どちらも `lastCorrectOf`（最後に正解した日時）順
- **id 文字列から順序や位置を推測している箇所も0件**（`parseInt`/`split`/`match` を id に当てていない）
- 履歴は id キー: `recordResult(id)` → `stats[id]`、`edits[base.id]`、
  `kq_battle_last_miss_v1`（idの配列）、対戦セッション `kq_battle_host_session_v1` の `questionIds`（idの配列）
- 出題順は `QA_DATA` の並び順で決まり、id とは独立

#### 直した内容
対戦側 `questionIds` と同じ形にそろえた。
```
新: { v: 2, quizIds: [id...], quizPos, quizResults: [{id, correct, recorded}] }
```
- `saveSoloSession()` … 添字を id に変換して保存
- `restoreSoloSession()` … id から添字を引き直す。**消えた問題は取りのぞき、`quizPos` も数え直す**
- **古い形式（`v` が無い＝添字保存）は読まずに捨てる。**
  中断中の練習が1回ぶんやり直しになるが、別の問題が出て成績が別の問題に紐づくよりよい、という判断
  （司令塔も同意）

#### 検証
`kosukequiz-battle\tools\test_solo_session_by_id.mjs`（**20件すべて成功**）。
実行: `node test_solo_session_by_id.mjs [index.htmlのパス]`
とくに**【2】問題を配列の途中に足しても、再開したとき同じ問題が出る**が本題のテスト。
ほかに、問題が消えたときの数え直し、古い形式の破棄、壊れたデータで落ちないこと、
保存内容に添字が残っていないことを確認している。

**→ この修正の配信をもって、data.js の配列の途中に問題を挿入してよくなる。**

### 2026-09-07 復習編1・2・7・8 を取り込み（QA_DATA 1411 → 1631件）

**復習編1は id フィールドを使って取り込んだ。位置から採番していない。**（下記「取り込みスクリプト」参照）

| 編 | 件数 | 内容 |
|---|---|---|
| 復習編1 | 93 → **102** | 既存93件はそのまま、新規9件（`kaki1_94`〜`102`）を**原文どおりの位置に挿入**。既存の内容差異は5件（`kaki1_71/72/89/90/91`、いずれも q のみ） |
| 復習編2 | 81 → **81** | 件数変わらず。内容差異7件（**`kaki2_42` は img のみ**、`56/58/75/76/77/81` は q） |
| 復習編7 | 0 → **91** | 全件新規 |
| 復習編8 | 0 → **120** | 全件新規 |

画像98枚を `images/` にコピー（復習編2=1枚 / 7=39枚 / 8=58枚）。

> **訂正（2026-09-07）**: 当初この表と、司令塔・claude-18 に送った報告で
> 「`kaki2_42` は q と img」と書いていたが、**正しくは img のみ**。
> q は取り込み前後で同一（どちらも「【地図】新幹線路線図中の①にあてはまる新幹線の名を答えなさい。」）で、
> `kaki_r2_map06.jpg` → `kaki_r2_map19.jpg` の差し替えだけ。git で前後を突き合わせて確定した。
> 元は claude-18 の基準値の書き間違いだが、**こちらの `--check` の出力は最初から img のみと出ていた**のに、
> 報告文では基準値の表をそのまま写してしまった。**自分のツールの出力を優先すべきだった。**
> 変更件数の合計12件（復習編1が5件・復習編2が7件）は変わらない。

#### 取り込み後に確認したこと
- 総件数 **1631件** / ユニークid 1631（**重複0**）
- 各編 102 / 81 / 86 / 89 / 85 / 111 / 91 / 120、**それ以外（通常回）866件は無変更**
- **全8編について、data.js の中身がフォルダJSONと id キーで完全一致（不一致0件）**
- 画像参照 **370種すべて実在、欠落0**
- **差分は 2352追加・12削除。** 削除12行は上表の変更5件＋7件ちょうどで、
  余計な整形差分は出ていない（既存の整形に合わせて書いたため。下記参照）
- ヘッドレスChromeで8編すべてが 102/81/86/89/85/111/91/120 と表示されること、JSエラー0を確認

### 2026-09-07 `kaki_r2_map06.jpg` を削除（ユーザー判断）

**理由: 赤丸が「秋田」の地名ラベルの上にあり、答えの「秋田新幹線」が画像から読めてしまうため。**
復習編2の取り込みで `kaki2_42` の img が `kaki_r2_map19.jpg`（赤丸を路線に付け直した版）に
差し替わり未参照になったもの。**残しておくと誤って再参照され、直したはずの不具合が復活する**ので、
司令塔経由でユーザーに諮り「消す」との判断を得た。

削除前に data.js・index.html・元データJSON・その他ファイルを検索し、参照が無いことを確認。
`index.html` の `IMG_SIZES`（画像の幅・高さの表）にだけ名前が残っていたので、そこも外した。
ファイルが無いのに名前が残っていると、次の担当が「まだ使われている」と誤解するため。

**復習編4の旧画像4枚（`kaki_r4_graph02` / `map01` / `map02` / `shapes01`）は「残す」のまま。**
以前ユーザーが残すと決めたもので、取り込み状況とは無関係の判断。**削除しないこと。**

### 2026-09-07 仕事3 完了: 二人で同時にこたえを開く／正解音／ホストの合図音

ユーザー要望の3点。**新しいタイマーは作っていない**（「一緒にオープンという体験だけ追加してほしい」との
追加回答があったため）。既存の「チーズを食べ終わったら自動で開く」タイマーはそのまま。

#### 1. こたえは二人そろってから開く
`revealAction` を追加し、「こたえを見る」を押したら相手に合図を送る。**両方が押して初めて開く。**
- 先に押した側: ボタンが「相手を待っています…」になり押せなくなる
- 待っている側: ボタンが「相手は準備OK！ こたえを見る」に変わり色がつく
- **片方が押さないまま時間が来たら、これまでどおり自動で開く。**固まらない
- ひとりのとき（相手がいない）は待たずに開く

**「押した／押していない」ではなく「何問目に対して押したか」を持たせている。**
フラグだと、相手の合図が自分の数えなおしより先に届いたときに消えてしまうため
（自動テストの【4】がまさにこれを検出した。最初の実装は消えていた）。
ラウンドの1問目だけは、前のラウンドの合図と取りちがえないよう全部消す。

#### 2. 正解したら、正解した側の端末で「ピンポン」を鳴らす
`updateMyResultUI()` で自分の結果が〇のときに `playChime()`。
この関数はそれぞれの端末が自分の結果を出すところなので、**相手の端末では鳴らない。**

#### 3. ホストが「わかった！」を押したら、ゲスト側でも鳴らす
ゲストが `nextAction` / `startAction`（1問目）を受けたときに `playChime()`。
**再接続の追いつき（resync）では鳴らさない。**

#### 音が重ならないようにした
正解の「ピンポン」（約0.7秒）が鳴っている間にアタック音が来ると、どちらも聞き取りにくい。
`playAttackSound()` は、鳴り終わってから出すようにした。

#### ★正誤の自動判定は入れていない（claude-18 の申し送りによる）
**答えが機械的に判定できない問題が混ざっている**ため、既存の自己申告（〇わかった／✕まちがえた）
のままにした。文字列一致で判定すると次のような問題で誤判定する:
- 記述式: `kaki8_110`（やませの特徴）、`kaki1_86`（キャベツを処分する理由）など全8編で十数問
- 答えが2部構成: `kaki6_105`「都市＝（サ）浜松、位置＝（9）です。」
- 複数正解: `kaki7_90`「（A・E・D）です。」（順番も答えの一部）

#### 検証
`kosukequiz-battle\tools\test_reveal_sync.mjs`（**21件すべて成功**）。
**2台の端末を模して**、実物の `requestReveal` / `maybeReveal` / `resetRevealSync` と受信ハンドラを
index.html から切り出して動かす。どちらが先に押しても、二人そろうまで開かないことを確認。

#### ⚠️ 実機（お子さんのスマホ）でしか確かめられないこと
**iOS/Safari は、その端末で一度も画面に触っていないと `AudioContext` が鳴りません。**
「相手の操作をきっかけに自分の端末で鳴らす」3番は、受け手が対戦中に一度も画面に触っていないと
鳴らない可能性があります。ただし対戦では判定ボタンなどを押すので、実際にはほぼ問題ないはずです。
**実機で確認をお願いする箇所として明示しておきます。**

### 2026-09-08 長い問題文57問に改行を入れたものを取り込んだ

作成担当 `claude-45` が、150字以上の57問すべてに改行を入れたもの。**q のみ・id は動かない。**
検証は編別 kaki1=3 / kaki2=0 / kaki3=8 / kaki4=5 / kaki5=23 / kaki6=13 / kaki7=2 / kaki8=3 ＝**57件**で、
司令塔・claude-45 の内訳と完全一致。**語句そのものの変更0件**（空白・改行・「／」を無視して比較）。
差分は **57行の置換のみ**（追加57・削除57）で、余計な変更なし。

#### 表示確認でやったこと（次に同じ確認をする人へ）
1. **`.wrap` を390pxに固定し、実際に何pxで描かれたかを数値で確認した。**
   ヘッドレスChromeは `--window-size=390` を指定してもビューポート485pxで描くことがある（D-9の補足）
2. **問題文は `.card` の中に入るので、本文の実効幅は314px。**器の外で測ると答えがずれる
3. **行数の計算だけでは食い違いが出たので、最後はスクリーンショットで目視した。**
   計算では「1行に収まる」と出た行が、実際には折り返していた

#### ★見つかった問題（9問。作成担当が対応中）
`kaki4_83`・`kaki5_67`・`kaki6_105`〜`111` の**グループ列（記号＋語を1行に並べた行）が折り返し、
記号と語が分断される**。
```
「カ 福井　キ 輪島　ク 岡谷　ケ 瀬戸　コ」／次の行に「燕」
「サ 浜松　シ 岡崎　ス 静岡　セ 甲府　ソ」／次の行に「岐阜」
```
**390px幅では1行あたり全角22字程度が限界**（23字でも溢れた）。1行4個なら収まることを実測で確認。

**取り込みは止めなかった。**9問も改行を入れた今のほうが入れる前より読みやすく、悪化する問題は0だったため
（司令塔も同判断）。**「完璧になるまで待つ」より「悪化ゼロで前進する」**を選んだ。

#### ⚠️ 取り込み中に元データが書きかわった（2件）
取り込み後の照合で `kaki4_83`・`kaki5_67` の2件だけ、data.js とフォルダJSONが一致しなかった。
**作成担当が上記9問の修正を進めている最中で、こちらが読んだあとに元データが変わったため。**
不具合ではないが、**取り込みスクリプトは「元データが編集中かどうか」を見ていない。**
→ **元データを直している最中は取り込まない**か、取り込み後に必ず照合すること（今回は照合で気づけた）。
→ 作成担当の修正が終わったら**もう一度取り込めば揃う**（q のみ・id 据え置きなので何度でも安全）。

### 2026-09-08 正解音を作り直した（実機フィードバック対応・2回目）

> **音はもうちょっと正解した感じが欲しいです。ピンポーンくらいの**

不正解音への苦情ではなく、**正解音が物足りない**という趣旨。玄関チャイムに寄せて作り直した。

| | 前 | 後 |
|---|---|---|
| 音程 | 880 → 659.25Hz（**完全4度**） | **784 → 523.25Hz（G5→C5・完全5度）** |
| 長さ | 0.70秒（0.32＋0.42） | **0.94秒（0.34＋0.68）** |
| 音量 | 0.30 | **0.42** |
| 倍音 | なし | **オクターブ上を倍率0.28で重ねる** |

変えた理由:
1. **音の間隔を広げた。** 玄関チャイムは完全5度で、この間隔だとはっきり「ピンポーン」と聞こえる
2. **2音目の余韻を伸ばした**（0.42→0.68秒）。「鳴り終わった」感じが出る
3. **音量を上げた。**不正解音（0.16）との差が開くほど「当たった」感が出る
4. **オクターブ上の音を小さく重ねた。**鐘らしい響きになる

`playNotes()` に、音符ごとの音量倍率 `g` を足した（倍音を小さく重ねるため）。

#### 変えていないもの（司令塔の指示どおり）
- **不正解音**（392→294Hz・0.32秒・音量0.16）… 苦情が出ていないのでそのまま
- **わかった通知**（1046Hz 単音0.10秒）… 「1音か2音か」で区別する設計を維持
- **3種類が同時に鳴らない仕組み**（`queueSound`）… 維持。正解音が0.94秒に伸びたぶん、
  あとに続く音の待ち時間も自動で伸びる（`CHIME_MS` を950に更新）

#### 検証
`test_sounds.mjs` の期待値を更新（**23件すべて成功**）。
**「不正解音が正解音より小さく・短い」という関係は維持**されている（0.16対0.42、0.32秒対0.94秒）。
自動テストは合計108件、全通過。

#### まだ物足りない／うるさい場合の調整先（次の担当へ）
`playChime()` の中の数値だけで調整できる。
- **もっと明るく**: 音量 0.42→0.50、倍音の倍率 0.28→0.38
- **もっと控えめに**: 音量 0.42→0.34、倍音の倍率 0.28→0.20
- **もっと余韻を長く**: 2音目の `d` を 0.68→0.85（`CHIME_MS` も同じだけ伸ばすこと）
- **音程をさらに広げる**: 784→392（オクターブ）。ただし低くなりすぎて締まらない可能性あり

### 2026-09-08 旧単元289問を消し、182問の記録を復習編に引きついだ

夏期講習の復習編5〜8に置きかわった旧単元を data.js から消し、端末に残っている正誤の記録を
新しい問題に移した。**QA_DATA 1631 → 1342問。**

| | 件数 |
|---|---|
| 消した旧単元 | 5.近畿地方57 / 6.中部地方78 / 7.関東地方66 / 8.東北地方56 / 8.北海道地方32 ＝ **289** |
| **記録を引きついだ** | **182（62%）** |
| 1対多で見送り | 53 |
| 対応が見つからず記録を捨てた | 54 |
| 複数の旧問題が同じ新問題を指すもの | 9件 |

**前回（`328af80`）の87%は包含一致で埋めた数字。今回の62%はすべて完全一致で、包含一致は0件。**

#### ★対応づけの判定（次に同種の移行をする人へ）
「答えの完全一致」を基本に、**括弧の直後の語が「種類を表す語」かどうか**で場合分けする。
括弧の中だけで比べると別物が混ざり、括弧の外まで厳密に比べると取りこぼす。その中間が要る。

| 旧の直後 | 新の直後 | 判定 | 例 |
|---|---|---|---|
| `県` | `用水` | **別物** | 「（愛知）県」と「（愛知）用水」 |
| `が行われています` | `地帯` | **同じ** | 旧が種類の語ではなく文の続き |
| `盆地` | （空） | **別物** | 「（諏訪）盆地」と「（諏訪）」＝諏訪市 |
| `市` | （空） | **同じ** | 市・村・町は新側で省かれる |

さらに次の3つが要る。**どれも実データで踏んだ。**
1. 位置番号は丸数字だけでなく**半角数字**もある（`（21天竜）川`）
2. 答えの括弧が2つあることがある（`（奈良県）です。（和歌山県に入ると…）`）→ **最初の括弧が答え**
3. 括弧の切れ目が旧と新でずれる（`（奥羽山脈）です` と `（奥羽）山脈です`）
   → **括弧の中と直後の語をつないでから**比べる

安全弁は「候補が1件のときにかける」のではなく、**候補をしぼる道具として先に使う**。
そうしないと候補が2件あるだけで見送られる（`（甲府）` と `（甲府）盆地` の2件から盆地を落とせば決まる）。

#### ★5回作り直した。毎回ちがう誤りが出た
| 回 | 件数 | 見つかった誤り | 誰が |
|---|---|---|---|
| 1 | 125 | `大阪→[大阪平野, 大阪湾]` 別物への紐づけ | 自分 |
| 2 | 77 | 厳しすぎて `奥羽山脈`・`若狭湾` が落ちた | 自分 |
| 3 | 115 | 半角数字・二重括弧・「市」の位置ずれ | claude-18 |
| 4 | 175 | **`（愛知）県`→`（愛知）用水`** | 自分 |
| 5 | **182** | **`（単作）が行われています` を誤って見送り** | claude-18 |

**4回目の誤りは claude-18 の提案に含まれ、5回目の誤りは自分の対処に含まれていた。**
どちらも相手が見つけた。**自分の表は自分では甘く見る。作成者と検証者は分けること。**

#### ★★移行は一覧で持ち、前の分を消さないこと（claude-18 の指摘で判明）
最初は `KAKI_MIGRATION_ID` を `kaki1-4` → `kaki5-8` に**書きかえて**しまい、
**前回の引きつぎ表（195件）をコードから消していた。**

**まだ前回の移行を済ませていない端末があると、旧1〜4の記録は引きつがれないまま消える。**
（旧1〜4は `328af80` で data.js から消えているので、片付けの対象になる）
「お子さんの端末は前回の移行が済んでいる」は**確認した事実ではなく推測だった。**
しかも対戦アプリなので**端末は2台**あり、別のブラウザで開いた可能性もある。

→ **移行を一覧 `KAKI_MIGRATIONS` で持ち、`done` に無いものを古い順に走らせる形にした。**
```js
const KAKI_MIGRATIONS = [
  { id: "kaki1-4", renames: {…4件}, stats: {…195件} },   // ★消さずに残す
  { id: "kaki5-8", renames: {…5件}, stats: {…182件} },
];
```
**次に復習編を作り直すときも、ここへ足すだけ。前の分は消さないこと。**

#### ★★片付けは、全部の移行が終わってから1回だけ（ここが罠）
消えた問題の記録の削除・中断セッションの片付けを、**各移行の中でやってはいけない。**
2つとも未実施の端末では、**先に走る `kaki1-4` の片付けが、次に走る `kaki5-8` の
引きつぎ元（旧5〜8の記録）をまるごと消してしまう。**
→ `cleanupAfterMigrations()` に出し、**全部の移行が終わってから1回だけ**呼ぶ。
→ テスト【5b】がこれを固定している。**片付けを移行の中に戻すと落ちる。**

#### ★バックアップの鍵を移行ごとに変えた（重大な穴だった）
移行前の記録は `kq_battle_stats_backup_kaki_v1` に退避される作りだったが、
**`if(!既存) 書く` だったため、前回の移行の退避が残っていて今回ぶんが退避されなかった。**
移行は一度きり実行で配信後は取り消せないので、**退避が無いと手で戻す道も消える。**
→ **鍵を `..._kaki5-8` のように移行IDつきにした。**前回の退避も上書きされない。

#### 検証
`tools\test_migration.mjs`（**22件すべて成功**）。実ブラウザで移行を走らせて確かめる。
index.html から移行の一覧をそのまま読むので、テスト側に表を書き写していない。
- **移行前の記録が、この移行ID専用の鍵で退避されること**
- 引きつぎ表どおりに記録が移ること
- **data.js に無い旧問題の記録が消えること**（幽霊レコードを残さない）
- 単元の選択が新しい単元名に置きかわること
- **引きつぎ先にすでに記録があるとき、それが消えず合算されること**
  ← **実機の状態そのもの。**お子さんは復習編5〜8をすでに解いているので、
  　 引きつぎ先の多くにすでに記録がある。上書きする作りなら、積み上げた記録が消えていた
- **前回の移行を済ませた端末でも、今回ぶんが退避されること**
- **★2つとも移行していない端末で、両方が正しく走ること**（片付けの罠を固定）
- **もう一度開いても二重に加算されないこと**

あわせて、data.js 側は総件数1342・id重複0・画像参照358種の欠落0、
**移行表の引きつぎ元が data.js に残っていない／引きつぎ先が data.js に無い、がどちらも0件**。

#### 副次効果
**優先度・難易度の未設定が463問 → 公民174問だけになった。**

#### ★この案件でいちばん重い事実: テストが134件通っていても出てこない誤りがあった
**「前回の移行表をコードから消していた」**という誤りは、
**自作テスト・smoke-test あわせて134件が全部通っている状態**で残っていた。
テストは「いま実行されるコードが正しく動くか」を見るもので、
**「まだその移行を済ませていない端末がある」という前提の欠落**は見つけられない。

見つけたのは claude-18（レビュー）で、**私は最後まで見えていなかった。**
逆に「片付けを移行の中でやると次の移行の引きつぎ元が消える」という罠は、
**claude-18 の指摘は机上の推論で、こちらが実装して初めて確定した。**

→ **作成者と検証者を分ける。どちらか一方だけでは足りない。**
→ **配信版はバイト列で比べる。**claude-18 が data.js と index.html を
　 LF に正規化して手元と突き合わせ、**1バイトの過不足も無いこと**を示した。
　 件数や表を数えて合わせるより強い保証になる。

#### ★実機で記録を確認するときの注意
複数の旧問題から記録が合流する9件（`kaki5_47` 京都市 ← `t5-21`＋`t5-37` など）は、
引きつぎが働いていれば正解数が確実に増えるので確認に向く。ただし
**もともと新問題として解いている分に足されるので、「旧2問ぶんの合計」より多くなる。**
**数が想定より多くても正常。**

#### 消したことで参照されなくなった画像12枚は削除していない（C-3）
配信が行き渡るまで旧版が参照するため。

### 2026-09-08 一覧が重かったのを直した（実機で最大10倍以上速く）

> 読み込みに時間がかかって、ボタンの反応が悪い（ユーザー・実機は低スペックAndroid）

#### ★まず測った。推測で直さなかった
CPUを絞って（CDP の `Emulation.setCPUThrottlingRate`）測ったところ、
**PCですら一覧を開くのに9.5秒、CPU4倍で46.9秒**かかっていた。
道具は `tools/perf-measure.mjs`（操作ごとの時間）と `tools/perf-profile.mjs`（関数ごとの内訳）。

#### ★見立てを2回外した。プロファイラで真因にたどり着いた
**1回目の見立て「1件ごとのリスナー登録が重い」→ 外れ。**
`renderListItems` は1件につきリスナー10個・`querySelector` 12回を実行しており、
1631問だと**リスナー16,310個**になる。ここを**イベント委譲**にして2個に減らしたが、
**9,493 → 9,493ms でほとんど変わらなかった。**

**プロファイラで測った内訳:**
| | 時間 | 割合 |
|---|---|---|
| **(program)＝ブラウザのDOM構築・レイアウト** | **8,937ms** | **81.3%** |
| 1件ごとのJavaScript | 1,229ms | 11.2% |
| `primeAudio` | 602ms | 5.5% |

**JavaScriptは11%しかなく、81%はブラウザ側だった。**だからJSを減らしても効かなかった。
実測すると **1行36要素 × 1541行 ＝ 58,797要素**。低スペック端末には重すぎる。

#### 効いた対策: `content-visibility: auto`（CSS 2行）
```css
.list-item{ content-visibility:auto; contain-intrinsic-size:auto 400px; }
```
**画面の外にある行は、ブラウザがレイアウトと描画を省く。**
DOMもJavaScriptも触らないので、**体験は1ミリも変わらない。**
`contain-intrinsic-size` は**実測した行の高さ**（1541行の中央値394px・平均449px）。
ここが実際と離れているとスクロールバーの長さがずれる。

| 操作（CPU4倍＝実機の目安） | 前 | 後 |
|---|---|---|
| 一覧を開く | 46,922ms | **5,613ms（−88%）** |
| 絞りこみ | 17,427ms | **1,360ms（−92%）** |
| 絞りこみをもどす | 28,074ms | **1,786ms（−94%）** |
| 単元1つだけ | 1,071ms | **230ms（−79%）** |
| 「一人で始める」 | 664ms | **67ms（−90%）** |
| 「〇わかった」 | 382ms | **82ms（−79%）** |

**実描画で確認ずみ**（C-4d）。4000pxスクロールした先も正常に出る。見た目の崩れなし。

#### あわせて直した: `primeAudio` の約0.7秒
`AudioContext` を作るのに実測で約0.7秒かかり、それを**最初のタップの中でやっていた**。
「最初だけ反応が鈍い」の原因。**読み込みのときに先に作る**ようにし、
最初のタップでは `resume()` だけにした（一度つながったらリスナーを外す）。
→ 科目を切りかえる 339→68ms。

#### ⚠️ イベント委譲について（誤解しないように）
**速度目的で入れたが、効果は測れなかった**（9,493→9,493ms）。
**残した理由は、速度ではなくメモリとコードの簡潔さ**:
リスナー16,310個ぶんのメモリが減り、行ごとの `querySelector` 12回も消えた。
**「これで速くなったはず」と思わないこと。**速くしたのは `content-visibility` のほう。

#### まだ目標に届いていないもの
| 操作（CPU4倍） | 実測 | 目安 |
|---|---|---|
| 一覧を開く（全1541件） | 5,613ms | 500ms |
| 起動〜単元が出るまで | 6,707ms | 3,000ms |

残りの内訳は **(program) 42% ＋ 1件ごとのHTML文字列の組み立て 33%**。
**全件を一度に出すかぎり、1行1,603文字×1541行の組み立ては消えない。**
ここから先は**表示件数を区切る（100件ずつ「もっと見る」）**しかないが、**体験が変わる**ので
ユーザー判断待ち。**絞りこみを使えば目標内**（単元1つ230ms）なので、急ぎではない。

### 2026-09-08 問題文・答えの改行を表示できるようにした

**チャレンジ問題の引用など、選択肢ア〜エが並ぶ長い問題文が1行に潰れてスマホで読めなかった。**
これまで問題文の要素に `white-space` の指定が無く、データに改行を入れても
**スペースに潰れて表示されていた**（`pre-wrap` は `.error-note` / `.sol-text` /
`.list-item .list-sol` にしか付いていなかった）。

#### 付けた場所（表示箇所すべて）
| 場所 | 要素 |
|---|---|
| 対戦・ひとり練習の問題文 | `.q-text` |
| 対戦・ひとり練習の答え | `.a-text` |
| 問題一覧の問題文・答え | `.list-item .list-q` / `.list-a` |
| 結果画面の「まちがえた問題」 | `.miss-item .q` / `.a` |
| 相手がスキップした問題の本文 | `.wrapped-q`（新設） |

**最後の1つは司令塔の依頼に挙がっていなかったが、ここも問題文を出しているので付けた。**
お知らせの文面まで `pre-wrap` にならないよう、問題文だけを `<span class="wrapped-q">` で包んでいる。

#### 確認したこと
- **全1631問で、q・a・sol・note に改行・CR・タブ・連続スペースは1つも無い**
  → **この変更だけでは見た目は何も変わらない**（司令塔の前提どおり）
- 実際に改行を入れた見本を実CSSで描画し、選択肢が行ごとに分かれることを確認
- 自動テスト106件すべて通過、JS構文チェックOK

#### ★作成担当に伝えるべき注意（改行を入れる前に）
**CSV取り込みの「問題文で照合する」経路が、改行を入れた問題では効かなくなる。**
- `els["import-file"]` の取り込みは、まず **ID で照合**し、**IDが合わないときだけ
  「単元＋問題文」で照合**する（`byQuestion`）
- 改行を入れると問題文の文字列が変わるので、**改行を入れる前に書き出したCSV**や
  **元アプリ kosukequiz のCSV**からの取り込みで、その問題だけ照合できなくなる
- ただし **ID が一致すればそちらが優先**されるので、このアプリ同士の書き出し／取り込みは影響なし。
  影響するのは「ID が違う別アプリのCSVを、問題文で突き合わせて取り込む」場合だけ

CSVの書き出し側（`csvEscape`）は改行を引用符で囲むので、**書き出し自体は壊れない。**
読み込み側（`parseCSV`）も引用符の中の改行を正しく扱える。**往復して同じになる。**

### 2026-09-07 音を3種類に分けた（実機フィードバック対応）

対戦して使ってみたお子さんの感想:
> **わかった、と正解の音が同じなのが気に入らない。**あとは**ずれの時の音もつけてほしい**

#### ★どの場面でどの音が鳴るか（実機で確かめるときの手引き）
※ 正解音は 2026-09-08 に作り直した。最新の値は下の「正解音を作り直した」の節を見ること。

| 場面 | 音 | 鳴る端末 | 中身 |
|---|---|---|---|
| ホストが「わかった！」を押した | **わかった通知** | **両方**（ホストは押した瞬間、ゲストは合図が届いた瞬間） | **高い単音1発**「コン」1046Hz・0.10秒・音量0.22 |
| 相手に〇と判定された | **正解音** | **正解した側だけ** | **2音「ピンポーン」784→523Hz・0.94秒・音量0.42**（倍音つき） |
| 相手に✕と判定された | **不正解音** | **不正解だった側だけ** | **低めの下がる2音**392→294Hz・0.32秒・音量0.16 |
| ホストが正解しゲストが不正解 | アタック音（既存） | 両方 | 220→55Hzに下がる「ボン」0.25秒 |
| 再接続の追いつき（resync） | **鳴らない** | ― | ― |

#### 1. 「わかった」の通知音を正解音と別にした
これまで両方とも `playChime()`（2音のピンポン）で、**鳴っても何の音か分からなかった。**
性質が違う音なので分けた。
- **正解音** … 「自分が正解した」という評価。うれしい音
- **わかった通知** … 「相手が答えた・次に進む」という状況の合図。うれしさとは無関係

**2音か1音かで瞬時に区別できる**ようにしてある。音の高さだけの違いだと対戦中は聞き分けにくいため。

#### 2. 不正解音を足した
正解音と対の位置づけ（自分の結果に対して鳴り、相手の端末では鳴らない）。

**きつい音にしていない。** このアプリは `index.html` のコメントにあるとおり
「**HPも敗北も置かない**」方針で作られている。クイズ番組のブザーのような強い否定の音は方針と衝突する。
- 低めの2音を**下がる形**で、**正解音より小さく（0.16 対 0.30）・短く（0.32秒 対 0.70秒）**
- やわらかい sine 波（アタック音のような square にはしない）
- 「まちがえた」を責める音ではなく、**「はい、次」という区切り**のつもり

#### 3. 音の重なり制御を作り直した
音が3種類になったので、**前の音が鳴り終わってから次を出す**仕組み（`queueSound`）に一般化した。
以前は「正解音の最中はアタック音を待たせる」という個別対応だった。
**「自分が正解した直後にホストの『わかった』が飛んでくる」**場面が実際にあり得るので、そこも塞いである。

#### 検証
`kosukequiz-battle\tools\test_sounds.mjs`（**21件すべて成功**）。
AudioContext を偽物に差しかえ、**いつ・どの高さの音が・どれだけ鳴ったか**を記録して確かめる。
- 3つの音が別物であること（**音の数が違う**ので耳で区別できる）
- **不正解音が正解音より小さく・短く・やわらかい**こと
- **3つ続けて来ても順番に鳴る**こと（正解音→アタック音→通知が 0ms / 700ms / 950ms）
- 音が使えない端末でも落ちないこと

**自動テストは合計106件**（16＋20＋21＋28＋21）になり、全通過。

#### ⚠️ 実機で確かめてほしいこと
- **音の温度感**（不正解音がきつくないか、3つを聞き分けられるか）は鳴らしてみないと分からない
- iOS/Safari は一度も画面に触っていないと鳴らない（仕事3と同じ）

### 2026-09-07 問題一覧に優先度・難易度を表示し、絞りこみにも追加した

ユーザー要望「難易度とか優先度ってどのくらいの問題に入ってますか？　問題一覧で見れるように
できますか？　問題一覧のフィルタにも足してほしいです」への対応。

#### 表示
各行の単元名の下に、優先度と難易度のふだを出す。値ごとに色分け（高=赤系／中=橙系／低=灰系、
基礎=緑系／標準=青系／発展=紫系）。**データに無いものは「優先度・難易度 未設定」を薄いふだで出す。**

#### 絞りこみ
「優先度」「難易度」の行を追加。**出題設定側の `priority`/`level` と同じ「ひとつ選ぶ」形にそろえた。**
- 優先度: すべて／高／中／低／**未設定**
- 難易度: すべて／基礎／標準／発展／**未設定**

**「未設定」を選べるようにしたのが肝。** 1631問中463問が未設定なので、
これが無いと絞りこんだとたんに463問が消えて混乱する。

一覧のほかの絞りこみと同じく、**一覧を開くたびにリセット**される（出題設定側とは独立）。

#### 実データの内訳（司令塔の集計と一致することを確認）
- **1631問中1168問に設定あり、463問が未設定**
- 優先度: 高 802 ／ 中 356 ／ 低 10 ／ 未設定 463
- 難易度: 基礎 672 ／ 標準 410 ／ 発展 86 ／ 未設定 463
- **優先度と難易度は必ず両方そろって有るか無いかで、片方だけ欠けているものは0件**
  （だから未設定はふだ1つにまとめている。片方だけの分岐も一応用意してある）
- 未設定463問の内訳は**旧単元289問＋公民174問**だけ。
  **→ 旧単元を整理すれば、未設定は公民174問だけになる**（旧単元整理の副次的な効果）

#### 検証
`kosukequiz-battle\tools\test_list_meta_filter.mjs`（**28件すべて成功**）。
**実物の data.js（1631問）を相手に**、index.html から切り出した実関数で数える。
上の内訳がすべて一致すること、「未設定」を選べないと463問に届かないこと、
空文字も未設定として扱うことを確認している。

見た目は実CSSでプレビューを作って確認。**390px幅で5つ目の「未設定」まで1行に収まる。**
320px では2行になるが、既存の絞りこみ行（「未クリアの問題」など）も同じ幅で2行になるので、
デザインとして一貫している。

### 2026-09-07 理科第2回の図版40枚を差し替え（作成担当 `quiz-csv-f3` からの依頼）

**印刷して紙で解いたとき「図だけでは解けない問題」があった**ため、理科クイズ作成担当が
**大問のリード文＋全図＋その大問の小問を1枚に含める**形で切り出し直した19枚を、
旧ファイル名すべてに同内容で書き込んだもの（計40枚）。

- コピー元: `5年下\quiz_csv_理科\画像プレビュー\` の `r2_*.jpg` 40枚 → `images\` へ**上書き**
- **40枚すべて中身も寸法も変わった**（例: `r2_01.jpg` 689×281 → 1400×673）
- **旧画像は1枚も削除していない**（C-3）。上書きなので旧版は git 履歴に残る。
  作成担当側にも控えあり（`quiz_csv_理科\_backup_旧画像_20260907\preview\`）
- **問題idは1つも変えていない**（C-5）

#### data.js の変更は1件だけ
`r2m16` の img: `r2_29.jpg` → **`r2_26.jpg`**（演習17P 練習問題2 全体の図）。
`r2_29.jpg` にも同じ内容が書き込まれているので**これは代表名に寄せるだけの整理**で、
直さなくても不具合は解消している。`r2m17` の `r2_40.jpg` は名前そのままで中身が改善（変更不要）。

**→ C-6 のとおり、元データ `第2回_てこ滑車輪軸.json` 側にも書き戻しが必要。
　 元データを触るのは作成担当の担当なので、`quiz-csv-f3` に「data.js をこう直した」と連絡ずみ。**

#### ★ここで危なかった点: `IMG_SIZES` の値が古いまま残るところだった
`update_img_sizes()` は当初**足りないものを足すだけ**で、**寸法が変わったものを直さなかった。**
画像を切り出し直すと寸法が変わるので、**登録されているのに実物と違う**状態になり、
未登録より悪い（間違った幅・高さが `<img>` に付く）。
→ **寸法が変わったものも直すように修正した。**
→ さらに、`update_img_sizes()` は **data.js が参照する画像しか見ない**ため、
　 参照されていない31枚の登録値が古いまま残った。**実ファイルと全件突き合わせてそろえた。**

検証: 総件数1631・重複0、画像参照370種の欠落0、**IMG_SIZES 未登録0・実寸とのずれ0**、
削除ファイル0、自動テスト57件（16+20+21）全通過、ヘッドレスChromeでJSエラー0。

#### 私が実施していないこと
`keisan-print-app\images\` への同じ40枚のコピーは、**当セッションの担当フォルダ外なので実施していない。**
（作成担当は権限でブロックされ、私に代行を依頼してきたが、司令塔が整理してユーザー判断に回した）
**印刷アプリは独自の画像フォルダを読んでいるので、そこを更新しないと紙に出る図は変わらない。**

### 2026-09-07 `IMG_SIZES` に画像226枚の幅・高さを追加（読み込み中のがたつき解消）

`index.html` の `IMG_SIZES` は、画像の幅・高さを `<img>` に付けて**読み込み中のがたつきを防ぐ**表。
data.js が参照する370枚のうち **226枚が未登録**だった。未登録でも表示は壊れないが
（`IMG_SIZESに無い画像はそのまま`という作り）、**スマホで画像の読み込み中に文字が飛ぶ。**

| 未登録だった内訳 | 枚数 | いつから |
|---|---|---|
| kaki_r4 / r5 / r6 | 39 / 38 / 51 | 前任 claude-ed の取り込み時から |
| kaki_r7 / r8 | 39 / 58 | 今回の取り込みから |
| kaki_r2_map19 | 1 | 今回 |

PIL で実ファイルを読んで埋めた。**IMG_SIZES 196件 → 422件。**
- **未登録0件**（data.js の参照370枚すべてに幅・高さが付く）
- **IMG_SIZESにあるのに実ファイルが無いもの0件**
- **既存196件の値も実ファイルの寸法と全件一致**していた（ずれ0件。ついでに確認した）
- 差分は `IMG_SIZES` の1行だけ。ロジックには触っていない

#### ★再発防止: 取り込みスクリプトに組み込んだ
**128枚が前任の取り込みから残っていたのは、手順に入っていなかったのが原因。**
`import_quiz.py` に `update_img_sizes()` を追加し、`--apply` のときに自動で埋めるようにした。
`--check` でも「IMG_SIZES に足りない幅・高さ: N件」と出る。**もう手順から漏れない。**

### ★取り込みスクリプト `kosukequiz-battle\tools\import_quiz.py`（次の担当へ）

前任のスクリプトが一時領域で消えていたので作り直した。**消えない場所に置いてある。**
```
python import_quiz.py --list        # 全編の状態を一覧
python import_quiz.py --check 1     # 取り込まずに検証だけする（必ず先にこれ）
python import_quiz.py --apply 1     # 実際に data.js を書きかえる
```

#### いちばん大事な決めごと: **id は JSON の `id` フィールドを使う。位置から採番しない**
復習編1では **102件中51件で「id と配列位置」がずれている**（原文の位置に9件挿入したため）。
位置から採番すると **既存93問のうち42問の中身が別の問題に入れかわり、解答履歴が別の問題に紐づく**
（claude-18 の実測）。
→ **`id` フィールドが無い元データは、既定でエラーにして止まる。**
　 位置採番は `--allow-position-ids` を明示したときだけ。黙って採番するほうが危ないため。
　 現在は復習編1〜8すべてに `id` があるので、この分岐に入る編は無い。

#### 安全装置
- **起動時に data.js の全ブロックを読めるか自己検査する**（読めなければ止まる）
- **検証はすべて id キー。位置での照合は使わない**
  … 消えた既存id・id重複・他単元との衝突・他単元が無変更か・既存idの内容差異・画像の欠落
- **その編の既存ブロックと同じ整形に合わせて書く**
  … data.js には「最後の項目のうしろにカンマが付く」ブロック（復習編1〜3）と
  　 付かないブロック（復習編4〜6）がまざっている。合わせないと、中身を変えていない問題まで
  　 差分に出てしまう（ROLE.md「元の整形を保ったまま該当ブロックだけ触る」に反する）。
  　 整形が既存と何件一致するかもレポートに出るので、そこがずれたら気づける
- 画像は data.js を書きかえる**前に**コピーする。元データフォルダにも無ければ止まる
- 書き込み後にもう一度読み直して照合し、合わなければ控え（`data.js.bak`）から戻す
- 対象の編以外のブロックは**生テキストのまま素通し**する。書き直さない

同じフォルダに claude-45 の `diff_dep.py`（deployed とフォルダJSONの差分チェック、idキー対応）もある。

### ★このフォルダの自動テスト（次の担当へ）
**`tools\` に移してgit管理下に入れた**（2026-09-08）。**index.html を編集したら全部走らせること。**
実物の関数を index.html から名前で切り出して動かすので、コピーではなく実コードを検証する。
行番号に依存していないので、index.html を編集してもズレない。**詳しい一覧は README を見ること。**
```
node tools\test_settings_by_subject.mjs   (16件)
node tools\test_solo_session_by_id.mjs    (20件)
node tools\test_reveal_sync.mjs           (21件)
node tools\test_list_meta_filter.mjs      (28件)
node tools\test_sounds.mjs                (23件)
```
以前は `C:\Users\真理子\scratch_kaki\` に置いていたが、**あそこは一時領域で消える。**
実際、前任の取り込みスクリプトはセッション終了で消えて作り直しになった。
作成担当の `diff_dep.py` / `addid.py` は引き続きあちらにある（あちらの担当分）。

### 次にやること（優先順）
2. ~~問題数・フィルタなどの設定を科目別に保存する~~ → **完了（上記）**
3. 二人対戦の新機能3点（回答の同時オープン・正解音・わかった通知音）
4. 復習編2の取り込み（claude-45 が指摘7件反映ずみ、差分チェック完了。取り込み前に C-6 の
   「deployed とフォルダJSONの差分確認（フォルダ側が古くないか）」を必ず行う）

#### 仕事2の内容（司令塔から訂正あり）
**対象は単元選択ではない。単元選択 `unitsBySubject` は `fd828e2` で科目別になっており正しく動いている。**
問題は `saveSettings()` で **`unitsBySubject` だけが科目別で、他が社会・理科で共通**になっていること。
- 科目別にする: `count`（問題数）/ `filterUnmastered` / `filterWeak` / `type` / `priority` / `level` /
  `shuffle` / `reviewMixCount` / `reviewMode`
- 共通のままでよい（対戦の進行・端末の好み）: `fairMode` / `headStartSec` / `answerTimeSec` / `skipNextTimeSec`
- **後方互換に注意**: 既存のlocalStorageはフラット形式。読み込み時に
  「フラットな値しかない場合は社会・理科の両方の初期値として引き継ぐ」移行を入れ、既存設定を消さないこと
  （`units` を旧バージョン互換用に残しているのと同じ考え方）。

## claude-ed への引き継ぎ（2026-09-06 時点）

### すぐ確認してほしいこと
- **復習編4（中国・四国地方、89問、id `kaki4_01`〜`kaki4_89`）は 9/5 の `1496a94` ですでにmasterに入っています。**
  取り込み予定のコミットに復習編4が含まれていると二重登録になるので、含まれていれば外してください。

### 復習編5/6/7 を取り込んだあとに必要な後処理
本日、復習編1〜4について「古い単元をdata.jsから削除し、端末に残っている正誤記録を新しい問題へ引きつぐ」処理を実装済み（`328af80`）。
復習編5/6/7 が入ると、以下が同じ扱いの対象になります。

| 消す古い単元 | 引きつぎ先 |
|---|---|
| `5.近畿地方`（57問） | 夏期講習 復習編5.近畿地方 |
| `6.中部地方`（78問） | 夏期講習 復習編6.中部地方 |
| `7.関東地方`（66問） | 夏期講習 復習編7.関東地方 |

手順（`tools/build_stats_migration.js` の冒頭コメントにも記載）:
1. `tools/build_stats_migration.js` の `UNIT_PAIRS` に組を足す
2. `node tools/build_stats_migration.js --report` で対応づけ具合を確認（1〜4は87%対応した）
3. `node tools/build_stats_migration.js` の出力で `index.html` の `KAKI_STATS_MIGRATION` を差し替え、`KAKI_MIGRATION_ID` を新しい値に変える（変えないと移行が再実行されない）
4. `KAKI_UNIT_RENAMES` にも新しい組を足す（単元の選択を新単元名に置きかえるため）
5. data.jsから古い単元を削除（**元の整形を保ったまま該当ブロックだけ**消すこと）

### 元データの場所（Google Drive にあり、クラウド側からも読めます）
`復習編5_近畿地方.json` / `復習編6_中部地方.json` / `復習編7_関東地方.json`、
漢字は `夏期講習_復習編4〜7_漢字.json`（漢字は姉妹アプリ `kanji-print-app` の担当）。

## 2026-09-06 にやったこと（claude-ed / ローカルPCセッション分）

### ⚠️ 先に読んでください：このリポジトリは2セッションが並行して触っています
本日、司令塔 `claude-1c` から「`kosukequiz-battle-app_cloud` が到達不能で担当が空席」との判断で、
ローカルPCのセッション `claude-ed` [7768c9] が本フォルダの担当に任命されました。
**しかし実際には cloud セッションが継続稼働しており、本日9件のコミットを push していました**
（下記「役割重複」の節）。Google Drive 上のファイルは 9/5 時点で止まっており、
**ローカルの作業ツリーとGitHubのmasterが乖離していた**のが原因です。

→ **このフォルダで作業を始める前に、必ず `git fetch` して origin/master との差を確認してください。**
　Google Drive のファイルが最新とは限りません。

### 夏期講習 復習編4・5・6 を data.js に取り込み（コミット `dc5d0e2`。**push は保留中**）
司令塔経由の依頼。データ元は `5年下\quiz_csv_夏期講習` の
`復習編4_中国四国地方.json`(89) / `復習編5_近畿地方.json`(85) / `復習編6_中部地方.json`(111)。

- **復習編4は「上書き更新」**。既存 `kaki4_01〜89` を置き換え（append すると id が二重化するため厳禁）。
  取り込み前に deployed 版と現行JSONで89件すべての `note`（出題位置）が一致することを自分でも再確認し、
  **並び順が1問も動いていない＝解答履歴が別問題に紐づく事故は起きない**ことを確認してから実施。
- **`kaki4_79` の解答不能を解消**（img: `kaki_r4_graph02.jpg` → `kaki_r4_shapes02.jpg` の分県図）。
  これが今回の最優先事項だった。あわせてレビュー指摘11件の修正と赤丸つき地図38枚の追加を反映。
- 復習編5・6 は新規追加（`kaki5_` `kaki6_` の既存idは無く衝突なし）。
- 画像128枚を `images/` にコピー（復習編4=39 / 5=38 / 6=51）。
- フィールド対応は既存の取り込みと同じ: `genre`→`u` / `subject`→`subj` / `file`→`img`、
  `figureNote` は取り込まない（従来どおり）。空文字のキーは出力しない。改行は **CRLF**（既存に合わせる）。

#### 検証したこと
- QA_DATA 1215件 → **1411件**（=1215-89+285）。id重複0、欠番なし
- JSON全285件の全項目（q/a/note/img/priority/level/sol/u/subj）と data.js の**不一致0件**
- 取り込み対象外の**既存1126件は差分0**（JSON文字列比較で完全一致＝非破壊）
- 画像参照273種すべて実ファイルあり（欠落0）。ローカルHTTPサーバで273種すべて **200** を確認
- ヘッドレスChromeで実際にアプリを描画し、単元バッジが
  **93 / 81 / 86 / 89 / 85 / 111問**（復習編1〜6）と正しく出ることを確認

#### 未処理（判断待ち）
- 旧画像4枚 `kaki_r4_graph02.jpg` / `kaki_r4_map01.jpg` / `kaki_r4_map02.jpg` / `kaki_r4_shapes01.jpg`
  は取り込み完了により**未使用**になったが、**削除していない**。消してよいか要確認。
  （なお `kai1_*` `r1_05` `r2_*` の38枚は姉妹アプリ分離時からの未使用ファイルで、今回とは無関係）

### ★役割重複の発見（司令塔に報告ずみ・自己判断していません）
初回の push が `behind` で拒否され、origin/master に本日付の Claude 名義コミット9件があると判明:
`fd828e2` 単元選択の科目別保存 / `328af80` 旧単元削除＋正誤記録引きつぎ(1438→1215件) /
`a26b0b7` 一覧の日付を「最終正解」に変更＋ひとり練習のやり直しラウンドを対戦と同じ扱いに /
`8072186` 一覧に「最後に正解した日」「最後に解いた日」を併記 / `a6fe925` CSV入出力 /
`06d9f28` 修正Issue→PR自動化 / `6d0f456` PR#5マージ(ユーザー本人) / `8699cb4` 復習ミックスの苦手判定統一

→ 取り込みは**origin/master の上でやり直し**、fast-forward 可能なローカルコミット `dc5d0e2` として保持。
　cloud セッションに稼働確認を送ったが、**cloud セッションは一方向で返信できない**仕様のため、
　push の可否はユーザー判断待ち。
→ **司令塔から依頼された「仕事2（一覧の最終回答の日付）」は、既に cloud セッションが実装済み**
　（`a26b0b7` / `8072186`）。重複実装しないこと。

---

## 2026-09-06 にやったこと

### 問題の修正報告Issueから、自動でプルリクを作るようにした
ユーザーから「問題の修正をIssueじゃなくて直接プルリクにできないか」との相談。

- **静的ページから直接PRを作るのは不可**。PR作成にはリポジトリへの書き込み権限＝トークンが必要で、GitHub Pagesで配信するJSに埋め込むと誰でも見られる。この点はユーザーに説明ずみ。
- 代わりに **GitHub Actions で「報告Issue → 自動でプルリク」** を実装（ユーザー了承ずみ）。**スマホ側の操作はこれまでどおり**（「保存してGitHubに報告する」を押すだけ）で、あとは自動でブランチが切られPRができる。**自動マージはしない。**
- `.github/workflows/apply-quiz-fix.yml` … `issues: [opened]` で起動。`workflow_dispatch`（Issue番号を入力）で流しなおしもできる。認証は標準の `GITHUB_TOKEN` のみで、個人トークンは不要。
- `.github/scripts/apply-quiz-fix.js` … Issue本文（`## 問題ID` `## 直した問題文` `## 直したこたえ`）を読んで data.js の該当ブロックの `"q"` / `"a"` の行だけを差し替える。**差分は2行だけ**になり、ファイル全体の整形には触れない（他セッションとの競合を避けるため重要）。書き出す前に必ず読み直して、問題数が変わっていないか・中身が想定どおりかを検証する。
- 安全対策：①自分（OWNER）が出した `[data.js修正]` で始まるIssueだけを対象にする、②Issue本文はシェルに展開せず環境変数経由でスクリプトに渡す（スクリプトインジェクション対策）、③問題IDは `[A-Za-z0-9_.-]+` のみ許可。
- 適用できなかったときは、理由をIssueにコメントして知らせる（自動では直さない）。

**前提条件**：リポジトリの Settings → Actions → General → Workflow permissions で「Allow GitHub Actions to create and approve pull requests」がONである必要がある。ユーザーに依頼ずみ。

#### 確認したこと（ローカル）
- 実際のIssue #3 の本文でスクリプトを流し、`"q"`/`"a"` の2行だけの差分になることを確認
- 異常系5件：存在しない問題ID／見出し不足／中身が今と同じ／変な問題ID（`../../etc/passwd`）／引用符・改行・バックスラッシュを含む修正 — いずれも正しく処理（前4件は適用せず理由を返す、最後は正しくエスケープして反映）
- ワークフローYAMLの構文と、各 `run` ブロックの `bash -n` 構文チェック

### 復習ミックスの「苦手」判定を、ホームの苦手判定と同じ基準に統一（過去の指摘の再発を修正）
ユーザーから「正解より間違いのほうが多い問題だと、何度も間違えた問題は何度も正解しないと消えない。過去にお願いしたのに改善されていないのでは」との指摘。**履歴を確認したところ、そのとおりだった。**

- `01d01e1`（8/30）で「累計だと解消しにくい」問題に対応し、**苦手判定を「直近2回連続正解で解消」（box基準）に変更**、復習ミックスも累計を使わない方式にしていた。
- ところが `d045fa4`（9/3）で復習ミックスをシンプルな2段階に作り直した際、②の判定に「**正解より失敗が多い**」＝累計比較が入り、**8/30に外したはずのルールが復活していた**。なお9/3の作り直し自体はユーザーとの相談で決めた仕様で、その文言をそのまま実装した結果こうなっている。
- 結果として、ホームの「よく間違える問題」（box基準）と復習ミックスの②（累計基準）で**判定基準が食いちがっていた**。

修正：`orderReviewCandidatesByWeakness()` の②を `wrong > correct` から `isWeak(id)` に変更。段階の構成（①未正解→②苦手→③それ以外、②③は最後に正解した日が古い順）はそのまま。

- 5誤3正の問題は、旧ルールでは累計6正5誤になるまで②から抜けられなかったが、**2回連続正解で③に移る**ようになった。
- 逆に「10回正解しているが直近で間違えた」問題は、旧ルールでは③に埋もれていたが②に上がってくる。

**この仕様は今後の作り直しでも壊さないこと。**「苦手」の判定はアプリ全体で `isWeak()`（＝一度でも間違えた かつ 直近の連続正解が0〜1回）に一本化する。累計の正解数・失敗数の比較で苦手を判定してはいけない（何度も間違えた問題ほど抜け出せなくなるため、2026-08-30・2026-09-06と2度同じ指摘を受けている）。

#### 確認したこと（ヘッドレスChrome）
- 4種類の状態（未正解／5誤3正で直近まちがえた／5誤3正で直近2連続正解／1誤10正で定着）を仕込んで実際に復習ミックスを出題させ、①→②→③の順に並ぶことを確認。累計が同じ5誤3正でも、直近2連続正解のほうが後ろに下がる
- 既存の回帰テスト7本（単元選択・ソロ/一覧・記録の引きつぎ・やり直しラウンド・過去データ補完・日付編集・CSV往復）でJSエラーなし

### CSVエクスポート／インポートの往復を直した
前項で指摘した「インポートが記録を壊す」件をユーザー依頼で対応。

- **エクスポート**：列に `最終回答日`（＝最後に解いた日 / `lastAnswered`）を追加。列は ID・単元・問題・こたえ・正解した回数・まちがえた回数・状態・最終正答日・最終回答日 の9列。
- **インポート**：これまで記録を `{correct, wrong, lastCorrectAt?}` で**置きかえて**いたため、取り込むたびに `lastAnswered`・`box`・`nextDue` が消え、間隔反復のスケジュールがリセットされ日付フィルタからも消えていた。`Object.assign({}, cur, …)` で**今ある記録に上書きマージ**する方式に変更。CSVに無い項目はそのまま残る。
- 日付は新旧どちらか**新しいほう**を採る（`最終正答日`・`最終回答日` の両方）。正誤の回数は従来どおり大きいほうを採る。
- 取り込みで初めてできた記録には `box` が無く、そのままだと `isWeak()`（wrong>0 かつ box<=1）で軒並み「苦手」になってしまうので、`状態`列から補う（苦手→0、正解ずみ→1、列が無ければ正誤の回数から判断）。
- `nextDue` は書き出していないので、まっさらな端末に取り込んだ直後はすべて「復習の時期が来ている」扱いになる。復元用途としては妥当と判断してそのままにした。

#### 確認したこと（ヘッドレスChrome）
- エクスポート → 記録を全消し → インポートで、2つの日付・正誤の回数・苦手/正解ずみが復元される
- 既存の記録があるところに取り込んでも `box`・`nextDue` が保たれる
- `最終回答日`列が無い古いCSV（旧アプリ由来）を取り込んでも、既存の `lastAnswered`・`box`・`nextDue` が壊れない

### 一覧の日付を2本立てにして、どちらも手で直せるようにした
上記の続き。司令塔経由で「最後に回答した日は不要では」という話が来たが、調べたうえでユーザーと直接確認し、**両方を出して両方編集できるようにする**方針に決まった。役割の整理は以下（ユーザーの言葉）:

- **最後に正解した日**（`lastCorrectAt`）… 苦手な問題の出題順序に使う
- **最後に解いた日**（`lastAnswered`）… 一覧の日付での絞りこみに使う

やったこと:
- 一覧の各問題に、上記2つを `<input type="date">` として並べ、それぞれ用途の説明を小さく添えた。変更するとその場で `stats` に反映（`setStatDate()` を新設）。空にするとその日付を消す。正誤の回数も日付も無くなったら記録ごと消える。
- **`lastAnswered` は削除しない**と確認済み。参照箇所は日付フィルタ（`renderListItems`）だけだが、この機能は「今日やった（けど間違えた）問題を探す」用途で `lastCorrectAt` では代用できない。
- 過去データの補完（一度だけ）：`correct > 0` なのに `lastCorrectAt` が無い記録に、`lastAnswered` を正解日として入れる（`backfillLastCorrectAt()`、フラグ `lastcorrect-backfill`）。`lastCorrectAt` 導入前の記録やCSVインポートした記録が「まだ正解なし」と出てしまうため。移行処理は複数を扱えるように `runStatsMigrations()` を整理した。

#### 見つけたが手をつけていないこと
- **CSVインポートは記録を `{correct, wrong, lastCorrectAt?}` で置きかえており、`lastAnswered`・`box`・`nextDue` を捨てている**（`index.html` のインポート処理）。つまりインポートすると間隔反復のスケジュールがリセットされ、日付フィルタからも消える。今回の依頼の範囲外なので指摘のみ。

### 「最終回答」の日付と、ソロのやり直しラウンドの正誤（司令塔経由の不具合報告）
ユーザー報告「一覧の最終回答が、やり直しで正解した問題にも今日の日付で入る。最終回答は最後に“正解”した日であるべきで、①スキップして正解、②まちがえた問題だけやり直して正解、は正解とみなしたくない」。

調べたところ**別々の2つの問題**だった。

1. **表示のズレ（仕様の取りちがえ）**：一覧は `s.lastAnswered`（＝最後に解答した日。正誤に関係なく更新）を「最終回答」として出していた。ラベルどおりの動きではあるが、ユーザーが期待する「最後に正解した日」＝`lastCorrectAt` とは別物。なお**CSVエクスポートは以前から`lastCorrectAt`を「最終正答日」として出しており**、2つの画面で食いちがっていた。
   → 一覧を「最終正解：日付（まだ正解なし）」に変更し、補足として「最後に解いた日」を小さく併記。日付フィルタは引きつづき`lastAnswered`（＝「今日やった問題」を探す用途なので）で、見出しに「解いた日」と明示した。
2. **ソロ側にやり直しラウンドのガードが無かった（バグ）**：2026-09-03に「スキップ・やり直しでの正解は誤り扱い」を実装したが、**二人対戦（`inMissRetryRound`／`hostSkippedIds`→`forcedWrong`）だけで、ひとり練習には入っていなかった**。そのため「まちがえた問題だけもう一度」「前回まちがえた問題だけやり直す」で正解すると、普通に正解+1・`lastCorrectAt`更新・box前進（＝苦手判定が解除）されていた。
   → `inSoloRetryRound` を新設し、対戦と同じ扱いに統一。ミスリストから外すかどうかは従来どおり実際の正誤で判定する。中断からの再開でも保持。やり直し中は画面に「ここで正解しても、記録は苦手のまま残ります」と表示（対戦側の文言に合わせた）。
   - `quizResults` に `recorded`（実際に記録した正誤）を持たせ、「ひとつ前の判定をやり直す」（undo）が実際の正誤ではなく記録した側を戻すようにした。
   - あわせて、一覧で手動で「〇 正解」にしたときや ± ボタンで正解を増やしたときにも `lastCorrectAt` が入るようにした（表示を`lastCorrectAt`に変えたことで、これが無いと「まだ正解なし」のままになるため）。

※ソロには「スキップ」機能自体が無い（〇/✕のみ）ので、①のスキップは対戦側のみ。そちらは以前から対応ずみ。

#### 確認したこと（ヘッドレスChrome）
- 通常ラウンドで✕→やり直しで〇 → correct=0 / wrong=2 / 最終正解日なし / 苦手のまま
- ホームの「前回まちがえた問題だけやり直す」でも同じ
- undo が通常ラウンド・やり直しラウンドの両方で正しく戻る
- 手動で「〇 正解」→ `lastCorrectAt` が入り、一覧に「最終正解：日付」が出る

### 夏期講習の復習編：古い単元を削除し、正誤記録を新しいほうに引きつぎ
ユーザー依頼「復習編の新しいほうができたら、できる範囲で正誤を古いほうから新しいほうに反映して古いほうを消して」。方針は追って「そんなに精密にやらなくてよい、だいたいで」と確認済み。

- 対象は復習編ができている4組。旧単元をdata.jsから削除（1438問→1215問、-223問）。
  - `1.日本の食料生産`→`夏期講習 復習編1.日本の食料生産`
  - `2.工業・資源・輸送機関`→`夏期講習 復習編2.…`
  - `3.九州地方`→`夏期講習 復習編3.…`
  - `4.中国・四国地方`→`夏期講習 復習編4.…`
- **新旧は1対1で対応しない**（旧＝本文の穴埋め、新＝地図・記号選択など別形式）。そこで「答えの語」が同じか一方が他方を含む（中京⊂中京工業地帯、釧路⊂釧路港、シラス⊂シラス台地）ものを同じ知識項目とみなして対応づけた。**223問中195問（87%）が対応**。1問が複数問に対応することもある（平均1.7件）。
- 対応表をつくるスクリプトを `tools/build_stats_migration.js` として追加。**復習編5以降ができたら、このファイルの`UNIT_PAIRS`に足して再実行 → index.htmlの`KAKI_STATS_MIGRATION`と`KAKI_MIGRATION_ID`を更新 → data.jsから旧単元を削除、という手順で繰り返せる**（`--report`で対応づけ結果を確認できる）。
- 正誤記録は端末のlocalStorageにあるので、アプリ側に一度だけ走る移行処理 `runStatsMigrations()` を追加。旧id→新idに移し、記録が両方にある場合は正解/不正解を合算、間隔反復の箱は小さいほう（＝復習が早く回るほう）に寄せる。実行済みフラグは `kq_battle_migrations_v1`。
- **移行前の記録は `kq_battle_stats_backup_kaki_v1` に丸ごと退避**してあるので、対応がつかなかった28問ぶんも消えてはいない。
- 単元の選択・復習ミックスの単元も、旧単元名→新単元名に自動で置きかえる（絞り込みが空にならないように）。
- 中断中のソロセッションは `quizQueue` が QA_DATA の**添字**なので、問題を消すと中身がずれる。移行時に破棄するようにした（対戦セッションは問題idで持っているので、消えた問題を含むときだけ破棄）。
- data.jsは**削除のみの差分**（1611行削除・追加0行）になるよう、元の整形を保ったまま行単位で消した（他セッションとの競合を避けるため）。

#### 確認したこと（ヘッドレスChrome）
- 旧記録を仕込んで起動 → 引きつぎ・合算・旧idの消滅・単元名の置きかえ・ソロセッション破棄・退避データの生成をすべて確認
- 2回目の起動で二重に加算されないこと（べき等）
- 単元選択まわりの既存テスト、ソロ出題・問題一覧の通しでJSエラーなし

### 「前回の設定（単元）が引き継がれない」バグの修正
ユーザー報告を受けて調査。ヘッドレスChromeで再現させたところ、原因は単元選択が**科目（社会/理科）ごとに保存されていなかった**ことだった。

- **主因**：`switchSubject()` が科目を切りかえるたびに `selectedUnits = new Set(getUnits())`（＝その科目の全単元）でリセットしていた。理科をやってから社会にもどすと社会の絞り込みが消え、そのまま `saveSettings()` で保存されるので、次回起動時にも戻らない。
  → 単元選択を科目ごとに覚える `unitSelectionBySubject`（`rememberUnitSelection()` / `restoreUnitSelection()`）を新設し、切りかえ時・起動時ともに前回その科目で選んでいた単元を復元するように変更。localStorageにも `unitsBySubject` として科目ごとに保存する。
- **あわせて修正1**：`loadSettings()` の `if(Array.isArray(s.units) && s.units.length)` により、**全単元を外した状態（＝復習ミックスだけで出題する使い方）が復元されず**、全単元選択にもどっていた。「まだ一度も選んでいない科目」と「わざと全部外した科目」を区別するようにして、空の選択もそのまま復元する。
- **あわせて修正2**：`syncReviewUnits()` が、今の科目に無い単元（＝もう一方の科目の単元）を `reviewSelectedUnits` から消していた。`reviewUnitsKnown` には残るため自動ONにもならず、科目を切りかえて戻ると**復習ミックスの単元選択が失われて選び直しになっていた**。整理の対象を今の科目の単元だけに限定し、データから消えた単元だけを取りのぞくように変更。
- 使われなくなっていた `socialSelectedUnitsSnapshot`（漢字プリント画面を姉妹アプリに分離した時点で読み取り側が無くなっていた）を削除。保存データの `socialUnits` は、旧バージョンの設定からの移行用に読むだけにした。

### 確認したこと（ヘッドレスChrome）
- 社会で絞る→理科へ→理科でも絞る→社会にもどす→また理科、で両方の選択が保持される
- リロード後も両科目の選択が残る／全単元を外した状態も残る
- 復習ミックスの単元選択が科目またぎで保持される
- 旧形式の保存データからの移行4ケース（保存時の科目=社会 / =理科 / 保存なし / 壊れたJSON）
- ソロ出題・問題一覧・復習ミックス（114問＝メイン112＋復習2）に影響なし、JSエラーゼロ

※この環境からは `https://esm.run/trystero`（WebRTC）に接続できないため、対戦部分はスタブに差し替えて検証した。対戦のコード自体は今回さわっていない。

## 次にやること
- 実機で、社会↔理科を行き来しても単元の絞り込みが残ることを確認してほしい
- 実機で、復習編1〜4の「正解ずみ/苦手」の件数が引きつがれていることを確認してほしい
- 実機で、やり直しで正解しても苦手のまま残ること・一覧の2つの日付（表示と編集）を確認してほしい

- 復習編5以降（近畿・中部・関東・東北・北海道）ができたら、上記の手順で同じ作業を繰り返す
- 修正報告→自動PRのワークフローは、実際にIssueを1件出して動くところまでは未確認（リポジトリ設定のON待ち）。次に修正報告を出したときに動作を見てほしい
- `images/` に未参照のファイルが38枚ある（大半が `r2_*.jpg`＝理科第2回の計算問題用で、`keisan-print-app` へ分離した際の残り）。掃除するかは要判断

## 2026-09-03 にやったこと（続きのその4：アプリ分割）

ユーザーから「理科（計算問題）と社会（漢字プリント）だけ別アプリに分けたい」との依頼。早押し対戦本体（社会・理科共通）は分割せず維持。

### Part A：社会「漢字プリント」→ 既存の`kanji-print-app`リポジトリを刷新
- `kanji-print-app`（`G:\マイドライブ\四谷大塚\kanji-print-app`）は、8/31時点の古い実装のまま止まっていたが、**私の知らないところで別セッションが並行して更新を進めていた**（印刷余白の調整、第1回・第2回への実データ同期など、コミット履歴あり）。着手前にコミットされていない作業中の変更（印刷CSSの途中編集）を発見し、`git stash`で退避（破棄はしていない）。
- kanji-data.jsをquiz_csv作成の正式な83問データ（第1回_漢字.json・第2回_漢字.json）に総入れ替え。id体系をkosukequiz-battle側`KANJI_DATA`と同じ`k{回}_{連番}`に統一。
- 進捗記録をkosukequiz-battleと同じlocalStorageキー（`kq_kanji_stats_v1`）・同じデータ形式に変更。GitHub Pagesの同一オリジン（`nambanamba.github.io`）のため、localStorageが自動的に共有され、既存の進捗がそのまま引き継がれる（エクスポート/インポート等の手動移行は不要）。
- 〇✕はタップした瞬間に確定・保存、苦手優先の並び替え、印刷レイアウトの拡大など、kosukequiz-battle側で確定した仕様をすべて反映。README.mdも刷新。
- ヘッドレスブラウザで動作確認、コミット・push済み。

### Part B：理科「計算問題」→ 新規リポジトリ`keisan-print-app`
- `gh repo create`で新規作成、GitHub Pages有効化。
- kosukequiz-battleのdata.js内`kind:"calc"`の項目（77問）と参照画像（39枚）を移植した最小限のスタンドアロンアプリ（stats/localStorageは使わない無状態機能）。
- ヘッドレスブラウザで動作確認、初回コミット・push済み。

### Part C：kosukequiz-battle本体の縮小
- `screen-kanji`・`screen-calc-setup`・`screen-calc`一式（HTML/CSS/JS）と、data.js内の`KANJI_DATA`・`kind:"calc"`の項目（77問）を削除。
- ホーム画面の「今日の漢字プリント」「今日の計算問題」ボタンを、それぞれ姉妹アプリへの外部リンク（新しいタブで開く）に置き換え。
- ヘッドレスブラウザで、対戦・ソロ・問題一覧・復習ミックスなど残る機能に影響がないことを確認（QA_DATA件数1147→1070に減少、想定通り）。README.md更新、コミット・push済み。

### 他セッションとの連携
- kanji-73（新規セッション、国語「漢字と言葉」の新アプリ検討中）から相談があり、「対戦機能はkosukequiz-battleのみ、印刷・練習系は姉妹アプリに分ける」方針を共有・推奨した。
- quiz-csv-9a（社会・通常回の作成担当）から、第3回_奈良時代.json（112問）・第3回_漢字.jsonの取り込み依頼が届いている（次にやること参照）。

## 次にやること
- quiz-csv-9aから届いている第3回_奈良時代.json（対戦クイズ本体）・第3回_漢字.json（kanji-print-app側）を取り込む
- kanji-print-appでstashした「作業中だった印刷CSS変更」は、今回の刷新で内容ごと上書きしたため、基本的にもう不要のはず。ただし念のため残してある（`git stash list`で確認可能）

## 2026-09-03 にやったこと（続きのその3）

### 対戦の正誤カウント修正：「まちがえた問題だけもう一勝負」の扱い
- ユーザー報告「正解不正解のカウントがおかしい。1回目正解で正解+1、スキップ・誤り・最後の誤りのやり直しのときは誤り+1にしたい」に対応。場所は二人対戦、「最後の誤りのやり直し」は「まちがえた問題だけもう一勝負」ラウンドのことと確認。
- 現状：スキップした問題を後で答え直したときはホスト側のみ強制的に「誤り」記録される仕組みが既にあったが、「まちがえた問題だけもう一勝負」ラウンドでは、そこで正解すると普通に「正解+1」されてしまっていた（元々間違えた問題なのに正解扱いになる）。
- 修正：`inMissRetryRound`（もう一勝負ラウンド中かどうか、host/guest共通）を新設し、このラウンド中の判定は常に「誤り」として永続的な正誤記録に残すように変更（今回だけ「まちがえた問題」リストから外れるかどうかの判定は、これまで通り実際の正誤で行う）。ホストからゲストへの`startAction`送信に`isRetryRound`フラグを追加してゲスト側にも伝播、対戦セッションの再開（`resume-battle-btn`）時も保持されるようにした。
- 実際の2人対戦（WebRTC）はヘッドレス環境で直接テストできないため、コードレビューと基本的な動作確認（ソロ・漢字プリントに影響がないこと）で確認。次に実機で対戦する際に、まちがえた問題のやり直しで正解しても苦手記録が残ることを確認してほしい。

## 2026-09-03 にやったこと（続きのその2）

### 復習ミックスの選び方をシンプルな仕様に作り直し
- ユーザーから「複雑にお願いしすぎたかも」とのフィードバックとともに、復習ミックスのやりたいことを明確化：①メイン単元選択（既存のまま）、②復習単元選択＋各種フィルタ＋「苦手な問題から選ぶ」＝正解より失敗が多い問題を正解が一番昔の問題から優先→残り（失敗経験ありだが失敗より正解の方が多い問題）も同様に正解が一番昔の問題から、という2段階の選び方。
- 旧来の間隔反復（box/nextDue）ベースの選定ロジックを廃止し、上記の2段階＋「一度も失敗していない問題」を最後に追加する3段階選定に置き換え（`orderReviewCandidatesByWeakness()`関数を新設）。「正解したのが一番昔」の判定用に`stats`へ`lastCorrectAt`（直近正解時刻ではなく、正解時のみ更新される専用フィールド）を追加。
- 復習単元のフィルタを、メイン単元選択と同じ「出題タイプ・優先度・難易度」の3種類に拡張（従来は優先度のみだった）。復習専用の独立した状態（reviewType/reviewPriority/reviewLevel）として持たせ、メイン側のフィルタとは干渉しない。
- ヘッドレスブラウザで、実際にstatsを仕込んで一人プレイを進め、①失敗過多の問題→②失敗経験ありの問題→③未失敗の問題、の順で正しく出題されることを確認（出題順どおりモードで検証）。フィルタのトグル→ラベル反映→リロード後の保持も確認ずみ。

### 復習ミックスの「復習する単元」バグ修正（このあと上記の作り直しで選定ロジック自体は差し替え済み）
- ユーザー報告「①どの単元を選んでいるのかわかりにくい／②選んだ単元から出ていない」に対応。原因は2つ：(1) チップの選択状態表示に存在しないCSSクラス`on`を使っていた（正しくは`selected`）、(2) メイン単元を1つずつ外していく操作で、2つ目以降に新しく復習候補になった単元が自動でonにならないロジックバグ。修正しヘッドレスブラウザで確認・コミット・push済み（0ab9ade）。

### 漢字データを対戦クイズから完全独立させる大改修
- quiz_csv（社会通常回）セッションが、ユーザーからの直接指示「漢字問題と通常問題は分けたい。漢字は完全に独立して進捗・正誤を管理したい」を受けて、対戦クイズ本体（`第1回_旧石器縄文弥生.json`等）から`kanjiWord`フィールドを削除し、代わりに完全に独立した`第1回_漢字.json`（40問）／`第2回_漢字.json`（43問、いずれもクイズ本体とは別id・読み仮名＋ヒント形式）を新規作成していたことが判明（当セッションへの返信待ちだった）。
- ユーザーに方針を確認：「独立（データは別ファイル）、ただし正誤管理の“方法”はクイズ本体と同じ、単元は共通の文字列で連動」という結論。
- 対応：
  1. `第1回_漢字.json`／`第2回_漢字.json`（計83問）を、data.js内に**QA_DATAとは別の新配列`KANJI_DATA`**として追加。id接頭辞は`k{回}_{連番2桁}`（例：k1_01）。`u`（単元名）は対戦クイズ側と完全に同じ文字列（例：`"第1回.旧石器時代・縄文時代・弥生時代"`）になっており、単元の自動連動はそのまま機能する。
  2. index.html側：`getKanjiCandidates()`を、旧来の`kanjiWord`/`kanjiSkip`自動抽出ロジックから、`KANJI_DATA`をそのまま使う形に全面書き換え。
  3. 漢字専用の正誤記録（`kanjiStats`、localStorageキー`kq_kanji_stats_v1`）を新設し、クイズ本体の`stats`とは別ストレージで管理するように変更（昨日実装した「一本化」を撤回）。間隔反復のスケジューリングロジック（`scheduleNext()`）はクイズ本体と共通の関数をそのまま再利用し、「管理方法は同じ」を担保。
  4. ヘッドレスブラウザで、KANJI_DATA件数（83件）・QA_DATA非破壊・漢字保存後もクイズ側stats（ホームの苦手/正解ずみ件数）が変化しないこと・印刷レイアウトを確認ずみ。
- README.mdにKANJI_DATAのスキーマを追記。
- **次にやること**：quiz_csv（社会通常回）セッションに、①今回の取り込み完了、②今後の回も同じ形式（`第{回}回_漢字.json`）で対戦クイズと合わせて作成してほしい旨、③旧ドリルPDF（`第1回・第2回_社会漢字_5回練習ドリル`等）はアプリ本体の印刷機能に統合されたため再生成不要、を連絡する（担当セッションの特定ができておらず、次回セッション起動時に対応）。

## 2026-09-03 にやったこと
- 漢字プリントの印刷を、姉妹アプリ`kanji-print-app`（G:\マイドライブ\四谷大塚\kanji-print-app）のレイアウトを参考に「①テストの穴埋め欄→②お手本＋なぞり書き＋4回練習マス」の1枚構成に変更。従来は「問題用紙（穴埋めのみ）」「解答用紙（答えのみ）」を別々に印刷する方式だったが、1枚に両方まとめ、印刷ボタンも4個（今日の分・まとめ印刷それぞれの問題用紙／解答用紙）から2個（今日の分・まとめ印刷それぞれ1個）に整理
- ヘッドレスChromeで印刷プレビュー（`@media print`エミュレート）のスクリーンショットを確認し、レイアウト・件数（テスト欄・練習マス目とも一致）を確認ずみ

## 2026-09-02 にやったこと

### アプリ本体（index.html）の機能追加
- 漢字プリントの対象単元を、ホーム画面（クイズ）の社会の単元選択と常に自動連動するように変更。漢字画面独自の単元選択UIは廃止し、「ホーム画面で単元を変更」リンクに置き換え
- 漢字プリントの正誤記録を、クイズ本体の正誤記録（苦手判定・間隔反復）と一本化。漢字プリントで記録した結果がクイズ側の「苦手な問題」にもそのまま反映されるようになった（従来はkanjiState.statsという別管理だった）。あわせてホームと同じ「正解ずみを除く」「苦手な問題だけ」フィルターを漢字画面にも追加
- 復習ミックスに「復習する単元」の個別選択（メインで選んでいない単元の中からさらに絞り込み）と、優先度（高/中/低）フィルターを追加
- 対戦に「こどもどうし公平モード」を新設（既存の親子戦フローはそのまま）。ONにすると、ホストによる問題の先読み・スキップ（考える時間のアドバンテージ）を行わず問題を両者に同時配信。ゲスト参加後、ゲスト側でも出したい単元を選べ、ホストの単元選択と合算して出題される
- ヘッドレスChromeでの自動操作確認（漢字プリント作成→記録→ホーム統計反映、単元/復習ミックス操作、公平モードトグル、ソロ開始）でJSエラーなしを確認ずみ

### 見つかった問題（未対応・要フォローアップ）
- 漢字プリントで実際に抽出される405問（すべてkanjiWord指定＝厳選ずみ）を全件確認したところ、約4割（162問）で「こたえ」が空欄の正解ではなく、問題文中の別の単語になっている疑いがある（特に公民分野で顕著。例：「国会議員は満何歳以上の選挙で選ばれますか」→こたえ「国会」など）。data.jsのkanjiWordはクイズ作成担当セッションの管轄のため、本セッションでは修正せず指摘のみ。ユーザー確認済み、対応は「クイズは別途作ってもらう」ため保留中

### データ取り込み
- quiz-csv-5a（理科・第2回レビュー担当）からの依頼を受け、理科第2回「てこ・滑車・輪軸」（87問：暗記r2m01〜17、計算r2c01〜70、画像r2_01〜40.jpg）をdata.jsに取り込み、コミット・push・デプロイ済み

## 次にやること
- 漢字プリントのkanjiWord誤り（上記162問）を、クイズ作成担当セッションと共有・修正する必要がある（現時点ではリスト化のみ保留、着手時はkanji_full_list_suspicious.jsonの生成ロジック（このセッションの一時ファイル、再生成可）を参考にできる）

## 2026-08-30〜31 にやったこと

### データ取り込み
- 夏期講習・復習編1（日本の食料生産v2）: 地図問題15問に丸数字を追加、レビュー指摘6件（八郎潟・利根川・境港・おうとう・港の記号問題3問）を反映
- 理科第1回「生物のつながり」を80問版（本文由来の設問を除去したもの）に差し替え、その後の細かい修正（no.53画像追加、no.66答えの先出し修正、no.68答え不足の修正）も反映
- 理科・夏期講習の両方に優先度（priority）・難易度（level）フィールドを追加（取り込み時に一度落としてしまっていたのを追加し直した）

### アプリ本体（index.html）の機能追加・修正
- 問題一覧画面（screen-list）にイラスト画像・出典（ページ数/問題番号）を表示するようにした
- 一人で解くとき（screen-solo）に「前の問題にもどる」機能を追加（通常モード・チェックのみモード両方対応）
- 計算問題の印刷で、画像の読み込みを待ってから印刷するように修正（印刷が空白になる不具合の対策）
- ホーム画面に優先度・難易度フィルターを追加
- 復習ミックスを間隔反復（スペースドリピティション）方式に変更：①未正解の問題→②間隔が来た問題→③まだ間隔が来ていない問題、の順で優先。苦手判定も「直近2回連続正解」で解消するように変更（累計正解数ベースだと解消しにくい問題があったため）
- 社会の「今日の漢字プリント」機能を新規追加。既存の問題データ（答えの括弧内など）から漢字語を自動抽出し、毎日数問印刷→手書き→〇✕入力→まちがえた分を翌日優先、というサイクルを実装
- 漢字プリントの単元フィルター＆印刷機能強化（2026-08-31）:
  - 中学受験社会の頻出漢字・記述指定重要語（歴史・地理・公民）を精選し、data.jsの全問題にkanjiWord（439問）/kanjiSkip（541問）を設定して平易な語（米・肉牛など）の混入を完全排除
  - 漢字画面（screen-kanji）内に「単元フィルター」（全単元/個別単元の選択・問題数バッジ）を追加し、単元ごとの絞り込みに対応
  - 「選んだ単元の漢字をすべて印刷（まとめテスト用）」ボタンを追加（問題用紙・解答用紙）
  - A4印刷時に手書きしやすい解答記入枠（四角ボックス）とタイトル・日付・氏名・得点欄ヘッダーを配置
- 二人対戦（ゲスト側）: ホストが「わかった／スキップ」を考えている間は問題文を表示せず「つぎの問題の準備中…」に変更し、前の問題のカードも非表示に（スキップした瞬間にはスキップされた問題を表示）
- 二人対戦の結果画面: 「まちがえた問題だけもう一勝負」を3秒カウントダウンで自動開始するように変更（ゲスト側にも同じカウントダウンを表示）

### 環境まわり
- このセッションの環境にgit・GitHub CLI（gh）が入っていなかったので、winget経由でインストールし、gh認証（デバイスコード方式）を完了させた。以降のpushはこのセッションから可能

### 複数セッションの役割整理
- quiz_csv（社会・通常回）フォルダにレビュー担当の役割をROLE.mdに追加（従来レビュー担当が不在だった）
- レビュー担当の運用を「指摘のみ、修正は作成担当が行い再確認する」方式に統一（理科・夏期講習の両ROLE.mdとクイズデータ作成ルール.mdに反映。レビュー担当自身の修正案に誤りがあった実例があったため）
- 社会（通常回）の自動チェックで、107問中20問が本文由来（2026-08-30ルールでは対象外）の可能性が高いことを発見、quiz_csv\ROLE.mdに記載済み

## 次にやること
- images/フォルダに未取り込みの画像が増えている（kai1_28.jpg, kai1_29.jpg, r2_01〜08.jpg）。理科第2回または社会の新しいデータの可能性が高いが、対応する取り込み依頼はまだ来ていないので、担当セッションから連絡が来たら対応する
- quiz_csv（社会通常回）のレビュー担当が本文由来20問の要否を判断する予定（まだ未着手のはず）
