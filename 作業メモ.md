# 作業メモ

## いま作業中
（作業を始めるときに「セッション名 / 触るファイル / 内容」を1行書いてpush、終わったら消す。
　作業前に必ずここを読み、他のセッションが同じファイルを触っていたら手を出さないこと）

- （いまは誰も作業していません）

### ★★2026-09-23 理科の id を元データから取るようにした（`tools/import_rika.py`・★data.js は1バイトも触っていない）
依頼: `司令塔\回答\対戦アプリ_理科のidを元データから取る_依頼_2026-09-23.md`（司令塔 [577495]）

#### 何が壊れていたか
`rec["id"] = "r%dm%02d" % (kai, i)` … **memo の並び順に連番**を振っていた。
→ **真ん中の1問を消すと、それより後ろの id が全部くり上がる。**`stats[id]` が別の問題に付けかわる。
★これは第4回だけの話ではない。**第3回は data.js の id が r3m102 まで飛び番（欠番13）**なのに元データの memo は89問。
そのまま `--apply 3` すると **89問すべてが r3m01〜r3m89 に振り直されていた**。

#### 直したこと
- **元データの `id` をそのまま使う**（社会の `import_quiz.py` と同じ形にそろえた）
- **id が無い行があったら `die` で止まる。**黙って採番しない
- ついでに止まるようにしたもの: **id の重複** / **別の回の id**（`r3m01` が第2回にまじる等）/ **知らないキー**
- `--allow-position-ids`（社会にある逃げ道）は残したが、
  ★**data.js にその回が1問でもあると、指定しても止まる**。＝記録の付けかえが起きる場面では絶対に使えない
- `--check` に **「そのまま残る id / 消える id / 増える id / 同じ id で問題文が変わる」** を追加
- `--verify-ids` を追加（全回、元データの id と data.js の id を突き合わせる。**書きかえない**）

#### ★入口の自己テスト（確認ポイント 4-6。`--check`/`--apply` のたびに先に走り、落ちたら数字を出さず終了コード3）
偽の元データは**実物のJSONから組み立てる**（回も番号も決め打ちしない・4-6b）。選ぶ条件は
「memo 4問以上・calc が途中にはさまる回」＝ no と並び順がずれる回（4-6e）。偽の id は**飛び番**にしてある。
```
(a) id を持つ元データ         → 元データと1件ずつ一致        ✔ 12問すべて一致（問題文も対応）
(b) ★真ん中の1行を消した元データ → 残った id が1つも変わらない  ✔ 11問すべて元のまま
(b2) その (b) が昔の式なら鳴るか → 鳴るべき                   ✔ 10/11問ずれる（＝空っぽの検査ではない）
(c) id の無い行がまじる        → 止まるべき                   ✔ 止まった
(c2) id が1つも無い            → 止まるべき                   ✔ 止まった
(d) id が重複                  → 止まるべき                   ✔ 止まった
(e) 別の回の id がまじる        → 止まるべき                   ✔ 止まった
```
★(b2) を入れた理由: **(b) だけだと「何も守っていない検査」でも通ってしまう**（4-6c の「対照が鳴るか」を、
git の HEAD ではなく**直す前の式そのものを書いて**固定した）。

#### ★実データでの確かめ（読むだけ。ファイルは書いていない）
第4回の消す候補13問（`5年下\quiz_csv_理科\第4回_表に吸収された問題_消す候補_20260923.md`）を
**メモリ上だけで**抜いて変換した:
```
★直したあと    中身が別の問題に移った   0 / 76問
⚠️ 直す前の式なら                      72 / 76問
   （例: r4m05 ← r4m09「小腸の…小さなとっ起」／ r4m06 ← r4m11「消化管が長いのは」）
```

#### `--verify-ids` の結果（2026-09-23 時点）
```
第1回  一致 73件 / 食い違い 0件
第2回  ★元データに id がありません（12/12問）。まだ焼き付いていません
第3回  一致 89件 / 食い違い 0件
第4回  一致 89件 / 食い違い 0件
合計   一致 251件 / 食い違い 12件（＝第2回ぶん）
```
★**第1回・第3回・第4回は、元データの id と data.js の id が1件ずつぴったり一致**（問題文まで照合）。
⚠️ **第2回は別の問題を抱えている: 元データの memo が12問しかないのに data.js には r2m01〜r2m17 の17問。**
　 いま `--apply 2` を打つと **5問が消えます。**（この依頼の範囲外。司令塔に報告ずみ）

#### ⚠️ 残っている同じ型の危険（この依頼では触っていない）
`tools/patch_r3_rev4.py`・`tools/patch_r4_figures.py`・`tools/import_rika3_yokogushi.py` は、
いまも **`no` や並び順から id を組み立てています**（`"r4m%02d" % q["no"]` など）。
どれも一度きりの道具で、いまは走らせていません。**再実行すると同じ事故が起きます。**

#### ★★2026-09-23 夕方　司令塔の承認と追加3点（すべて完了）
**1. `--allow-position-ids` を削除**した。理由は司令塔の言い方で
「**押せそうに見えて押せないボタンは、次の人が押そうとして時間を溶かします**」。
docstring と `--check` の説明に「**id は元データに必ず持たせる**」を残してある。

⚠️ **そのとき `import_g.py`（社会の現役の取り込み）を壊しかけた。**
`import_g.py` は `ir.die()` を借りている。その `die` を `sys.exit` から例外 `Stop` に変えたので、
止まるときに traceback が出る形になっていた。`import_g.py` の入口で `ir.Stop` を受けるように直し、
`python tools/import_g.py --check 99` → `✖ 第99回のファイルを知りません。`（終了コード 1）で、昇りと同じ見え方に戻した。
★**道具を直すときは、その道具を import している別の道具を探すこと**（`grep -rn "import_rika"`）。

**2. 一度きりの道具を止めた**（消していない。記録として残す）。
`patch_r3_rev4.py` / `patch_r4_figures.py` / `import_rika3_yokogushi.py` の**先頭（import より前）**で `sys.exit`。
```
✖ 一度きりの道具です。2026-09-23 以降、id は元データが持ちます。
  この道具は no から id を組み立てる（69行目 `"r4m%02d" % q["no"]`）ため、走らせると記録が別の問題に移ります。
```
3本とも実際に `--apply` を打って、終了コード 1 で止まることを確かめた。

**3. 同じ書き方の道具を全走査した。id を番号から組み立てているのは 5カ所／4ファイル。**
| 道具 | どう作っているか | 判断 |
|---|---|---|
| `patch_r3_rev4.py` | `"r%dm%02d" % (KAI, r["no"] - calc_before)` | ★止めた |
| `patch_r4_figures.py` | `"r4m%02d" % q["no"]`（2カ所） | ★止めた |
| `import_rika3_yokogushi.py` | `"r3m%02d" % (FIRST_NEW + n)`（並び順） | ★止めた |
| `import_g.py:79` | `"g%dr%d" % (kai, int(r["no"]))` | 止めていない。**社会の現役の取り込み**。社会は `no` そのものが id（引き算が無い）なので、真ん中を消しても他は動かない。★ただし**`no` を振り直した瞬間に同じ事故**になる |
| `patch_g4_sol_note.py` | `"g4r%d" % r["no"]`（2カ所） | 止めていない。社会の規則と同じ。一度きり |

参考（危険ではないと判断したもの）
- `patch_r3_notes.py` … id は **`q`（問題文）で引いている**ので安全。ただし中の検算「id番号 == no − 前の calc 数」はもう成り立たず、走らせれば `sys.exit` で止まる（安全側）
- `patch_r4_table_hints.py` … `IDS = ["r4m%02d" % n for n in range(80, 90)]` は **`no` からではなく、実在する id の直書き**。sha256 で元データを固定している
★**どちらも止めるかどうかは司令塔に判断を仙いだ（报告済み）。**

**4. 自己テスト (c2) が空振りになるのを直した。**
(c2)「id が1つも無い元データで止まる」を**実物の JSON で**見ていたので、
★**理科担当が第2回に id を焼き付けた瞬間、この枝が一度も通らなくなった**（実際にそうなった）。
→ **id をこちらで全部はがした偽の元データ**で確かめる形に変えた。4-6d（検査が黙って無力化する）の同じ型。

#### ★理科からの「元データの書式が3種類」について
**`import_rika.py` は元データに書き戻しません**（書き込む先は `DATA_JS` と `DATA_JS + ".bak"` だけ。
`grep -n 'open(.*"w\|copyfile\|\.write(' tools/import_rika.py` で 3行、すべて data.js）。
→ この罠は踏みません。★ただし**将来元データに書く道具を作るなら、改行と末尾の改行を実測して当てること**。

#### 第2回の id 焼き付け後の `--verify-ids`（2026-09-23）
```
第1回 一致 73 / 食い違い 0     第3回 一致 89 / 0     第4回 一致 89 / 0
第2回 一致 12 / 食い違い 5（全部「data.js にしか無い」← calc に回した5問。問題文が違う 0）
合計   一致 263 / 食い違い 5
```
`--check 2` の出力: **消える id 5問 = r2m12 / r2m13 / r2m15 / r2m16 / r2m17**。
そのまま残る 12問、増える 0問、同じ id で問題文が変わる 0問。

#### ★★2026-09-23 第4回12問削除の確かめ（取り込みはまだ）
```
--check 4  消える id 12問 = r4m05 r4m06 r4m07 r4m08 r4m19 r4m20 r4m24 r4m25 r4m26 r4m45 r4m50 r4m53
           ★司令塔が挙げた12問と**集合として一致**（多い0・足りない0）。`r4m10` は残置を確認
           そのまま残る 77問 ／ 増える 0問 ／ 同じ id で問題文が変わる 0問
```
⚠️ **第3回の元データが動いています。**memo 89 → **109問**。
**r3m103〜r3m122 の20問（【水溶液の表①〜】の表問）が元データにだけあります**。
★**番号は r3m102 の続きで、既存の id は1つも動いていません**（一致 89 のまま・問題文の食い違い 0）。
第3回は司令塔から「まだ」と言われているので**着手しません**。

#### ★「未クリア ◯問」に残りカスが効かないこと（司令塔の申し送りへの答え）
司令塔の当てで合っています。**数える側はすべて `QA_DATA` を見ています。**
- `refreshUnmasteredCount` … `buildPool(true).filter(i => !isMastered(QA_DATA[i].id))`
- `buildPool` … `QA_DATA.forEach(...)`　← 消した問はプールに入らない
- `refreshStatRow`（全問数・正解ずみ・苦手）… 同じく `QA_DATA.forEach`
- `restoreSoloSession`（中断の再開）… `if(!byId.has(id)) return;` で**消えた問を取りのぞく**

`stats` の鍵を直接回すのは **2カ所だけ**で、どちらも数に効きません:
- `backfillLastCorrectAt` … `lastCorrectAt` を補うだけ
- `cleanupAfterMigrations` … `QA_DATA` に無い id の記録を消す。★**ただし変換が1つでも未実施の端末でしか走らない**
  → 全部済んだ端末では、消した12問の `stats` が localStorage に**残りつづけます**（`r4m90` と同じ）。
  ★**数には出ません。容量だけの話です。**
★これは**コードを読んだ結果**です。**取り込み後に実機（または mikaku）でも一度見ます。**

#### ★司令塔の判断（受けたもの）
- `patch_r3_notes.py` ・ `patch_r4_table_hints.py` … **止め木は不要**。先頭に理由を1行入れた（入れずみ）
- `import_g.py`（社会466問）… 同じ手当をするが、★**順番は後。理科の取り込みと公開が先**。
  内容は「理科と同じ --check を付け、★同じ id で問題文が変わるが1件でもあれば --apply を止める」。
  ⚠️ **指示は別に出る。いま着手しないこと**（CLAUDE.md 0c）

#### ★★2026-09-23 夕　理科を取り込んだ（第2・3・4回。★公開はpushしていない）
司令塔 [577495] の承認ごとに `--check` → `--apply` を1回ずつ。**data.js は直したが push はしていない。**
```
          配信中   いま     消えた  増えた
第1回     73      73        0      0   （元データと差0。取り込み不要）
第2回     17      12        5      0   （calc に回した5問）
第3回     89      59       55     25
第4回     89      77       12      0   （r4m10 は残置）
data.js 総数 1674 → 1627 ☀ id重複 0
--verify-ids … ★四回とも **一致 221件 / 食い違い 0件**（問題文まで突き合わせ）
```
★**どの回も「同じ id で問題文が変わる」は 0件。残った問は1バイトも変わっていない。**

##### ★第3回は2度取り込んでいる（理由を残す）
1度目は `32ba1077…`（memo 68・「39消じ18増」の承認）。
その直後の `--verify-ids` で**元データがまた変わっている**のを検出し、司令塔から訂正が来た。
最終版は `331acefd…`（memo 59・表こ3の追加）。
★**教訓: 取り込む前に sha256 を照合する。**今回は `--verify-ids` が後から拾ったが、先に見れば無駄な取り込みが1回減った。
最終的に4つとも sha256 を確かめてから打っている（1:8f8ccb83 / 2:78a5efbd / 3:331acefd / 4:ae1b5e8e）。

##### 画像（3枚。★寸法と実物の両方をそろえた）
`images/` に `画像プレビュー/` からコピーし、`tools/sync_img_sizes.py --apply` で登録。
```
r3_seishitsu.jpg  1404x1864  新規
r3_shijiyaku.jpg  1404x 736  新規（★838 ではない。表を2を 5列→3列にしたため）
r3_chuuwa.jpg     1404x 556  新規（表こ3・中和の進み方）
★実物の寸法・申告の寸法・登録の3つが一致。sha256 も画像プレビューと同一
```

##### 通した確かめ
```
verify_data.mjs   合計: 問題なし（画像361種実在・id重複なし・夏期講習の突き合わせ全部 0件）
smoke-test 22 / list_meta_filter 21 / migration 22 / solo_session_by_id 20 /
settings_by_subject 16 / battle_resume 28 … すべて 0件失敗
```

##### ★実機の目視（B-12）—— 新しい道具 `tools/mikaku/deleted_ids_probe.mjs`
本物の Chrome・390px で**その回を頭から全問通しに出題**して見る道具を作った。
`mikaku_check2.mjs` は「名指しの数問を見に行く」道具で、**「出てほしくないものが出ない」を確かめられない**ため。
```
第2回  12問出題 / 消した5問とも出ない / 問題文全問一致 / JSエラー0
第3回  59問出題 / 問題文全問一致 / ★残った34問の出る順が配信中の版から変わっていない
第4回  77問出題 / 消した12問とも出ない / ★残った77問の出る順が変わっていない
表は3枚とも写真を目で見た：水溶液の表・指示薬の表（3列）・中和の表。
  ★つぶれていない。★空欄の丸数字が読める。★紙に答えは出ていない
```

##### ⚠️ この道具を作るときに踏んだ穴（★次の人へ）
1. ★**`#order-toggle` は checkbox ではなく `div.toggle`。**`o.checked` を見ると**常に undefined** で、
   一度も切りかわらない。★ランダム順のまま「並びが変わった」と**誤報した**。
   `classList.contains("on")` を見て、必要なら click する。**切りかわらなかったら止める**ことまで入れた
   ⇒ `mikaku_check2.mjs` にも同じ書き方がある（あちらは名指し探索なので実害なし）
2. ★**`#order-toggle` は `#battle-settings-panel` の中にあり、たたまれている。**先に開くこと
3. ★**「出題中か」を先に見る。**あとで見ると**最後の1問を2回数える**（77問が78問と出た）
4. ★**出題の並びは data.js の並びとは違う。**比べる相手は**「消す前に同じ手順で出したときの並び」**
   （`--before <消す前の data.js>` で固定する。4-6c と同じ考え方）

#### ★★2026-09-23 公開した（コミット 381e9c8）
ユーザー承認ごみ（司令塔 [577495] 経由「はい」）。`origin/master` へ push、Pages ビルド built。
```
公開版  第1回73 第2回12 第3回59 第4回77 = 221問（総数 1627問・id重複 0）
        元データと 一致 221件 / 食い違い 0件（問題文まで）
        消した17問（5＋12）は公開版にいない
        公開URLで直接出題：第2回12 / 第3回59 / 第4回77、JSエラー0
        表　3枚とも公開版の写真を目視。つぶれなし・丸数字が読める・答えは出ていない
```

##### ★「バイト一致」は **作業コピーではなくリポジトリの版と比べる**
```
作業コピー  645592 B  CRLF 16925
リポジトリ  628667 B  LF   16925   ← 公開版とバイト一致
```
`core.autocrlf=true` なので、**data.js は昔から LF で保存されています**（HEAD~5 も LF）。
★作業コピーと比べると**必ず「ちがう」と出ます。**`git show HEAD:data.js` と比べること。
（index.html・画像3枚は作業コピーとも一致します）

##### ⚠️ ★公開版でだけ出た冗談（道具の側の穴）
**r3m121（中和の表）の写真に、前の問の指示薬の表が写っていました。**
★**アプリの不具合ではなく、写真を撮るのが早すぎただけでした。**
画像は別ファイルなので、**入れかわった直後は前の画像がまだ画面に残っています。**
★**ローカルだと瞬時に入れかわるので、気づけません。網越しで初めて出ました。**
→ `deleted_ids_probe.mjs` に **`#solo-img` の src を突き合わせる検査**を足しました。
   「画像が出ているか」ではなく **「その問の画像か」** を見、**読み終わるまで待つ**。
   直したあとは公開版で **画像全問一致・読み終わっていない0件**。写真も正しくなりました。
★教訓: **画面の写真を証拠にするなら、「何が写っているか」を写真以外の方法でも確かめること。**
★教訓: **ローカルで通っただけでは足りない**（確認ポイント 0-4）。これがその実例です。

#### ★★2026-09-23 夕々　第1回・第4回を取り込んだ（★未コミット・未公開。ユーザー承認待ち）
```
          公開中   いま    消えた  増えた
第1回      73      55      23      5   （★r1m03 は残置。r1m74〜78 が新規）
第2回      12      12       0      0   （差0を --check で確認）
第3回      59      59       0      0   （同上）
第4回      77      76       1      0   （r4m10。★r4m32・r4m33 は残置）
data.js 総数 1627 → 1608 ☀ id重複 0
--verify-ids … ★四回とも 一致 202件 / 食い違い 0件（問題文まで）
★四回とも「同じ id で問題文が変わる」0件。残った問は1バイトも変わっていない
第1回・第4回以外の並びも中身も無変更
```
sha256 を取り込み前に4つとも照合（1:ded0e9f7 / 2:78a5efbd / 3:331acefd / 4:6c18ce9d）。

##### 画像4枚
```
r1_shinrin.jpg  ★新規 1404x1075   r3_seishitsu 1404x1864
r3_shijiyaku    1404x 736           r3_chuuwa    1404x 556
★実物の寸法・申告・IMG_SIZES の登録 の3つが一致。sha256 も画像プレビューと同一
★接頭辞が `r1_`（`r3_` ではない）ことを確認した
```
⚠️ ★**`images/r1_shinrin.jpg` は未追跡です。公開するとき必ずコミットに入れること**。
（前回、画像3枚を入れずに push しかけました。`git status --porcelain | grep '^??'` を必ず見る）

##### 通した確かめ（ローカル）
```
verify_data.mjs … 画像362種実在 / id重複なし / 図表を指すのに画像が無い問 0件 / 夏期講習 全部 0件
smoke 22 / list_meta_filter 21 / migration 22 / solo_session_by_id 20 /
settings_by_subject 16 / battle_resume 28 … すべて 0件失敗
deleted_ids_probe … 第1回55問（消した23問が出ない）/ 第4回76問（r4m10 が出ない）
   ★画像は「その問の画像か」を src で突き合わせて全問一致、読み終わっていない0件
   森林の表（r1m74）を目視。つぶれなし・空欄①〜⑤が読める・★答えは出ていない
```

#### やっていないこと
- ★**`--apply` は一度も走らせていません。**`data.js` は `git diff` で無変更、`data.js.bak` も作られていません
- ★**元データ（`5年下\quiz_csv_理科\*.json`）も書きかえていません**
- 取り込みと公開は、司令塔の指示待ちです

### 🚨2026-09-23 ★「もう一度つなぐ」を直した（`leave()` を待ってから入り直す・ローカルのみ・★公開の承認待ち）
ユーザー承認ずみ（司令塔 [577495] 経由）。下の「原因を特定」の節がこの直しの根拠。

#### 直したところ（`index.html` のみ。画面の流れ・番号・data.js・記録は変えていない）
- `leaveRoomAndWait()` … ★**`await room.leave()`**（固定の待ち時間では代用しない。ライブラリの実装が変わると壊れるため）
- `retryRejoin()` … 連打止め（処理中はボタンを押せなくする）＋ 知らせを消す ＋ 入り直し
- 作る側・入る側の両方のボタンが、これを通る

#### ★直る前と後（`tools/mikaku/retry_button_probe.mjs`・まねごとの待ち合わせ先。★直す前に先に測った）
| | 押したあと8秒の告知 |
|---|---|
| 直す前・作る側 | ★**0件** |
| 直す前・入る側 | ★**0件** |
| **直したあと・作る側** | **4件** |
| **直したあと・入る側** | **4件** |
連打（5回続けて押す）… 名乗りは続き、告知は8件（1回ぶん4件前後なので、何倍にもならない）。番号も変わらない。エラー0
通し: verify_data / smoke 22 / list_meta_filter 21 / migration 22 / battle_start 17 / battle_resume 28 / diag_room / diag_retired

#### ⚠️ 言えること・言えないこと
★**言えるのは「押すと名乗りが止まる」が直ったところまで。**
**「対戦がつながるようになった」とは言えません**（まねごとの待ち合わせ先では症状の再現が不安定: 60秒×／90・120秒○）。
**実機での確認はユーザーにお願いする。**

#### 公開の確認（2026-09-23・`6b69d48`）
公開 index.html に `leaveRoomAndWait`／`retryRejoin`／連打止めあり。`data.js` 1674問のまま。版 `2026-09-23`。
★**公開サイトを実機の Chrome（390px）で開いて、本物の待ち合わせ先ごしに1回だけ確認**:
```
1回目のパネル       名乗り: 出した 24回（6本×4件） ／ 部屋に入ってから 20秒
「もう一度つなぐ」→  名乗り: 出した 24回（★0から数え直して同じところまで戻った） ／ 部屋に入ってから 20秒（★数え直し）
部屋の番号 3226 → 3226（変わらない） ／ エラー 0
```
⚠️ 連打止めは、ここでは確かめられなかった（押すと**ボタンごと隠れる**ため `disabled` を読めない）。
　 連打の確認は `retry_button_probe` の③（5回続けて押しても告知が何倍にもならない）で取ってある。

### 🚨🚨2026-09-23 ★原因を特定：「もう一度つなぐ」は**入り直せていない**（ライブラリの作り。★まだ直していない）
依頼: 司令塔 [577495]（(A)開き直しは何をリセットしているのか (B)何秒ごとなら安全か）

#### ★(A) 答え: **`leave()` を待たずに `joinRoom` を呼んでいること**。ページの開き直しは要らない
`@trystero-p2p/core@0.25.4` のソース:
```
joinRoom の先頭:  if(f[S]?.[R]) return f[S][R]      ★同じ appId・同じ部屋番号なら、既にある部屋を**そのまま返す**
leave():          await 送信; await 99ms待つ; 相手を捨てる; 片づけ()   ★片づけは少なくとも99ミリ秒あと
アプリの「もう一度つなぐ」: room.leave()（待たない）→ すぐ joinRoom(同じ番号)
                  → ★**古い部屋を受け取り、その直後に片づけられる**
```
★実測（`tools/mikaku/rejoin_timing_probe.mjs`・まねごとの待ち合わせ先）:
| やり方 | 同じ入れ物が返るか | 入り直したあと8秒の告知 |
|---|---|---|
| **待たずに入り直す（いまの作り）** | ★**はい** | ★**0件**（＝名乗りが止まる＝相手から見つけられない） |
| 150ミリ秒 待つ | いいえ | 4件 |
| 300ミリ秒 待つ | いいえ | 4件 |
| ★**`leave()` を待ってから** | いいえ | 4件 |

→ ★**「もう一度つなぐ」は、押すと逆に黙る。**ユーザーの「押しても繋がらない」と完全に一致。
→ ★**最小の直しは `await room.leave()` してから入り直すこと。**ページの開き直しは不要（画面もちらつかない）

#### (B) 通信量（`tools/mikaku/announce_cost_probe.mjs`・待ち合わせ先1本あたり）
- ふつうに入ると **最初の5秒で4件**、そのあと60秒は静か（入り直すたびに「名乗りの山」ができる）
- **10秒ごとに入り直すと、ふつうの約6倍**（1分あたり24件／本 ＝ 本物6本で約144件／分）→ ★締め出しの危険
- → **30秒以上**を推奨。**相手の名乗りを受けたら止める／上限回数／ばらつき（jitter）**も要る

#### 🚨(C) ユーザーの案「10秒ごとに自動で入り直す」は、そのままでは**逆効果**だった（実測）
`tools/mikaku/periodic_rejoin_probe.mjs`（90秒後に相手が来る・25秒ごとに入り直す）:
```
A いまの作り（入ったまま待つ）   … ○ 出会えた
B 25秒ごとに入り直す（提案）     … ✘ 出会えない（入り直し5回）
```
★**相手が来ている最中に入り直すと、その握手ごと壊します。**
→ 自動でやるなら **「相手の名乗りを受けていない間だけ」**という歯止めが要る（診断の `diag.ann.recv` がそのまま使える）
⚠️ **まねごとの待ち合わせ先は、症状の再現が不安定**（60秒で出会えず・90秒と120秒では出会えた）。
　 **直しの効果をここで測りきることはできません。**本物・実機での確認が要ります

#### ★次にやること（承認待ち。まだ触っていない）
1. **「もう一度つなぐ」を `await leave()` してから入り直す形にする**（これだけで、ユーザーが押している手が効くようになる）＋連打止め
2. 自動の入り直しは **30秒以上・相手の名乗りがない間だけ・上限あり** で設計してから

### 🚨2026-09-23 調査：対戦が「少し経つと入れない」（★index.html は診断パネルだけ・未公開）
依頼: 司令塔 [577495]（ユーザー証言: 素早く入れば入れる／少し経つと入れない／もう一度繋ぐでは直らない／ホストが抜けて作り直すと入れる／両方10秒以内にリロードすれば入れる）

#### ★分かったこと（すべて実測）
| 調べたこと | 結果 |
|---|---|
| 20秒であきらめたあと、部屋は生きているか | ★**生きている。**`startConnectTimeout` は知らせを出すだけで `leave()` を呼ばない。**25秒後に入った相手と2.4秒で出会えた**（`meet_probe --delay 25`） |
| 購読は切れていないか（司令塔の見立て） | ★**切れていない。**5分の観察で **CLOSED 0件**・イベントは25〜55秒ごとに届き続け・ソケットも開いたまま（`relay_subscription_probe`）。→ **「記録の○が嘘」ではなかった** |
| ★症状の再現 | **60秒・180秒・300秒後に入った相手とは、3回とも出会えなかった**（本物の relay・1回の長い観察） |
| ★名乗りは届いているか | ★**届いている。**出会えなかった入る側の記録に「**★相手の名乗りを受けた 8回**」。**届いているのに、つながらない** |
| 「もう一度つなぐ」は何をしているか | ★**すでに `leave()` → `joinRoom`（同じ番号）をやっている。**押しても直らなかった（実測）。両方が押しても直らない |
| ホストが抜けて作り直すと？ | ★**出会えた。**ただし **番号は同じ（2880）のまま**だった。→ **効いているのは「番号が変わること」ではない**。変わったのは**入る側がページを開き直したこと**と、部屋を作り直したこと |
| まねごとの待ち合わせ先でも起きるか | ★**起きた**（60秒で1回）。→ **relay 側の事情ではなく、こちら側（アプリ／ライブラリ）の話**。ただし **120秒では出会えた＝時間で決まるというより不安定** |

#### ★いまの見立て（★まだ直していない）
- **20秒の知らせは無実。**部屋は生きているし、購読も生きている
- **名乗りは両方向に届いている**のに、ライブラリが**相手として扱わない**ことがある（`onPeerHandshake` が鳴らない）
- 効く手は**ページを開き直すこと**（両方）。`leave()`→`joinRoom` だけでは戻らない
  → ROLE.md の「**relay の接続はページの寿命ぶん使い回される**（`createRelayManager` の `relays`）」と同じ型
- **直し方の案**: 「もう一度つなぐ」を**ページの開き直し＋同じ番号で入り直し**にする（番号は `saveLastCode` で残っている）。
  ＝ユーザーが手でやっていることを、ボタンがやるだけ。⚠️ 連打できないようにすること（relay に嫌われる）

#### 診断パネルに足した3つ（`index.html`・★公開の承認待ち・版 `2026-09-23`）
- ★**部屋の番号**（2台ぶん並べれば、同じ部屋にいたかが一目で分かる）／**部屋に入ってからの秒**
- ★**名乗り: 出した / 自分のが返ってきた / ★相手の名乗りを受けた**
  ⚠️ 待ち合わせ先は**自分の名乗りも送り返してくる**ので、pubkey で自分のぶんを分けて数えている（混ぜると「届いている」と誤読する）
- 実物で確認: 入る側の記録に `★部屋の番号: 2464 ／ 部屋に入ってから 20秒` `名乗り: 出した 22 回 ／ 自分のが返ってきた 15 回 ／ ★相手の名乗りを受けた 8 回`

#### 道具（すべて新規）
`relay_subscription_probe.mjs`（購読が生きているかを1回で長く観察）／`retry_same_code_probe.mjs`（同じ番号で入り直す vs 作り直す）／
`meet_age_probe.mjs`（部屋の古さと出会えるかの境目・**まねごとの待ち合わせ先なので何度でも回せる**）／`diag_room_probe.mjs`（パネルの新しい行が鳴る／鳴りすぎない）／
`fake_relay.mjs` に **broadcast（告知を配る）と injectEvent（よその名乗りを差しこむ）** を追加
⚠️ 本物の relay を使うのは `relay_subscription_probe` と `retry_same_code_probe`。**続けて回さないこと**（2026-09-20 に締め出された）

#### ★`isDue` は定義だけで、呼び出し0箇所（司令塔の読みの裏取り・2026-09-23）
**`nextDue` は答えるたび保存されているのに、出題の判断には一度も使われていません。**
★実測: **次に出す日を1年先にしても、復習ミックスにふつうに出ました。**＝**間隔反復は効いていません。**
並びを作っているのは `lastCorrectAt`（正解した日が古い順）です。**いまは直さない**（ユーザーの目的は果たせているため）が、
**「間隔反復が効いている」と誤解しないこと**（確認ポイント 0-3）。

### ★2026-09-23 調査：「今週の単元＋昔クリアした問題の復習」は、いまの作りでできるか（★index.html は触っていない）
依頼: 司令塔 [577495]。ユーザー「朝晩で1日30問。1回では終わらない。授業直後に1回で答えられた用語も、いまは忘れているはず」
道具: `tools/mikaku/review_mix_probe.mjs`（新規・実機 Chrome 390px・7項目すべて実測）

#### ★結論: **いまの「復習ミックス」でできる。作るものは無い。ただし設定が2つ要る**
| 調べたこと | 実測 |
|---|---|
| 昔の単元が**全部クリアずみ**でも足されるか | ★**足される**。3段目が「正解した日が古い順」なので、**昔クリアしたものほど先に戻る** |
| 足される順 | 3段の並び（未実施→苦手→それ以外／各段は正解日の古い順）。★**最優先は「選ばれる問題」を変えない**（並びだけ） |
| 2回目 | ★**同じ10問は居すわらない。**解くと正解日が今日になり、次は11〜20番目に古い10問に進む |
| `isDue`/`nextDue` | 🚨 **司令塔の読みどおり。`isDue` は定義だけで呼び出し0箇所**。`nextDue` は保存されるが出題に使われない。**次に出す日を1年先にしても、ふつうに出た**（実測）。＝間隔反復は**効いていない**。並びは `lastCorrectAt` が担っている |
| 問題数30＋復習10 | ★**合計40問**（「30のうち10が復習」ではない）。1日30問なら **問題数20＋復習10** |
| ★はじめの状態の落とし穴 | **復習する単元は「選んでいない単元ぜんぶ」が自動でON。**その状態だと足される10問は**まだ一度も解いていない単元**（1段目）から来て、**昔クリアした問題は回ってこない**（実測: 10問とも公民1） |
| 来週の付けかえ | ★**先週の単元は、復習の対象に自動では入らない**（「知っている単元」として覚えられるため）。**毎週タップして足す操作が要る** |

#### ★ユーザーに渡す手順（1日30問・今週＋昔の復習）
1. 「単元をえらぶ」… **今週の単元だけ**を選ぶ
2. 「問題数」… **20問**（合計は 問題数＋復習ミックス）
3. 「復習する単元」… ★**いったん「クリア」で全部外し、昔やった単元だけをタップ**（ここを飛ばすと未実施の単元が出る）
4. 「復習ミックスの問題数」… **10**
5. ボタンが「**30問（復習10問こみ）**」になっていれば OK
6. 来週は 1 の単元を付けかえ、**先週の単元を 3 に足す**

#### 次に作るかもしれないもの（★ユーザー判断待ち。着手しない）
- 「復習する単元」に**先週までの単元をまとめて足す**ボタン（毎週の操作を減らす）
- `isDue` を出題に効かせる（間隔反復の本来の姿）。★**いまの並びで足りているなら要らない**

### ★2026-09-22 ホームに「未クリア」（あと何問）と、問題数「15問」を足した（`b6574ee`・★**公開ずみ**。ユーザー承認は司令塔経由）
ユーザー（司令塔経由）:「毎週その週の単元で、とりあえず未はなくなるようにしたい。あと何問未クリアがあるかわかるように。15問の選択肢を」
- ホームの数字の列に4つ目「**未クリア**」。数え方は `refreshUnmasteredCount()` ＝ `buildPool(true)`（選んだ単元＋種別・優先度・難易度の絞りこみ）のうち `!isMastered`（box 0。まちがえた問も入る）
  ＝「未クリアの問題」だけオン＋「全部」で出る問題と**同じ集合**。★絞りこみを付けていると「全問題数−正解済み数」とは合わないことがある（ほかの3つは単元だけで数えているため）
  　ラベルは「未クリア あと」だと 390px で2行に折れたので「未クリア」にした
- 問題数に「15問」（10問と20問のあいだ）。保存は既存の `count` のまま。★6つになり「自由入力」が2行に折れたので、`#count-row .toggle` だけ幅を字に合わせた
- 🚨 **もとからあった食いちがいを1件直した**: `renderHome()` がボタンの「◯問」を数え直していなかった → 解いてホームにもどると、ボタンは前の数のまま（出題そのものは正しい数）。`refreshCreateButton()` を足した
- 判定・3段の並び・★最優先・stats・data.js は触っていない
- 検査: `tools/mikaku/unmastered_count_probe.mjs`（新規・実機390px）… 表示＝記録から独立に数えた数＝トグル＋全部の数＝実際に始めた問題数（59）／単元を足すと増える（59→180）／全部正解ずみで 0／〇を2問で 59→57・トグルの数とも一致／15問が選べて開き直しても残り、始めると 15問
  通し: verify_data / smoke 22 / list_meta_filter 21 / migration 22 / battle_start 17 / battle_resume 28 / priority_probe / orphan_probe
- 見た目: `tools/mikaku/shots/unmastered_statrow.png`・`count15_row.png`
- **公開の確認（2026-09-23）**: 公開 index.html に `stat-unmastered`・`15問`・`refreshUnmasteredCount` あり／公開 data.js 1674問（変化なし）／★**公開サイトを実機の Chrome（390px）で開いて**「未クリア 113」「15問」を確認・エラー0（`shots/live_statrow.png`）

### ★2026-09-22 理科 第4回：消化の表の画像だけ差し替え（ヒントを大きく・濃く。`b33b65c`・★**公開ずみ**。ユーザー承認は司令塔経由）
ユーザー「ヒントの文字が小さすぎます」→ 画像を作り直したもの（司令塔 [577495] 依頼）
```
images/r4_04.jpg sha256 23594db6…（331417B）一致 ／ JSON は eacb7c39… のまま
IMG_SIZES r4_04 1404x1762 → ★1404x1960（sync_img_sizes --apply。ほかは不変） ／ data.js 差分 0
検査 verify_data 問題なし / smoke 22 / list_meta_filter 21 / migration 22 / battle_start 17 / battle_resume 28
```
- 実機（390px）r4m80/85/89 出題3/3・JSエラー0。★ヒントが表の本文と同じ大きさで、拡大しなくても読める
- **公開の確認（実物）**: 公開 IMG_SIZES r4_04 = [1404,1960]／公開 images/r4_04.jpg sha256 23594db6… 一致／公開 data.js 1674問・ヒント入りの q あり

### ★2026-09-22 理科 第4回：消化の表 r4m80〜r4m89 にヒントを入れた（`94b0417`・★**公開ずみ**。ユーザー承認は司令塔経由）
依頼: `司令塔\回答\対戦アプリ_消化の表ヒント取り込み_依頼_2026-09-22.md`（司令塔 [577495]）
```
sha256 json eacb7c39…（55465B・89問・CRLF）／ 画像 r4_04.jpg 8539e10d…（1404x1762）どちらもバイナリで一致
公開ずみ（HEAD ff58663）との差（id）: 総数1674・追加0・削除0・★変更10＝r4m80〜r4m89 の q だけ（a・img・他の欄は不変）
data.js 行: 10行の入れかえだけ ／ IMG_SIZES r4_04 1404x1583 → 1404x1762（sync_img_sizes --apply。ほかは不変）
検査 verify_data 問題なし / smoke 22 / list_meta_filter 21 / migration 22 / battle_start 17 / battle_resume 28
```
- 道具 `tools/patch_r4_table_hints.py`（新規）: 【消化の表N】の見出しで対応づけ、a と img の一致を独立の証拠にして q の行だけ差し替え
- 実機（mikaku_check2・390px）: r4m80/81/84/85/88/89 出題6/6・JSエラー0。表に①〜⑩と灰色のヒントが出る／問題文にヒントの語が出る
- ⚠️ 気づき: スマホ幅だとヒントの灰色の字はかなり小さい（読めるが、拡大したくなる大きさ）。画像側の話なので報告のみ
- ⚠️ **スマホ幅で灰色のヒントが小さい。読めるが拡大したくなる。**ユーザーには司令塔が伝えた。問題文にもヒントがあるので今回は直さず公開。読みにくければ、あとで絵を作り直す
- **公開の確認（実物）**: 公開 data.js 1674問・r4m80〜89 の q 10件にヒントの語あり／公開 IMG_SIZES r4_04 = [1404,1762]／公開 images/r4_04.jpg の sha256 8539e10d… 一致
  （公開 data.js と手元の作業コピーは改行の違い（CRLF／LF）で cmp 不一致。コミットの中身との比較は、自動許可の判定で止められたので未実施）
- この作業メモの追記はローカルコミット `9fa1be3`（push は次の公開で一緒に）

### ★2026-09-22 ★最優先の並び方を直した（`9edcee7`・★**公開ずみ**）— ★**下の 4903e74 の記録の「特別扱いしない」は誤り**
ユーザー訂正（司令塔の読み違いを直したもの）:「出題順の一番頭に」「未クリアのみを押したら、最優先で未クリアなものは一番上」
「間違いやすい問題も、いつもの順番で並び替えて、順序が全く一緒なら、優先度高が一番上」（優先度高＝★最優先のこと）

#### 【仕様】
```
ふだん           … ★を出題順のいちばん頭に置く
未クリアのみ     … 未クリアの問のうち、★を最初に置く（正解ずみの★は未クリアに含まれない）
よく間違えるのみ … 3段の並びで並べ、並びの鍵（段・最後に正解した日）が同じ問どうしでは★を先に置く（★は同点のときの決め手）
未クリア＋よく間違える（両方オン）… 未クリアのみと同じく、★を最初に置く（2026-09-22 ユーザー確認）
```
**どの問が入るか（絞り込み）は変えていない。変えたのは並びだけ。**★0件なら直す前と同じ。

#### 【備考（結果として起きること。仕様ではない）】— 鍵の細かさの実測
- 並びの鍵は「段」と「最後に正解した日（`lastCorrectAt`）」
- ふつうに解いたときの正解日は **`Date.now()` のミリ秒** → **2段目（一度は正解した問）どうしの同点はめったに起きない**
  → よく間違えるのみでは、**2段目の★は並びが変わらないことが多い。これは仕様どおり（ユーザー確認ずみ）。★の処理は消さないこと**
- **1段目（まだ一度も正解していない問）は正解日が無く、鍵が全部同じ（0）** → ★は**必ず1段目の先頭**に来る
- 例外: **一覧で手で直した正解日・CSV で取り込んだ正解日は日単位（その日の0時）** → 同じ日どうしは同点になり★が効く
- ⚠️ 鍵をわざと粗くして★を効かせる工夫は**しない**（ユーザー「その場合、星は効かなくてもいいです」）

#### 直したところ（`index.html`）
- `orderByTiers(indices, starTie)` … starTie のとき、段も正解日も同じなら★を先（**復習ミックス・fillFromRest は starTie なし＝並びを変えない**）
- `buildFinalPool` のトグルの枝 … `orderByTiers(pool, true)` → 未クリアを含むなら `starsFirst` → `fillFromRest`
- ホームの「★最優先 N問が先に出ます」… ★が頭に来る出し方（ふだん・未クリアを含む）のときだけ
- コメントを【仕様】と【備考】に分けて書き直した（古い「特別扱いしない」を消した）

#### 検査（`tools/mikaku/priority_probe.mjs`・全部通過）
★0件なら**★を入れる前の版（991ecfa）とも、いまの公開版（HEAD）とも**表示・並びが同じ（5設定）／ふだん: ★が頭／
未クリアのみ: ★で未クリアの3問がいちばん上・正解ずみの★は出ない・数は同じ／両方 ON: ★が上／
よく間違えるのみ: 1段目の★が1段目の先頭・2段目の★（鍵がちがう）は位置が変わらない・★の無い問どうしの順は同じ／
一覧・保存・記録に触らない は前回どおり。通し: verify_data / smoke 22 / list_meta_filter 21 / migration 22 / battle_start 17 / battle_resume 28

### ★2026-09-22 ★最優先（親が問題一覧で付ける）を作った（`4903e74`・★**公開ずみ**。二人対戦でも★を先に出す＝ユーザー了承）
**ユーザーの選択**: 案「一覧で★だけ」。「星は消さなくていい」「正解したら未クリアのみからは外して」「苦手な問題のみの時の優先順位も他と一緒で」

#### 作ったもの（`index.html` のみ。`data.js`・記録には触らない）
- 保存: **`kq_battle_priority_v1`（id の一覧）**＝記録とは別の箱（漢字プリントの `kanji_app_picked_v1` と同じ形）。壊れていたら★なし扱い
- 問題一覧: 各行に **「☆最優先」ボタン**（押すと★）。右の□（いまだけ選んで出題・保存されない）とは別物
  　一覧の上に **「★最優先のみ」** の絞り込み、**件数**、**「表示中を全部★にする」「表示中の★を外す」**（確認つき）
- 出題（`buildFinalPool`）: **ふだん（トグル OFF）だけ★を先頭**へ → そのあと問題数で切る。シャッフル／出題順どおりの順は保つ。
  　復習ミックスで混ぜたあとも★を前に戻す。~~未クリア／よく間違える の枝は1文字も変えていない~~ ★**誤った仕様だった。上の「並び方を直した」が正しい**
- ホーム: 問題数の表示に「**（★最優先 N問が先に出ます）**」（ふだんの出題のときだけ）
- ⚠️ 二人対戦も同じ `buildFinalPool` を使うので、ホストが「二人で始める」ときも★が先に出る（2台では未確認）

#### 検査（`tools/mikaku/priority_probe.mjs`）
- ★**★0件なら直す前（HEAD）の版と、表示も並びも同じ**（5つの設定で並べて比べた）
- ふだん・出題順どおり: ★4問が先頭・5問目からいまどおり／ふだん・シャッフル: ★4問が先頭
- ~~未クリアのみ・よく間違える のみ: ★を付けても並び・数とも同じ~~ ★**誤った仕様での検査。上の記録が正しい**／正解ずみの★は未クリアに出ない
- 一覧: 付け外し・保存・★のみ・まとめて付け外し・開き直しても残る・**記録（stats）は1文字も変わらない**・エラー0
- 通し: verify_data / smoke 22 / list_meta_filter 21 / migration 22 / battle_start 17 / battle_resume 28
- 見た目: `tools/mikaku/shots/star_list.png`

### ★2026-09-22 理科 第4回：r4m90 を消し、消化の表を差し替えた（`991ecfa`・★**公開ずみ**）
**公開を実物で確認**（`4903e74` まで）: data.js・index.html とも公開＝手元／1674問・r4m90 なし／r4_04.jpg HTTP 200 252561B・sha256 が手元と一致／IMG_SIZES [1404,1583]／「☆最優先」「★最優先のみ」「kq_battle_priority_v1」あり
```
sha256 json 0c6657e3…（55282B・89問・CRLF）／ 画像 r4_04.jpg 101c6cf6…（1404x1583）どちらも一致
公開ずみとの差（id）: 追加0・変更0・★削除1（r4m90）・総数 1675 → 1674 ／ data.js 行: 追加0・削除13＝r4m90 の1ブロックだけ
照合 verify_rika_pointwise 4: 差分0 ／ IMG_SIZES r4_04 1404x1718 → ★1404x1583（sync_img_sizes が「ちがう」を検出して直した）
検査 verify_data / smoke 22 / list_meta_filter 21 / migration 22 / battle_start 17 / ★battle_resume 28（シナリオ7を追加）
```
- 実機: 表が出て①〜⑩が読める／★吸収の行は ⑨・⑨・⑩（毛細血管・毛細血管・リンパ管）。門脈・肝臓・静脈・⑪は無い／空らんに答えなし

#### ★消えた問の記録（stats[r4m90]）が残っても壊れないか（道具 `tools/mikaku/orphan_probe.mjs`）
ホームの数字・苦手の数・問題一覧・CSV 書き出し・「前回まちがえた問題」・一人の途中再開 → **すべて壊れない**。記録そのものは消えずに残る（C-5）。
CSV には消えた問の行は出ない（書き出しは data.js の問だけ）

#### 🚨 1件見つけて直した（`index.html`）：**二人対戦の途中再開に、消えた問が入っていると画面が落ちた**
- 一人の途中再開は消えた問を取りのぞくが、**二人のほうは保存した questionIds をそのまま使い**、`getQById()` が null で `d.u` で落ちた
- ★直す前に `test_battle_resume` シナリオ7（次に出す位置に消えた id を混ぜて再開）で**落ちることを確かめてから**直した
  （`Cannot read properties of null (reading 'u')`）
- 直し: 再開のときに消えた id を取りのぞき、添字（currentIndex・recordedIdx）を数え直す。
  ★「採点ずみの問題には戻らない」の判定は、**取りのぞく前の添字**でする（順番に意味がある）
- 直したあと 28件すべて通過（前からの6つの再開シナリオも）

### 2026-09-22 ★親が「最優先」を決められるようにする → **案を出して止まっている（ユーザーの選択待ち）**
案 = `kosukequiz-battle\案_親が最優先を決める_2026-09-22.md`（画面3案・外れ方3案・保存と CSV の扱い）
- ★発見: **3段の並びは「よく間違える／未クリア」ON のときだけ**。ふだんはシャッフルか出題順どおり → ★は**どのモードでも先頭**に置く必要
- ★は端末ごと（localStorage）。別の箱 `kq_battle_priority_v1` に持つ＝記録に触らない。CSV は「最優先」列を足す（古い版は無視するので壊れない）
- 推し: 案2（一覧で付け、ホームに「★最優先だけやる」1ボタン）＋ ①親が外すまで残る

#### 受付時
依頼書 `司令塔\回答\対戦アプリ_親が最優先を決める_依頼_2026-09-22.md`。**現状を確かめ、画面の案を2〜3個出して止まる**。`index.html` は触らない。

### 2026-09-21 「直近足した問題の見方」を実機で確かめた（`index.html` は見るだけ・道具 `tools/mikaku/recent_probe.mjs`）
司令塔がユーザーに伝えた手順は**すべて合っていた**。
```
A 社会: 「全単元」で空 →「歴史」の文字を押してひらく →「第5回.総合」 → ★29問
B 理科: 「第4回」＋「出題順どおり」 → ★80〜90問目が r4m80〜90（1〜79問目も id 順）
C 問題一覧: 単元で絞れる（90問）／並びは id 順・いちばん下が r4m80〜90／★一覧の行にも表の絵が出る
D ★いちばん簡単: 問題一覧 → 単元をえらぶ →「未実施のみ」 → 理科第4回は ★新しい11問だけが残る
  （社会は全単元だと第5回29問のほか未実施の単元も出る。単元「第5回.総合」を選べば29問）
```
⚠️ B は**79問送らないと届かない**うえ、「わかった／まちがえた」で**記録が付く**。見るだけなら一覧（D）、
　 出題形式で見たいなら「チェックのみ（採点・記録なし）」を ON にする
⚠️ 「未実施のみ」は、**お子さんがもう解いた新しい問は出ない**（記録が付いているため）

### ★2026-09-21 社会 第5回の赤丸：**g5r1〜g5r7 の7問・9矩形を入れた**（g5r8・g5r9 は丸なし）★**公開**
**決着（司令塔判断・ユーザー承認）**: 案1＝**g5r9 は丸を付けない**（丸が読むべき文字を塗りつぶすなら、丸は害）。
再確認: 丸の数 7問・9矩形／実機で全9問「画面の数＝表の数」（g5r8・g5r9 は0）／★g5r9 の㋐〜㋓の文字が読める／
検査 verify_data・smoke 22・list_meta_filter 21・migration 22 通過／data.js 差分0

#### （以下は止めたときの記録）
- 受け取り: `5年下\quiz_csv\第5回_年表座標.md`。単位＝割合・測った絵＝kai5_01（1189x1027）の記載あり。**g5r8 なし**。13矩形／8問
- ★独立に描き戻して1つずつ切り出した → **13個とも正しい番号の上**（空らんの枠6つは 73〜74×34〜35px の1種類＝申告と一致）
- 実機: **画面の丸の数＝表の数（全9問、g5r8 は0）**。g5r5 は3つの「2」にぴったり。拡大してもずれない
- 🛑 **g5r9: 4つの丸が出来事の行ごと囲む形で、通常表示だと文字が赤線で塗りつぶされて読めない**
  （問いは「できごとの名前で答えなさい」＝**読めないと解けない**。拡大すると半分ほど読めるが、線が文字を横切る）
  → 案: ①g5r9 は丸を付けない（問題文で㋐〜㋓と名指ししている）／②㋐〜㋓の**記号だけ**を囲む（確認ポイント1-5「丸はラベル自体に」）。**司令塔の判断待ち**

#### （以下は予告を受けたときのメモ）
届いたらやること:
1. **単位（割合）と測った絵（`kai5_01.jpg` 1189×1027）の記載を確かめる。無ければ取り込まずに聞き返す**
2. `TIMELINE_MARKS` に `g5r1`〜`g5r7`・`g5r9` を追加（**`index.html` のみ。`data.js` は触らない**）
3. ★**`g5r8` は入れない**（「時期がまちがっているもの」を探す問い。⑪に丸を付けると答えが見える）。**入っていたら止めて報告**
4. ★実機で**全部の丸が正しい枠に乗るか**を、**拡大した状態でも**見る（`g5r5` は3か所、`g5r9` は㋐〜㋓の4か所）
   道具: `tools/mikaku/g4_timeline_probe.mjs g5r1 …`（2026-09-21 に教科・単元・絵を id から引く形にした）
5. 検査を通して報告 → **公開の承認は司令塔がユーザーに取る**

### ★2026-09-21 理科 第4回「消化の表」11問を取り込んだ（`f26c1c9`・★**公開ずみ**）
**公開を実物で確認**: data.js・index.html とも公開＝手元／公開 1675問・r4m80〜90 の11件／r4_04.jpg HTTP 200（284678B）／IMG_SIZES `[1404,1718]`。
**A-1 候補14件は直さない**（司令塔判断: 同じ知識を表と一問一答の2通りで聞いているだけ。r4m32 は語を外すと問いが成り立たない）

#### 結果
```
sha256 7e6a07f2…（56105B・90問・CRLF）一致 ／ 90問すべて memo・欠番なし＝id番号＝no（ずれ0件）
公開ずみとの差（id で数えた）: ★追加 11件（r4m80〜r4m90）／ 変更 0件 ／ 削除 0件 ／ 総数 1664 → 1675
★data.js の行の差分は「追加143行・削除0行」＝既存の行は1行も書き換わっていない
照合 verify_rika_pointwise 4（新しい sha）: 差分0 ／ 欄の総点検: 第4回 90/90・欠けなし
画像 r4_04.jpg 1404x1718（申告と一致）→ IMG_SIZES は sync_img_sizes で1件足した
検査 verify_data 問題なし / smoke 22 / list_meta_filter 21 / migration 22
```
- ★`import_rika.py` は第4回のかたまりを**まるごと書き直す**作りなので、**適用して行の差分と id 単位の差を数えてから**進めた（書式の揺れは出なかった）

#### ★実機（本物の Chrome・390px・画素2倍）
- 11問 **すべて出題でき、表（r4_04.jpg）が出る**。拡大（2.56倍）もできる
- ★**①〜⑪ は拡大しなくても読める**（くっきりした作図の表）
- ★**A-2b: 表の空欄はすべて空。11問の答えの語は表に1つも印刷されていない**（原寸を目で見た）
- `r4m85` の答え「しぼう酸とモノグリセリド」がそのまま出る

#### ★A-1 の候補が14件（判定は作成担当・司令塔。**データは触っていない**）
**既存の r4m の問題文に、表の答えの語が出ている。**
```
r4m19 「だ液アミラーゼは…」→ 表①の答え ／ r4m20 「…ペプトンに変える…」→ 表④
r4m21・22 「胆汁が…」→ 表⑤ ／ r4m24・33 「しぼう酸／モノグリセリド」→ 表⑥
★r4m32 「ブドウ糖やアミノ酸…門脈を通って肝臓へ」→ 表⑦⑧⑪ の3つが1問にまとまっている
r4m34・35・58 「肝臓」→ 表⑪
```
私の見立て: **同じ単元の同じ知識なので害は小さい**（表のねらい＝つなげて覚える、と同じ中身）。
ただし **r4m32 は3つ同時で、いちばん強い**。

#### 道具の直し
`g4_timeline_probe.mjs` が**教科を「社会」に決め打ち**していて、理科の単元が選べず「問題がありません」で止まった。
**教科も id から引く**ように直した（アプリは正しかった）。

### ★2026-09-21 社会 第5回（総合・29問）を取り込んだ（`b57e76f`・★**公開ずみ**）
**公開を実物で確認**: data.js・index.html とも公開＝手元（改行をそろえて一致）／公開 data.js 1664問・g5r 29件／
kai5_01.jpg（196276B）・kai5_02.jpg（50098B）とも HTTP 200／IMG_SIZES に kai5 2件。ビルド完了を待ってから取得。
**次**: 赤丸（g5r の TIMELINE_MARKS）はユーザーの声がかかったら社会担当が座標を測る。それまで待機

#### 結果
```
sha256 0d906320…（13185B・29問・LF）一致   画像 kai5_01 1189x1027 ／ kai5_02 743x534（申告と実寸が一致）
公開ずみとの差（id で数えた）: ★追加 29件（g5r1〜g5r29）／ 変更 0件 ／ 削除 0件   総数 1635 → 1664
u = 「第5回.総合」／ img: kai5_01 ×9・kai5_02 ×3 ／ ★sol: g5r8 の1件が入った
照合 import_g --check 1〜5: 全回「変わらない」（88/115/113/121/29）
欄の総点検: 第5回 29/29 対応・説明のつかない欠け 0（残るのは理科第2回の sol 3件＝既知）
検査 verify_data 問題なし / smoke 22 / list_meta_filter 21 / migration 22
```
- `import_g.py` の FILES に第5回を足した（1行）。新しい回は data.js の**末尾**に入る（並びは記録に影響しない）
- `IMG_SIZES` は `sync_img_sizes.py --apply` で実物から2件足しただけ
- **赤丸（TIMELINE_MARKS）は入れていない**（司令塔の指示どおり、後回し）

#### ★実機（本物の Chrome・390px・画素2倍）
- 図つき12問 **12/12 出題・JSエラー0**。★**12問とも図でしか分からない記号を指す**（Ⅰ〜Ⅲ・1〜4・①〜⑮・㋐〜㋓・★あ/う/え）＝図が無いと解けない
- ★年表は**拡大（2.56倍・画面上919px）で丸数字・㋐㋒・枠の番号まで読める**ことを目で見た
- ★地図は**★あ〜えが拡大なしで読める**。都の名前は写っていない（A-2b）
- ★`g5r8` の**解説が「こたえ」の下に出る**
- ★**「歴史」のチェック1回で第1〜5回（466問）が入る**（予告どおり）。「1〜4回だけ」には第5回を外す1タップ

#### 道具の直し（どちらも第4回を決め打ちしていた）
- `g4_timeline_probe.mjs` … 単元を問題の id から引く／絵は**その問の img**と照らす／**赤丸なしの回は「0と0」で合格**
  （★直す前は ✘5件。**道具の決め打ちで、アプリは正しかった**＝C-8b の型）
- `narrow_probe.mjs` … 「歴史」は「第◯回」の単元**全部**が入るかを見る形に

### 2026-09-21 待機中（司令塔の判断が出た）
- 社会1〜4回の絞り込み … ★**案1（何も作らない・手順を伝える）で決着。`index.html` は触らない**
  （案2 0件の説明／案3 歴史トグル … どちらも見送り）。落とし穴2つは司令塔からユーザーへ伝える
- ★**次に来るもの: 社会 第5回（30〜35問・`g5r`）の取り込み**（社会作成担当が作成中）
  - 年表 `kai5_NN.jpg`・**赤丸は後回し**の見込み（第4回と同じ）／地図1枚（聞き方を反転・答えは実体）
  - 届いたら: sha256 をバイナリで照合 → `import_g.py`（`no` が id 番号）→ id 単位で全件照合 →
    `sync_img_sizes.py` → 検査 → 実機（図が要る問は「図が無いと解けないか」まで）→ 承認 → push → 公開を実物で確認
  - ⚠️ 第5回は名前が「第5回.…」なら**自動で「歴史」に入る**（司令塔: 総合回なので好都合）
- ★名指しの回以外は着手しない（0c）

### 2026-09-21 社会 第1〜4回を「絞って回せる」か → ★**いまの画面でできる**（報告まで。`index.html` は触っていない）

#### 結果（実機・本物の Chrome・390px。道具 `tools/mikaku/narrow_probe.mjs`）
```
初めて: ①「全単元」で空に → ②「歴史」のチェック（第1〜4回 437問）→ ③「よく間違える問題」→ ④「一人で始める」 ＝ 4タップ
2回目から: ★設定が残る（単元もトグルも）→ いちばん上の「一人で始める（32問）」1タップ
問題数（テスト用の記録）: まちがえた32問 → 437問が ★32問 に。上の「苦手な問題」にも 32 と出る
```
#### ★落とし穴1: 既定は「全単元」が選ばれている
**その状態で「歴史」を押すと、歴史が外れる**（1377→940問。私も道具で1回踏んだ）。先に「全単元」で空にする。
#### ★落とし穴2: まちがえた問題が0件のとき（新しい子・全部できた子）
**止まらない代わりに、黙って「437問ぜんぶ」になる**（fillFromRest。2026-09-13 ユーザー判断「0問でも押せる」）。
ボタンに「437問」と出るので見れば分かるが、**説明は出ない**。「20問」にしておけば20問（未実施から順）。
#### その他
- 「よく間違える」＝ `wrong>0 && box<=1`（まちがえて、まだ2連続正解していない）
- 「未クリア」は未実施も含むので、437問の大半が未実施なら**ほとんど減らない**
- 「復習ミックス」は**選んでいない単元**（地理・公民）から足す機能。**1〜4回の復習には使えない**
- ⚠️ 第5回が「第5回.…」の名前で入ると、**自動で「歴史」に入る**（`u.startsWith("第")`）

#### 受付時のメモ（以下）
**依頼**: 第5回（総合＝1〜4回の復習）の準備。「全部はできない」ので**絞る**。
**やること**: ①実機で現状を確かめる（何タップで「1〜4回・まちがえた問題だけ」が出るか／設定が残るか／0件の画面）
②足りない所を小さい順に案として出す ③★**そこで止めて報告。`index.html` は触らない**

### ★2026-09-20 夜 ②診断パネルに「断られて使えなくなった」を入れた ＋ 🚨図の欠けは**誤報**でした（`84afed0`・**公開ずみ**）

#### 🚨 まず、理科の図の件：**1枚も落ちていません**
司令塔から「理科 第3回は元データ画像15件なのに data.js は0件」と来ましたが、**落ちていません。**
```
理科第3回  画像あり 15（memo  0 / calc 15）→ data.js 0   ★15枚とも calc。方針どおり取り込まない
理科第1回  画像あり 14（memo  9 / calc  5）→ data.js 9   ★memo の9枚ちょうど
理科第2回  画像あり 85（memo 10 / calc 75）→ data.js 15  ★memo 10 ＋ 残した calc 5（既知）
理科第4回  画像あり 10（memo 10 / calc  0）→ data.js 10  ★一致
```
★**「計算問題は本で解く」（2026-09-12）の方針そのままです。**取り込みの道具の不具合ではありません。
⚠️ **ただし「第3回の memo には図が1枚も無い」は事実**です。
**memo に図が要るかどうかは中身の話**なので、理科担当のA/B判定を待ちます（こちらの持ち場ではありません）。

#### ★★全教科・全回で「元データにあって data.js に無い欄」を洗いました（`sol` 事件の型・0-2）
**道具 `tools/audit_fields_vs_src.py`。問題文で対応づけ、欄ごとに数えます。**
```
🚨 説明のつかない欠け（全28ファイルの合計）: ★sol 3件（理科 第2回）だけ
★対応がつかなかった行 584行 = 漢字ファイル 489行（別アプリの持ち物）＋ 理科 calc 95行
  → ★584 = 489 + 95 でぴったり。**取りこぼしはありません**
```
★**最初は「genre 1461件 / subject 1461件 が欠け」と出ましたが、これは私の道具の誤りでした。**
**`genre → u`・`subject → subj`・`file → img` と、data.js 側で名前が違うだけ**でした。
→ ⚠️ **名前が違うものを「欠け」と数えると、本物の1件（`sol` 3件）が 2922件に埋もれます。**
　 **道具に対応表を書いて、理由つきで別枠に出すようにしました。**

★**残った `sol` 3件（理科 第2回）は、引きつぎに載っていた「取り込み判断待ち」そのものです。**
**新しい発見ではなく、既知の保留が1件、正しく残っているだけ**でした。

#### ★② 診断パネル（`index.html` を触りました。`data.js` は1バイトも触っていません）
```
待ち合わせ先（○いま開いている △切れた ✕だめ ✘断られた）: ○… / ✘…
★断られて使えなくなった: 1 本 ／ relay.damus.io「banned: too many rate-limit violations…」
                        （アプリを閉じて開き直すと戻ります）
```
- ★**理由の文字列は、そのまま出します。**要約すると次に読む人が原因を絞れません
- ★判定は**ライブラリと同じ区切り**（`CLOSED` または `OK=false` で `rate-limited:`・`duplicate:` 以外）
- 受信の解析は**先頭が `["OK"` / `["CLOSED"` のときだけ**（中身のほとんどは EVENT なので、そこで帰す）
- `diagRetired` は **`diagReset` で消しません**。★**開き直すまで戻らない**のが実態だからです

#### ★検査（両側を見ています・4-3）: `tools/mikaku/diag_retired_probe.mjs`
```
① 誰も断らない        → 「なし」と出る          ✔（鳴りすぎない）
② 1本が banned で断る → 「1 本」＋理由そのまま   ✔ 待ち合わせ先の行にも ✘ が付く
③ rate-limited: で断る → ★鳴らない              ✔（ライブラリの区切りと一致）
```
⚠️ **まねごとの待ち合わせ先です。**本物・実機で同じ文言が返る保証はありません。
⚠️ **パネルは「相手が見つからないまま20秒」たってから出ます。**短く待つと空を読みます（1回踏みました）。

#### 通した検査
```
verify_data 問題なし / smoke 22件 問題0 / list_meta_filter 21/0 / migration 22/0
test_battle_start 17/0（本物の trystero）/ meet_probe --delay 0,5 → どちらも出会える
★data.js の差分 0（git diff で確認）
```

#### 次
- ★**push の承認をお願いします**（`index.html` を触ったため）
- 2台ぶんの診断パネルが届けば、**この行で一発で読めます**

### ★2026-09-20 夜 **「原因が確定」は撤回します**。ただし直すべき形は、ソースから確定しました

**司令塔の指摘（「今日の自分の試験が banned を招いた可能性」）は当たっていました。**

#### ★★決め手: **damus は、いまは受け付けます**
```
21:5x 頃  ["OK",…,false,"banned: too many rate-limit violations, try again later"]
その約10分後  ★○受け付けた
```
**"try again later" は文字どおりでした。**★**数分〜十数分で解けています。**
→ ⚠️ **今日ここで何十回も叩いたのが原因と考えるのが自然です。**
→ ★**したがって、これは「お子さんが何日も前から繋がらない」ことの説明になりません。**
　 **「原因が確定した」は言いすぎでした。撤回します**（0-1）。

#### ★司令塔の問い「damus だけか、他も出るか」への答え
**`relay_probe.mjs` に ⑤「書き込み（告知）を受け付けるか」を足して測りました。**
```
[いま使用] ○ nostr.sathoarder.com / nostr-01.yakihonne.com / top.testrelay.top
           ○ relay.damus.io（★さっきは banned、いまは○）/ nos.lol / relay.primal.net
[候補]     ○ nostr.mom / relay.snort.social / purplerelay.com / nostr.data.haus
[候補]     ✘ relay-rpi.edufeed.org ← "blocked: ephemeral kinds are not accepted on this relay"
```
★**banned が出たのは damus だけ**でした（しかも一時的）。**他の先は正常です。**

#### ★★それでも直す価値がある形が、ソースから確定しました
**「一時的な拒否が、そのページのあいだ永久の損失になる」**——これが本体です。
```ts
// nostr/index.ts … "banned:" は rate-limited: でも duplicate: でもないので terminal
isTerminalRejection = CLOSED || (OK=false && !rate-limited: && !duplicate:)
if (isTerminalRejection) retireRelay(client)      // WeakSet に入れて client.close()
```
```ts
// core/utils.ts … ★接続は**URLごとにページ内で使い回す**
const relays: Record<string, TRelay> = {}         // ページの寿命ぶん残る
register: (key, createRelay) => { const relay = relays[key]; if (relay) return relay; … }
```
★**`register` は、切り捨てずみの接続でもそのまま返します。**
→ ★★**部屋を出て入り直しても戻りません。戻す方法は「ページを開き直す」だけです。**
→ **だから「2台とも閉じて開き直す」が効きます。**★**推測ではなく、ソースで裏が取れました。**

#### いま言えること / 言えないこと（線を引きます）
```
★言える: 一時的な拒否1回で、その待ち合わせ先はページの寿命ぶん失われる
★言える: 生きている本数は減る一方で、**開き直す以外に戻す手段はない**（ソースで確認）
★言える: 出会うには共通で生きている先が1本要る（実測）／0本なら永久に出会えない
★言える: いま banned なのは damus だけで、しかも一時的
⚠️言えない: **お子さんの「何日も前から繋がらない」の原因**。今日の banned は自分たちの試験が濃厚
⚠️言えない: 実機（Silk・LTE）で何が返っているか → ★2台ぶんの診断パネル待ち
```

#### ★見つかった別件（候補選びに効きます）
**`relay-rpi.edufeed.org` は「ephemeral な種類は受け付けない」**と返します。
**Trystero は ephemeral（kind 2xxxx）しか使いません。**
→ ★**この先は、つないだ瞬間に必ず切り捨てられます。**
⚠️ **これは Trystero の既定の一覧に入っています。**決めうちを外した端末は、1本を確実に無駄にしています。

#### 道具
`relay_probe.mjs` ⑤ … ★**告知を実際に1本出して、`banned` / `rate-limited` / `duplicate` / `CLOSED` を
Trystero と同じ区切りで分類**する。**入れ替え候補も同じ物差しで測る**。
★**自己確認つき**（自分の作った告知の id と署名が正しいかを先に検算する）。
⚠️ **繰り返し走らせないこと。**これ自体が出入り禁止のもとになります（今日それをやりました）。

### ★★★2026-09-20 夜 **原因が確定しました**：`relay.damus.io` に**出入り禁止にされていて、ライブラリが永久に切り捨てています**

**司令塔の指示でライブラリのソースを読みました。推測ではなく、実物のコードと実際の返事です。**
★**直しはまだ入れていません**（`index.html` は1バイトも触っていません）。

#### ★実物の返事（ブラウザのコンソールと、受けた通信そのもの）
```
relay.damus.io  4.6秒
  ["OK","f3285e38…",false,"banned: too many rate-limit violations, try again later"]
コンソール:
  Trystero: relay failure from wss://relay.damus.io/ - banned: too many rate-limit violations, try again later
```
★**「出入り禁止（banned）。rate-limit 違反が多すぎる」**と言われています。

#### ★ライブラリのコード（`@trystero-p2p/nostr@0.25.4` の元ソース。source map から取り出した）
```ts
const isTerminalRejection =
  msgType === 'CLOSED' ||
  (didRejectEvent && !isRateLimited && !isDuplicate)   // ← rate-limited: と duplicate: だけが例外

if (isTerminalRejection && !retireRelay(client)) { return }
```
```ts
const retiredRelays = new WeakSet<SocketClient>()      // ★「二度と使わない」印
const retireRelay = (client) => {
  retiredRelays.add(client)
  client.close?.()                                     // ★ライブラリ自身が接続を閉じる
}
```
★**`banned:` は `rate-limited:` でも `duplicate:` でもないので、terminal 扱いです。**
→ **その待ち合わせ先は、そのページを開いているあいだ、二度と使われません。**
→ `publishTopic` の先頭でも `if (retiredRelays.has(client)) return stopAnnouncing` と弾かれます。

#### ★★これで、ぜんぶ辻つまが合いました
```
1. アプリが6本につなぎ、部屋の告知を出す
2. damus が「banned」と返す（4〜5秒で）
3. ライブラリが damus を**永久に切り捨て、自分で接続を閉じる**
   → ★だから「切れたのに張り直さない」ように見えた。**張り直さないのではなく、切り捨てている**
4. 生きている本数は**減る一方**。**アプリを開き直したときだけ戻る**（印はページ単位）
5. 2台の生きている本数の**重なりが0になった瞬間から、開き直すまで永久に出会えない**
   （重なりが1本でもあれば出会えることは実測ずみ）
```
★**「2台とも同時に開くと繋がる」＝ 開きたてで切り捨てがゼロだから。**ユーザーの言うとおりでした。

#### ★前の記録との関係（どれが生き、どれが消えたか）
- ✔ **「damus が張り直されない」という観測は正しかった**。★**理由の読みだけが間違っていました**
- ✘ ~~張り直しの仕組みが無い~~ … **取り消し。仕組みはあるし、動く**（まねごとの相手では毎回つなぎ直す）
- ✔ **まねごとの待ち合わせ先で張り直したのは、こちらが `OK true` を返していたから。**
  **切り捨ての条件を踏んでいなかっただけ**でした。**食いちがいは解消です**

#### ★なぜ banned にされたのか（ここは推測を含みます。★印だけが実測）
★**実測: 張り直しに待ち時間がありません**（すぐ切られる相手に2秒おき＝毎分30回）。
★**実測: このPCからは、新しいページを開くたび4〜5秒で banned が返ります**＝**すでに出入り禁止**です。
⚠️ **いつ・何が原因で禁止されたかは分かりません。**「たぶんこれ」と書かないでおきます（1-6）。
⚠️ **禁止はこの家の回線（IP）に対してのはずです。**お子さんの端末も同じ回線なら、同じ扱いのはずです。

#### ★効きそうな直し（★指示待ち。まだ入れません）
1. ★**`relay.damus.io` を待ち合わせ先から外す。**いまは**毎回4〜5秒で死ぬ1本**を数に入れています
   ⚠️ ただし `RELAY_URLS` の上3本は「古い版の端末と出会うため」に選んであります。damus は4番目なので外しても断絶しません
2. **切り捨てられた本数を、画面の記録に出す**（いま「いま開いている N 本」だけでは、**なぜ減ったか**が分からない）
3. **待ち合わせ先を増やす／選び直す**。`relay_probe.mjs` に「**書き込みを受け付けるか**」の確認を足す
   （いまは「つながるか」しか見ていません。**つながっても banned なら役に立ちません**）
4. バックオフ … ★**いま触らない**（司令塔の判断。仕組みが分かってから一緒に設計する）

#### 道具に足したこと
`relay_traffic_probe.mjs` に **②c（EVENT 以外の返事を中身ごと）**と **②d（コンソールの警告）**を足しました。
★**これが無ければ、理由は永久に分かりませんでした。**「閉じた」だけ見ていても届きません。

### 🛑★2026-09-20 夜 **訂正**：「切れても張り直さない」は**誤りでした**（直しは入れていません）

**ひとつ前の記録（`f7b1f4c`）の結論を、自分で覆します。**
司令塔の指示で「わざと1本切って本数が戻るか」の検査を先に作ったところ、
**いまのコードで通ってしまいました。**＝ 私の見立てが違っていました。

#### ★何を間違えたか
**切り方が、実際の壊れ方と違っていました**（確認ポイント 4-1「きっかけになった実例そのものを仕込んで鳴らす」）。
```
私が最初にやった検査 … ★こちらから close() する  → 張り直す（通ってしまう）
実際に起きていたこと … ★相手（relay）から切られる → 張り直さない
```
そこで **`tools/mikaku/fake_relay.mjs`（自前のちいさな待ち合わせ先）**を書いて、
**向こうから切る**ところまで作り直しました（`ws` ライブラリが無いので、WebSocket の握手と枠組みを自前で実装）。

#### ★その結果わかったこと：**相手から切られても、ちゃんと張り直していました**
```
8秒ごとに切られる    → 毎回つなぎ直す（50秒で 6回）
0.3秒ごとに切られる  → ★40秒で 19回。**2秒おきに延々と**
切らない側を5本にして合計6本（＝本物と同じ形）→ ★それでも張り直す
```
**「一定数つながっていれば張り直さない」という線も、これで消えました。**

#### 🛑 ところが、**本物の relay では張り直していません**（食いちがいが残っています）
ページの中で閉じられ方まで記録しました（`relay_traffic_probe` の ②b）。
```
relay.damus.io   作った 2.7秒 / 開いた 3.7 / ★閉じた 5.4秒 code=1005 reason=""
→ ★そのあと145秒、**同じ先に2回目をつなぎに行っていない**（口すら作っていない）
```
**まねごとの待ち合わせ先（`ws://` 近所）では張り直すのに、本物（`wss://` 遠く）では張り直さない。**
★**この差は、まだ説明できていません。**close の番号（1005）は両方同じでした。

#### ★★だから、直しは入れませんでした（ここが今回いちばん大事です）
**「切れたら張り直す」を足す案は、いまのところ根拠がありません。**
**アプリは、私が作れるどの条件でも、すでに張り直しています。**
**分かっていない壊れ方に対して直しを入れても、効いたかどうかを測れません**（0-1・4-1）。
→ **司令塔とユーザーの判断を仰ぎます。**

#### ★ただし、別の心配が1つ見つかりました（こちらは実測ずみ）
**張り直しに待ち時間（バックオフ）がありません。**
**すぐ切られる相手には、2秒おきに延々とつなぎ直します**（40秒で19回＝毎分30回）。
★**relay に嫌われて切られる側の原因になりえます。**
**実機の Silk が「10本試して4本しか保てない」のと、向きが合っています。**
→ **これは直す価値があると思いますが、指示を待ちます。**

#### いまの正しい理解（前の記録のうち、生きているもの）
- ✔ **遅れは原因ではない**（0/2/5/10/30秒すべて出会える。回線を絞っても同じ）
- ✔ **告知は止まらない**（最初の3秒に4回 → 以後60秒ごと）
- ✔ **出会えるかは「購読が生きているか」で決まる**（入る側の告知を、その場で受け取る形）
- ✔ **重なりは1本あれば足りる。0本なら永久に出会えない**
- ✘ ~~切れても張り直さない~~ … **取り消し。張り直しています**
- 🛑 **本物の damus だけ張り直さない理由は、未解明**

#### 道具（`tools/mikaku/`）
| 道具 | 何を見るか |
|---|---|
| `fake_relay.mjs` | ★中身を決められる待ち合わせ先。**向こうから切る**／何回つなぎに来たかを数える |
| `relay_serverclose_probe.mjs` | ★**相手から切られたあと張り直すか。**`--keeps N` で本数を変えられる |
| `relay_reconnect_probe.mjs` | こちらから切ったとき。⚠️ **実際の壊れ方とは違う**と先頭に明記した |
| `relay_traffic_probe.mjs` | ②b に**閉じられ方（code/reason）と、同じ先に2回以上行ったか**を足した |

⚠️ `relay_serverclose_probe` の「画面の“いま開いている”= -1」は、
**`diagWsOpenNow` がページの外から呼べないため**です。**通信は受け側で数えているので判定には影響しません。**

#### 次（★指示待ち。`index.html` は1バイトも触っていません）
1. **バックオフを入れるか**（実測ずみの問題。ただし症状が治る保証は無い）
2. **本物の damus だけ張り直さない理由**を追うか（`wss://` と `ws://` の差から）
3. ★**2台ぶんの診断パネル**が来れば、重なりの話は確定できます。そちらが先だと思います

### ★★2026-09-20 夜 「つながらない」の切り分け：**待ち合わせ先を、切れても張り直していない**（コミットずみ・push 前）

**司令塔の依頼（①遅れの境目を出す ②告知の間隔を実測 ③relay の出入りを時間で追う）に対する回答です。**
★**直しはまだ1行も入れていません**（`index.html` は触っていない）。落ちる条件を先に押さえる段です。

#### ★まず、司令塔の読み2つは**どちらも外れ**でした（実測）
| 疑い | 実測の結果 |
|---|---|
| **① 遅れて入ると出会えない** | ✘ **0/2/5/10/30秒、全部 2.5秒前後で出会える。**回線を絞った側（往復400ms・400kbps）でも 0/2/5/15秒すべて6秒台で出会える |
| **② 告知が途中で止まる** | ✘ **止まっていない。**最初の3秒に4回 → **以後ぴったり60秒ごと**に出し続ける（186秒まで確認） |

⚠️ **私の前回の報告「2秒で出会えた」が、うまくいく側しか試していなかったのは司令塔の指摘どおりです。**
**遅れを足す検査を作りました**（`meet_probe.mjs --delay`）。**その結果、遅れは原因ではないと分かりました。**

#### ★出会える仕組みが分かりました（ここが効きます）
**告知が届くかではなく、「部屋を作った側の購読が生きているか」で決まります。**
```
部屋を作った側: 6本すべてに REQ（購読）を出し、CLOSE を一度も送らない＝開きっぱなし
入る側        : 入った瞬間に自分の告知（EVENT）を出す
→ 購読している側が、その場で受け取る。**だから遅れは関係ない**
```
**必要なのは「2台が同じ待ち合わせ先に**同時に**生きて繋がっていること」だけです。**

#### ★★そして、これが原因だと思われます: **切れたら、二度と張り直さない**
```
測った3回ぶん:
  120秒 … 6本つないで 0本切れた（延べ6）
  200秒 … relay.damus.io が 186.6秒に切れた → ★130秒後も延べ6のまま＝張り直していない
  130秒 … relay.damus.io が ★4.9秒で切れた → ★そのあと125秒、延べ6のまま＝張り直していない
```
★**「いま開いている本数」は減るだけで、増えることが一度もありません。**
**アプリを開き直したときだけ、6本に戻ります。**

#### ★重なりは1本あれば足りる（実測）
`meet_probe.mjs --overlap` を作って、2台に配る待ち合わせ先を変えて測りました。
```
全部おなじ（6本重なる）… ○ 3.2秒     半分（2本重なる）… ○ 3.1秒
★1本だけ重なる       … ○ 2.7秒     ★重なり0            … ✘ 25秒たってもだめ
```
**＝1本でも共通で生きていれば出会えるし、0本なら絶対に出会えません。**

#### ★これで実機の症状がきれいに説明できます
```
同時に開く   → 両方とも**開きたて＝最大の本数**。重なりがあるのは当たり前 → 繋がる
時間がたつ   → 両方の生きている本数が**減る一方**（張り直さないので）
             → 重なりが 0 になった瞬間から、**開き直すまで永久に繋がらない**
実機の Silk は「10本試して4本しか保てない」＝**最初から本数が少なく、0になりやすい**
```
⚠️ **ただし「2秒ずれるとだめ」という秒数そのものは、まだ説明できていません。**
**自宅PCでは30秒遅れても繋がるので、症状の再現はできていません。**そこは正直に申告します。

#### ★試したが空振りだったもの（記録として）
**回線を10秒切って戻す**（`relay_traffic_probe.mjs --blip`）→ **本数は5本のまま変わらず。**
Chrome の「オフライン」エミュレーションは、**すでに開いている WebSocket を落としません**でした。
→ **この道具では「電波が途切れたとき」を再現できていません。**実機でないと分からない部分です。

#### 新しい道具（`tools/mikaku/`）
| 道具 | 何を見るか |
|---|---|
| `meet_probe.mjs --delay 0,2,5,10,30` | ★遅れて入ったときに出会えるか。`--slow-guest` で入る側の回線を絞れる |
| `meet_probe.mjs --overlap` | ★2台の待ち合わせ先が一部しか重ならないとき出会えるか。**重なり0で必ず落ちる**＝鳴る検査 |
| `relay_traffic_probe.mjs` | ★**本物のWebSocketの中身**を記録。接続・切断の時刻、告知の間隔、購読の有無 |

★どちらも**基準の両側**を出すようにしてあります（4-3）。
「全部つながった」で終わらず、**落ちるはずの条件で落ちること**を毎回いっしょに表示します。

#### ★ユーザーに確かめてほしいこと（司令塔へ上げます）
1. **繋がらなくなったとき、そのまま60〜90秒待つと繋がるか**（告知は60秒ごとに出ています）
2. ★**繋がらないときに、2台ぶんの診断パネルの「待ち合わせ先」の行**（○が付いているもの）。
   **2台に共通の○が1本もなければ、この読みで確定します**
3. **片方だけアプリを開き直すと繋がるか**（本数が戻るはず）

#### 次（★指示待ち。まだ直しません）
効きそうな直しは「**切れた待ち合わせ先を張り直す**」。ただし**落ちる条件を再現できていないので、
直しても効いたか測れません。**上の2番（2台ぶんの診断パネル）が取れてからにしたいです。

### ★2026-09-20 夜 3回目の公開：理科 第3回＋第4回の取り込み（38件）★**公開ずみ**（`7262507`）

#### ★公開を実物で確かめた（D-9。司令塔の報告を写しただけにしない）
`acfcf5f..7262507` を push。**公開サイトを取ってきて中身を数えた**:
```
★公開サイトの data.js と手元の data.js が**バイト単位で完全一致**（改行をそろえて sha256 比較）
総数 1635問   r3m 89問 / r4m 79問
r3m で短い note を持つ  13件     r4m で img を持つ  10件
r3m24 note = 「演習問題集、20P、まとめ⑪-赤色リトマス紙(酸性)」 ＝答えの露出が消えている
r4m67 note = 「演習問題集、32P、練習問題2 問4（2026-09-20 図を添付: r4_02.jpg）」
画像3枚とも HTTP 200（91738 / 83295 / 84898 bytes）
```
⚠️ **push 直後の1回目は前の版が返ってきました**（GitHub Pages の反映待ち。ビルドは `built`）。
**「一致しない＝失敗」と決めつけず、少し置いてから取り直すこと。**

#### 結果
```
問数 1635 → 1635      ★増えた id 0 ／ 消えた id 0（C-5 は守られた）
中身が変わった id 38件   g3r113 1 / g4r116 1 / r3m 15 / r4m 21
変わった欄（延べ）      note 31 / q 19 / img 11    ★答え(a) が変わった問は無い
```

#### ★司令塔の見込み（35件）と食いちがったので、いったん止めて報告した
**これが今回いちばん効いた判断です。**司令塔も独立に数え直して 38 で一致し、見込みのほうが誤りと決まりました。
```
r3m が 13 ではなく 15 … 第3回は今日 a40e7962 → a05024a9 → 3041afb4 → f368d4c8 と4回変わっており、
                        司令塔は3回目を飛ばして足し算していた（no.109・119 の2件を数え落とし）
r4m が 20 ではなく 21 … 第4回を当てたときに r4m73 が新たに差分に入った（r4m67 は元から差分内）
```
★**理科担当の「10件」も正しい数でした。**`3041afb4 → f368d4c8` の差が10件で、こちらは
**公開ずみ（a40e7962）からの差**を見ていたため。**食いちがいではなく、基準の違い**でした。
→ 理科担当は「次から『対 data.js で何件か』も添える」とのこと。

#### ★★理科の id は `no` ではない（今回いちばん危なかったところ。`ROLE.md` に書いた）
```
id番号 == no        → 89問中 70問しか当たらない
id番号 == memo連番  → 89問中 76問
★正しい規則: id番号 = no −（それより前にある calc の数）… 89/89 成立
```
`no` を id だと思って直すと **19問が別の問題に当たります**（`stats[id]` が付けかわる）。
→ **問題文（q）で対応づけ**、**答えと img の一致**を証拠にし、**上の規則を独立の検算**として通した。
　 道具 `tools/patch_r3_notes.py`（自己テスト7件を入口に置いた・4-6）。理科担当も自分の元データで検算し、成立を確認ずみ。

#### ★道具の検査を1つ作り直した（`patch_r4_figures.py`）
もとは「**img が外れる差分**が申告11件と一致するか」を見ていた。
**data.js に前回ぶんが既に当たっていると、この差分は 0件になる**＝**状態しだいで意味が変わる検査**だった（4-1）。
→ **最終状態**（img を持つ id が申告の10件と一致するか）で見る形に変え、**書いたあと読み直して**も確かめるようにした。

#### 照合と検査（全部通過）
```
sha256（バイナリで実測・4ファイルとも宣言値と一致）
  理科 第3回 f368d4c8… 80568B 107問（memo 89/calc 18）   理科 第4回 f5d02d74… 47288B 79問
  社会 第3回 8d8e77ed… 113問                             社会 第4回 c07d6fb5… 121問
照合  理科第3回 差分0 ／ 理科第4回 差分0 ／ 社会第3回 0問 ／ 社会第4回 0問
検査  verify_data 問題なし ／ smoke 22件 問題0 ／ list_meta_filter 21/0 ／ migration 22/0
```

#### ★実機で図つき10問を見た（B-12。前回の「図が飾り」の反省をふまえて）
`r4m43 r4m44 r4m46 r4m47 r4m48 r4m49 r4m65 r4m66 r4m67 r4m75` → **10/10 出題でき、JSエラー0**。
- ★**10問とも、図でしか分からない目印を指している**（①②④⑤⑧⑨／A・M管・N管／ア〜ク）
  ＝**図が無いと答えが決まらない。**前回の「図が飾り」は解消されている
- ★**3枚の図を目で見た**（`r4_01` ①〜⑨ ／ `r4_02` A・M・N ／ `r4_03` ア〜ク）。**記号はすべて読める**
- ★**図に答えの語は1つも印刷されていない**（A-2b）。**裏写りの文字を明るさを持ち上げて拡張しても判読不能**
- ⚠️ **`r4_03` は元画像そのものが下で切れていて、`（図1）` の下半分が欠けている。**
  **アプリの表示不具合ではない。**ア〜クは全部読めるので今回はこのまま。**切り直すなら元PDFから**

#### 次にやること
- 司令塔へ結果報告（公開の実物確認つき）
- ★**Silk の診断パネルの控え**待ちは、そのまま継続（「つながらない」の原因はそこで決まる）
- 次回送り: 理科第3回の「画像15件あるのに `【図】` タグ0件」／「なぜですか型」6問／社会の検査の再走査

### ✅ 2026-09-20 夜 ★解消ずみ：ここに書いてあった未コミットの取り込みは、下の「3回目の公開」で公開しました

（以下は当時の記録。**もう待っていません。**）

**もしこのセッションが落ちたら、次の人はまずここを読んでください。**

#### 何が起きているか
3ファイルを取り込んだ**直後**に、司令塔から「理科 第4回は止めて。sha256 が変わる」と連絡が来ました。
ご指示は「**巻き戻さず、そのまま報告**」だったので、**作業ツリーに置いたまま、コミットしていません。**

```
data.js に適用ずみ（未コミット・未公開）:
  理科 第4回 1f8f0744…  20問を書きかえ／img が 11問から外れた（画像つき 21→10）
  社会 第3回 8d8e77ed…  g3r113 の note だけ
  社会 第4回 c07d6fb5…  g4r116 の note だけ
照合: 79/113/121問とも不一致0件・総数1635問・id重複0
テスト: verify_data・smoke 22・list_meta_filter 21・年表の赤丸 すべて通過
```
**公開はしていません。**お子さんの端末には届いていません。

#### 次に何をすればよいか
1. **理科 第4回の新しい sha256 を待つ**（`r4m67`・`r4m73` に別の露出が見つかったため作り直し中）
2. 届いたら `tools/patch_r4_figures.py` の **`SHA` と `DECLARED_IMG_REMOVED` を書きかえて `--apply`**
   ★**やり直しになりません。**この道具は**元データと data.js の差だけ**を直すので、**2件だけが差分**として出ます
3. **理科 第3回**（`a05024a9…` 107問）も**1〜2件追加になる見込み**。★**理科の2ファイルはそろってから**
4. そろったら commit → 司令塔経由で承認 → push

#### ★自分の検査の穴を1つ申告（道具にも書き足した）
`tools/check_answer_in_note.py` は **`r4m67` の露出を拾えませんでした。**
```
r4m66 答え「毛細血管・リンパ管」／ r4m67 の答えがそれを含んでいる
→ ★経路が「出典」ではなく「答え」。A-1 の型で、この道具の守備範囲の外
```
→ 道具の先頭に「**別の問の答えの露出（A-1）は見ていない／問題文も見ていない**」と明記した。
　 **「4件だから他は大丈夫」と読まれないようにするため。**

#### 今回の取り込みで確かめたこと（作り直しても消えない事実）
- ★**図が必要な10問は、すべて図でしか分からない目印を指している**（①②④⑤⑧⑨／A・M管・N管／ア〜ク）。
  **前回の「図が飾り」は解消**。外した11問は図なしで解ける形（例「胃液にふくまれる塩酸で…」→ 胃）
- 実機で `r4m43` を開き、**図に ①〜⑨ が出て読めること**を確認
- ★**`img` が外れる11問は、司令塔の名指しと実測が完全一致**（道具が突き合わせて、違えば止まる作りにした）

### 2026-09-20 20:48 担当を引きついだ（新セッション。前任は ListAgents に不在）

- 着任時の `ListAgents` は **`あなたは司令塔です [3fb91b]` の1件だけ**＝**対戦アプリ担当は他にいません**（前任は不在）。名簿を更新した
- ★**司令塔も入れかわっています**（前任の名簿では `[efdd7d]`）。**連絡前に必ず `ListAgents` で確認すること**
- ⚠️ 前任と同じく、**このセッションの `ListAgents` は 1行目の `This session is …` を返さない**ため、自分の ref は不明
- 実測（`git fetch origin master` 済み・0-4）:
  - `origin/master` = `8252353`、ローカル `master` = **ahead 3**（`1c84c0c` `80448ac` `efabb4e`）。**中身は `作業メモ.md` と `ROLE.md` だけ**
  - 🛑 **`data.js` は未コミットのまま**（`git diff --stat HEAD` = 33挿入/44削除）。上の🛑の記述と一致。**巻き戻していません**
  - ほかの未コミットは `tools/mikaku/shots*` などのスクリーンショットのみ。「いま作業中」欄は空＝競合なし
- **引きつぎ（理科 第4回の新 sha256 待ち／理科 第3回も待ち／Silk の診断パネルの控え待ち／TURN の替えはその後）は、そのまま引きつぎます。**返事は来ていません
- CLAUDE.md 0c に従い、**名指しの指示が来るまで着手しません**

### ★2026-09-20 夜 いま待っていること（3回目の公開ぶん）

**待機中。指示があるまで着手しません**（0c）。司令塔から「2つそろったら声をかける」と言われています。

| 待っているもの | 状態 | 私がやること |
|---|---|---|
| **理科 第4回の作り直し** | 理科担当が作業中 | 届いたら取り込む。**図つき 21→11問前後**になる見込み |
| **社会の note 直し2件** | **納品ずみ**（下記 sha256） | 理科とそろったら一緒に |
| 未 push `1c84c0c` | 作業メモの切り出し（文章のみ） | 3回目の公開に同梱 |

#### ★理科が作り直しになった理由（今日入れたぶんの話・記録として残す）
ユーザーが実機を見て「**図が飾りになっている**」と指摘。
```
[消化器官の模式図 ①〜⑨] 【図】消化の仕上げをし、養分の吸収を行う消化器官はどこですか。 → 小腸
```
**図を見なくても解ける。**前任が図を文章に置きかえた版に、**図だけ戻して問いを戻していなかった**ため。
→ ★**私の実機確認は「図が出るか・記号が読めるか」までで、「図を見ないと解けないか」は見ていませんでした。**
　 `r4m65`・`r4m75` の2問（司令塔が名指しした分）はそこまで見ましたが、**残り19問は見ていません。**
　 **B-12 の「その画面で解けるか」には、「その図が要るか」までは含まれていなかった。**次から見る。
→ **画像 `r4_01`〜`r4_03` はそのまま使える。**変わるのは `q` と、図を外す問の `img` だけ。

#### 取り込むときの注意（届いたとき用）
- ★**改行コードが回ごとに違う**: 第3回 CRLF ／ 第4回 LF。**読み書きは `newline=''`、sha256 はバイナリで**
- 社会の納品（`note` 1件ずつだけ・`q`・`a`・`no` は不動）
```
第3回_奈良時代.json  8d8e77ed6c36b5fac0ef0341c6b5c88d27a2764abaee61ed839e497a42ccda50（113問）
第4回_平安時代.json  c07d6fb5e852deb99b8eca06bba6037b1740524ea71b9c2fc89429fab40e61eb（121問）
```
- 理科は **`import_rika.py` を使わない**（id がずれる）。`tools/patch_r4_figures.py` の形で id 指定で直す
- ★**`img` が外れる問が出る。**外した画像は `sync_img_sizes.py` が「参照なし」と報告するが、**消さない**（C-3）

#### 司令塔が実測で答えてくれた2件（こちらの問いへの回答・決着ずみ）
- **`figureNote` は画面に出ない**（`index.html`・`data.js` とも0件）→ **消さない**
- **`sol` は答えの下に罫線つきで出る＝答えと同時**→ **正常。直さない**

### ★2026-09-20 2回目の公開ぶん：赤丸24個 ＋ 理科 第4回の図つき21問（`19721f3`・`58b4994`・**公開ずみ**）

#### ★公開を実物で確かめた（D-9）
`0e33d70..acfcf5f` を push。**公開サイトを取ってきて数えた**:
```
TIMELINE_MARKS の g4 … 20問 / 24矩形     IMG_SIZES の r4_ … 3件そろっている
data.js の r4m で画像つき … 21問         総数 1635問
画像3枚とも HTTP 200（91738 / 83295 / 84898 bytes）
```

#### ① 社会 第4回の赤丸24個（`TIMELINE_MARKS`。data.js には触っていない）
受け取った座標を**こちらでも独立に確かめた**（D-9）:
- 20問・24矩形／0〜1 の外なし／2矩形は ⑥⑨⑩⑲（申告と一致）
- 狭い枠は g4r70(①)・g4r84(⑮) の2つだけ＝**答えが西暦年の2問**と一致（独立した検算）
- ★**24矩形を絵に描き戻し、1つずつ切り出して丸数字と id の対応を目で見た**（24/24 一致）
- ★全体図でも「すべての空らんに枠があり、余計な枠が無い」ことを見た
- ★実機: 20問すべて**赤丸の数が座標表と一致**。**拡大（2.56倍）してもずれない**

#### ② 理科 第4回の図つき21問
- ★**`import_rika.py` は使っていない**（memo だけの並びで id を振り直すため。欠番が詰まって id がずれる）。
  代わりに `tools/patch_r4_figures.py` を書き、**id ごとに `q`・`note`・`img` だけ**を直した
- ★sha256 は**バイナリで読んで**照合（CRLF のため。テキストだと合わない）
- 79問すべて照合 → 不一致0件。総数 1635問・id重複0
- ★**図が無いと答えが決まらない2問**（r4m65・r4m75）を実機で見た → **図が出て記号が読める**

#### ★★見つけた別件（今回の変更が原因ではない。司令塔へ上げた）
**`note`（出典）は、答えを見る前から画面に出ています。**そこに答えの語が入っている問が **7件**。
```
g3r113(遣隋使) r3m24/r3m27(変化しない) r3m102(重ソウ) r4m65(柔毛) g4r116(藤原元命)
r3m80 は「の結晶」で、これは言いすぎ（誤検出）
```
**6件とも今日より前から公開ずみ**（r4m65 も 09-19 から）。**今回の取り込みで増えてはいません。**
→ 道具 `tools/check_answer_in_note.py` を残した。**データの中身は作成担当の持ち場なので、勝手に直さない。**

#### 道具の直し（`g4_timeline_probe.mjs`）
★**出題順を固定するようにした。**既定はシャッフルで、**先の id が自分より前に出ると、進んでも二度と届かない**。
3問目で落ち続けたのは**道具の側の問題**で、アプリは正しかった（C-8b の型）。

### ★2026-09-20 社会 第4回の演習年表20問を取り込んだ（`2fd83a6`・**push 前**）

ユーザー判断で**赤丸（`TIMELINE_MARKS`）を待たずに先に公開**する方針。司令塔が納品を宣言（sha256 一致）。

#### 結果
- `g4r70`〜`89` の 20問を上書き更新。**id は1つも動いていない**。総数 1635問・id重複0
- `images/kai4_01.jpg`（1189×1839）を追加。`IMG_SIZES` は **`sync_img_sizes.py` で実物から**入れた
- **121問すべて元データと照合 → 不一致0件**（★C-6 の書き戻しは**不要**。今回は元データ→data.js の一方向で、
  data.js 側で直した箇所は無く、照合で完全一致を確かめてある）

#### ★画面で解けるかを先に確かめた（B-12。今回いちばん大事なところ）
書き換え前は文章だけで解けたが、いまは **絵が無いと20問すべて解答不能**になる形。
- ★実機（390px・画素2倍）で **絵が出て、①〜⑳ が読める**（枠 312×483。押すと拡大でき、2.56倍＝幅919px）
- ★**20問すべて、実際の絵の空らんと答えが合っている**（絵を見て1つずつ確かめた。①=794 … ⑳=宋）
- ★**絵は答えを見せていない**（番号の箱はすべて空・A-2b）
- 聞いている番号は ①〜⑳ がそろっている（抜け・ダブり無し）

#### 新しい道具 `tools/mikaku/g4_timeline_probe.mjs`
絵が出るか・押して拡大できるか・拡大で実際に大きくなるかを見て、写真に残す。
★**「番号が読めるか」は判定しません**（機械では決められないので、人が写真を見る）と道具に明記した。

#### 赤丸はあと
座標が届いたら次の公開で足す。**データに触らない**ので安全（画像つき479問のうち赤丸持ちは50問だけ）。

### 2026-09-20 第4回の取り込み：届く前の下見（**データには触っていません**）

**納品前の JSON は読んでいません**（C-4c-2）。実物を見たのは画像と data.js と index.html だけです。

#### 実測した数字
| 見たもの | 実測 |
|---|---|
| `画像プレビュー\kai4_01.jpg` | **1189×1839**（司令塔の言う `[1189,1839]` と一致） |
| ★`画像プレビュー\kai4_01_master600.jpg` | **1390×2150**（**別サイズの同じ絵が並んで置いてある**） |
| `images\` の kai4 | **0件**（まだコピーされていない） |
| data.js の `g4r70`〜`89` | **20件すべて在る**（第4回は121問）。いま `file` 欄は**無い**。`a` は「（794）年です。」など |
| `TIMELINE_MARKS` の g4 | 0件 ／ `IMG_SIZES` の kai4 | 0件 |
| `IMG_SIZES` の入れる場所 | `"kai3_07.jpg"` の直後、`"kaki_r1_01.jpg"` の直前 |

#### ★★座標の受け取りで、いちばん危ないところ（社会担当に確かめること）
**同じ絵が2つ、ちがう大きさで置いてあります**（1189×1839 と 1390×2150）。**縦横比は同じ**です。
```
座標が「割合（0〜1）」で来る → ★どちらの絵で測っても同じ。そのまま使える
座標が「px」で来る          → ★どちらの絵で測ったかで、1.169倍ずれる
```
**`TIMELINE_MARKS` は割合で持ちます**（`index.html` の `drawMarks` が naturalWidth 基準で描くため）。
→ **納品時に「単位（割合か px か）」と「px なら測った絵の大きさ」を書いてもらうこと。**
　 書いていなければ、取り込む前に必ず聞く。**ここを取り違えると、赤丸が全部ずれた位置に乗ります。**

#### ★`TIMELINE_MARKS` は後から足せる（司令塔の問いに、実物で答えた）
**足せます。データには触りません。**根拠は読んだだけでなく、いま動いているものが証拠です。
```
画像つきの問 479件 のうち、赤丸を持つのは 50件だけ。
★残り 429件は、赤丸なしで今まさに配信されている（simg1・g1r41 など）
```
- `drawMarks` は `TIMELINE_MARKS[qid]` が無ければ**その場で戻る**（`index.html`）。絵はふつうに出る
- `TIMELINE_MARKS` は **`index.html` の中にあり、id を鍵にした表**。data.js とは独立
→ **画像・`q`・`img`・`IMG_SIZES` を先に出し、赤丸だけ後から足す**のは安全です。
⚠️ ただし**絵を切り直したら座標は無効**になるので、**赤丸を足すのは絵を確定させたあと**。

#### ★申し送り（司令塔から・2026-09-20）
`kai4_01_master600.jpg` は **1390×2150** で、**第3回の master600（2851×4800）ほどの原寸控えではありません。**
agy に渡した切り出しがその大きさだったため。**いまは作り直しません**（配信用 1189 幅は第3回と同じで実績があり、
絵を切り直すと座標が全部無効になるため）。
→ **将来もっと大きい控えが要るなら `社会演習.pdf` 31P から取り直す。そのときは座標も測り直し。**

#### ★訂正（私の言い方の誤り）
先に「いま `file` 欄は無い」と書きましたが、**data.js 側の鍵は `img` です**（`file` は元データJSON側の名前）。
中身の話は変わりません（**第4回 121問に画像つきは0件**）。取り込みのときに取り違えないこと。

#### 届いたらやる順番
1. **`sha256` を照合してから**元データを読む（C-4c-2）
2. 画像を `images\` へコピー（★差し替えではなく新規。旧画像の削除は無し・C-3）
3. ★**`IMG_SIZES` は手で書かず `python tools/sync_img_sizes.py` で実物から入れる**（C-4b。
   「宣言された数字」ではなく「実物の寸法」が入るので、取り違えが起きない）
4. `import_g.py` で `no` を id の番号として取り込む（★通常回に `id` 欄は無い・C-6b）
5. `TIMELINE_MARKS` に `g4r70`〜`89` を追加（**24矩形。⑥⑨⑩⑲ は2か所ずつ**）
6. 元データへ書き戻し（C-6）＋ 自前照合
7. ★**実機で見る**: `node tools/mikaku/mikaku_check2.mjs g4r70 … g4r89`（引数に id を並べる作り）。
   ⚠️ **年表は縦長で、390px 幅だと赤丸が小さい。**小さい写真で「乗っている」と判断しないこと。
   **拡大したときのずれは、拡大しないと出ません**（`drawMarks` のコメントに前例あり）。`zoom_probe.mjs` も使う

### ★2026-09-20 つながらない件：記録の直しと待ち合わせ場所の決めうち（`11fa015`・**公開ずみ**）

#### ★公開を実物で確かめた（司令塔の報告を写しただけにしない・D-9／B-12）
`8252353..698d4b3` を push。**公開サイトを取ってきて中身を数えた**:
```
版 DIAG_VERSION = "2026-09-20b"      ／ relayConfig の指定 1件
「相手を見つけたかを先に見る」はしご 1件 ／ いま開いている本数を見る箇所 3件
待ち合わせ先の行 1件
配られている待ち合わせ先 = 決めうちの6本ちょうど
  nostr.sathoarder.com / nostr-01.yakihonne.com / top.testrelay.top /
  relay.damus.io / nos.lol / relay.primal.net
`relayUrls:` の残り1件は**説明文の中だけ**（効かない書き方だと注意書きしている行）
```

⚠️ **`ROLE.md` の追記（`cbb2e75`）は未 push。**承認は `11fa015`＋`698d4b3` に対して出たもので、
そのあとに作った分なので、**次の push にまとめる**（勝手に足さない）。


司令塔の依頼2通（16:40 と 16:51 で優先順位が入れかわった）に対応。**コードはローカルにコミットずみ、push は承認待ち。**

#### ★★まず、依頼書の指定を1つ訂正しました（実物で確かめた）
> 依頼書は「`ROOM_CONFIG` に **`relayUrls`** を足す」でしたが、**trystero 0.25.4 では効きません。**
> 正しくは **`relayConfig: { urls: [...] }`**。`relayUrls` は**黙って無視**され、エラーも出ません。

`tools/mikaku/relay_probe.mjs` ③で**両方の書き方をためして**確かめています。
**09-18 の TURN（書いてあったのに一度も効いていなかった）と、まったく同じ型でした。**

#### 直したこと（`index.html`。版 `DIAG_VERSION` は `2026-09-20b`）
1. **判定の順番を入れかえた**（司令塔の新しい優先1）
   もとは待ち合わせの本数を先に見ていたため、**「相手を見つけた: 1回」と書いてある同じ紙に
   「探しに行けていません」**と出ていた。**相手を見つけたかを先に見る。**
2. **待ち合わせの数え方**を「新しく作られた本数」→「**いま開いている本数**」に。
   ライブラリは接続を appId ごとに**使い回す**ので、2回目は 0 本になっていた
3. **★つないだ待ち合わせ先の名前を出した**（2台ぶん並べれば、同じ場所にいるか一目で分かる）
4. **道づくりを通信路ごとに分けた**（司令塔の新しい優先2）。
   ライブラリは相手用のほかに**使い捨ての通信路**も作る。混ぜていたので `disconnected` が誰の話か読めなかった
5. **待ち合わせ場所を6本に決めうち**（`RELAY_URLS`）。★うち3本は**既定でも選ばれる先**にしてある。
   こうしないと、**まだ古い版を開いている端末と出会えなくなる**

#### ★実測で分かったこと（司令塔の読みとの差）
| | 司令塔の読み | 私の実測 |
|---|---|---|
| 2台は別の relay を選んでいる？ | 本命の仮説 | **選び方は appId で決まり、2台とも同じ5本**（別ページで2回引いて一致） |
| 計測がライブラリの接続を捉えていない？ | 「一度も捉えていない可能性」 | **PCでは6本すべて捉えられた**（`meet_probe` ④）。捉え損ねではなく**使い回し**で説明がつく |
| 既定の5本は使えるか | — | ★**生きていたのは3本だけ。**relay は知らせを断ると**ライブラリ側から接続が閉じられる** |

→ 「切れた7 / 失敗6」という Silk の記録は、この**閉じられ方**と一致する。

#### ★検査の穴を1つ塞いだ
**これまでの「2人ぶん」の検査は全部スタブで、待ち合わせは一度も試されていなかった。**
`test_battle_start` は本物を読むが**部屋を作るところまで**。
→ `tools/mikaku/meet_probe.mjs` を作った。**本物の relay ごしに2つの画面が実際に出会えるか**を見る。
　 実測: 決めうち・既定まかせ **どちらも2秒で出会える**／片方を別の場所にすると**出会えない**（検査が鳴る）

#### 実測（全部通過）
smoke 22 ／ resume 25 ／ start 17（本物の trystero）／ diag_panel（⑤⑥を追加）／ meet_probe ／ relay_probe

#### 次
- ★**社会・理科 第4回の取り込みが最優先**（司令塔 16:55。ユーザー「そろそろ使いたい」）。届き次第そちらへ
- 中継（TURN）の選択肢と費用の整理 → 司令塔へ上げる（**ユーザー判断**。自分で決めて入れない）
- ⚠️ **この版が届くまで、実機の記録は読み方が変わりません。**push 後にもう一度取ってもらうのが有効

### 2026-09-20 担当を引きついだ（新セッション。前任は ListAgents に不在）
- 着任時の `ListAgents` は **`あなたは司令塔です [efdd7d]` の1件だけ**。**対戦アプリ担当は他にいません**＝前任は不在。名簿を更新した
- ⚠️ 前任と同じく、**このセッションの `ListAgents` は 1行目の `This session is …` を返さない**ため、自分の ref は不明
- 実測（`git fetch origin master` 済み）:
  - `origin/master` = `8252353`、ローカル `master` = **`2e8948a`（ahead 1）**。中身は **`作業メモ.md` だけ**でコードは1バイトも違わない
  - 未コミットは `tools/mikaku/shots*` などのスクリーンショットのみ。「いま作業中」欄は空＝競合なし
- **下の引きつぎ（Silk の診断パネルの控え待ち／中継の替えはその後）は、そのまま引きつぎます。**返事は来ていません
- CLAUDE.md 0c に従い、**名指しの指示が来るまで着手しません**

### ★2026-09-20 「つながらない」の調査（司令塔 claude-33 の依頼）— 実装前の報告まで
依頼は2通。①「LTEとWi-Fiだとつながらない」→ ②途中で「**Chrome どうしはつながる。Silk だとつながらない**」に更新。
**司令塔の判断待ち。実装はまだ1行もしていません**（`index.html` は触っていない）。

#### ★★① Open Relay（TURN）は死んでいる。09-18 の `0e3527a` は**配信した日から効いていなかった**
**実機の Chrome で ICE 候補を実際に集めて確認**（新しい道具 `tools/mikaku/ice_probe.mjs`）。
```
① index.html の設定そのまま → 候補3件 {host:2, srflx:1}  ★relay 0件
     error 701 turn:openrelay.metered.ca:80  — TURN host lookup received error
     error 400 turn:openrelay.metered.ca:443 — TURN allocate error
     error 701 turns:openrelay.metered.ca:443?transport=tcp — Failed to establish connection
② TURN 1本ずつ（中継だけ使う設定）→ 生きている TURN 0 / 3
③ ★この検査が鳴るかの自己確認 → でたらめな TURN で relay 0件。**検査は鳴る**（D-17）
```
裏づけ2つ:
- `nslookup openrelay.metered.ca` → **名前が引けない**（`metered.ca` 本体は HTTP 200 で生きている）。**ホスト名ごと消えている**
- metered は**APIキー必須に変わった**（無料枠は残るが、合いことばを固定で埋め込む使い方は終了）

⚠️ **「Chrome どうしはつながる」と矛盾しません。**あれは**直接つながった**ので中継を使っていません。
**中継が要る回線（LTE・きびしいNAT）だけが、静かに落ちていました。**

#### ② `ROOM_CONFIG` は効いている（キー名も合っている）
**当て推量ではなく、配信で読み込んでいる実物のバンドルを取ってきて読んだ**（`@trystero-p2p/core@0.25.4`）。
```js
initPeer: En …… e.initPeer(false, e.config)   // e.config = joinRoom に渡した設定そのもの
En = (_, {rtcConfig: t, turnConfig: f}) => new RTCPeerConnection({iceServers: 既定.concat(f ?? []), ...t})
```
→ **`rtcConfig` は正しく届いている。**ただし `...rtcConfig` が後ろなので、**`rtcConfig.iceServers` を書くと既定も `turnConfig` も丸ごと上書き**される。
　 いまは既定の STUN 2本を自分で書いているので実害なし。**将来 `turnConfig` を併用すると黙って消えるので注意。**

#### ③ ★使っていない切り分け道具が、ライブラリ側にすでに在った
`joinRoom(config, roomId, **第3引数**)` を受け取る作りで、**いまは第3引数を渡していない**。
```
onJoinError      … 「SDPは交換できたのに道ができなかった」を文で教えてくれる
onPeerHandshake  … 「相手を見つけて話し始めた」時点が分かる
```
→ **「待ち合わせまで行けたのか」「道ができなかったのか」は、これで機械的に切り分けられる。**依頼2の2番はここが本体。

#### ④ Silk について、いま言えること／言えないこと
**言えない**: Silk 実機はここに無い。**「Silk が WebRTC に対応しているか」は、ここからは確かめられない**（D-16）。
**言える**（配信中のバンドルを走査した実測）:
- 読み込む4本が `??=`・`||=`・`Object.hasOwn`・私有フィールド・`replaceAll` を使っている
  → **おおよそ Chromium 93 以上でないと、読む前に構文で落ちる**
- **jsDelivr の `+esm` は、相手のブラウザに合わせて古い書き方に直してはくれない**（同じ1本を返す）
- つまり **Silk が古ければ、対戦に入る前の `import()` が失敗**し、いまの作りでは
  「接続に失敗しました： …」の赤い字が出る。**「相手を待っています…」のまま止まるのとは別の症状**
→ ★**ユーザーからの「つながりません」は、この2つを区別できていない。**まず診断を出すのが先。

#### ★ユーザー回答で分かった切り分け（司令塔経由・09-20）
- **症状は B**（原文「接続の失敗はつながりませんでした ふたりともおなじwifiだとつながりやすいです ってでます」）
  → **Silk でもライブラリは読めている。**案4（同梱）は急ぎでない
- **Chrome どうしなら LTE×LTE でもつながる**（原文「クロム同士なら、LTEとWIFIでもWIFIとWIFIでもLTEとLTEでもつながります」）
  → ★**中継が無くても実用上は足りている。つながらないのは Silk が絡むときだけ**
- 中継は「無料のものを探して使う」でユーザー判断が出た（★ただし Silk の切り分けより後）

#### ★できたこと（実装・検査ずみ・**ローカルにコミットのみ。push していない**）
**① 死んだ中継を外した**（`RTC_CONFIG` の TURN 3本を削除。STUN 2本は残す）
- 死んだURLを残すと、つなぎ始めに毎回そこへ問い合わせて**無駄に待つ**ため
- ⚠️ **いまは中継0本。**替えを足したら**必ず `ice_probe.mjs` で relay 候補が実際に取れることを確かめる**

**② 診断パネル**（`index.html`。ふだんは出ない。**つながらなかったときだけ**出る）
- **1行目に「どこで止まったか」の答え**を置き、以下は根拠。並びは**つながるまでの順**
  `①ブラウザの能力 → ②ライブラリ → ③待ち合わせ → ④相手 → ⑤道`。**止まった場所がそのまま出る**
- 取り方:
  - `WebSocket` を**数えるだけ**の入れ物に差し替え（中身は本物をそのまま返す）→ **待ち合わせの本数**
  - `rtcPolyfill` に**本物を作って返すだけ**の関数を渡す → **候補の種類・ICEの状態・選ばれた道**
  - `joinRoom` の**第3引数**（`onPeerHandshake`／`onJoinError`）→ **相手を見つけたか・道ができたか**
- **「この内容をコピー」ボタン**つき。★新しい書き方が無い端末でも動くよう古いやり方に落ちる。**両方だめなら、そう表示する**
- **版の名前 `DIAG_VERSION`** を入れた。**中身を変えたら必ず変える**（どの版で取った控えか言えるように）

**③ ついでに直した（先天的な穴）**: `tools/test_battle_start.mjs` だけ Chrome への切り替えが無く、
**このPCでは一度も動いていなかった**（`npx playwright install` が通らないため）。他の検査と同じ形にした → **17件全通過**

#### 実測
- `smoke-test.mjs` **22件** ／ `test_battle_resume.mjs` **25件** ／ `test_battle_start.mjs` **17件**（★本物の trystero を使う検査）
- **新しい道具2つ**:
  - `tools/mikaku/ice_probe.mjs` … 本物の Chrome に ICE 候補を集めさせる。**設定は index.html からその場で抜く**（写さない）
  - `tools/mikaku/diag_panel_probe.mjs` … **3つの止まり方を作り分けて、パネルが別の答えを出すことを見る**
    （①相手が来ない ②ライブラリが読めない ③**WebRTC が無い端末＝Silk 想定**）。**全項目 ✔**
- **どちらの道具も「自分が鳴ること」を自分で確かめる節を持つ**（D-17）
- 写真 `tools/mikaku/shots_diag/`。**390px 幅で実際に出して目で見た**（C-4d）

#### ★公開ずみ（2026-09-20・`aa77e0b..8252353`。司令塔が実行・ユーザー承認あり）
**こちらでも公開サイトの実物を取って確かめました**（司令塔の報告を写しただけにしない・D-9）:
- `master` と `origin/master` が**一致**
- `https://nambanamba.github.io/kosukequiz-battle/index.html` を取得 →
  `この内容をコピー` **3**・`DIAG_VERSION` **2**・`ROOM_CALLBACKS` **4**・`rtcPolyfill` **2**
- ★`openrelay` の残り **1件は説明文の中だけ**。`iceServers` は **STUN 2本のみ**で、**死んだ中継は本当に消えている**

#### 次（★待機中。返事待ち）
- ★**ユーザーが Silk で1回試して、パネルの内容をコピーして送ってくる**のを待っています（司令塔が転送）。**そこで原因が決まります**
- 読み方（届いたときに使う）:
  - `WebRTC: ★ない` → Silk が対戦のしくみを持っていない。**別のブラウザしかない**
  - `待ち合わせ: つながった 0` → 探しに行けていない。**strategy 変更（案6）が効く**
  - `相手を見つけた: ★なし`（待ち合わせは OK）→ 部屋は見えるが相手が来ていない
  - `相手を見つけた: 1回` かつ `選ばれた道: なし` → **見つけたが道ができない。中継（案3）の話**
- 中継の替えは、そのあと。**「無料のものを探して使う」でユーザー判断は出ている**

### 2026-09-20 担当を引きついだ（前任 `claude-1d` は ListAgents に不在）
- 名簿を更新: `司令塔\セッション名簿\kosukequiz-battle-app.md`
  - ⚠️ **このセッションの `ListAgents` は 1行目の `This session is …` を返さない**ため、**自分の ref が分かりません。**
    名簿には、他セッションから見えるであろう名前（`あなたは対戦アプリ担当です`）を書きました
- 着任時の実測: `git fetch origin master` 済み。**`master` は `origin/master` より 1コミット先（`1e1d76c`）で、
  中身は `作業メモ.md` だけ**（コードは1バイトも違わない）。**push はユーザー承認が要るので保留**
- 未コミットはコード以外だけ（`tools/mikaku/shots*`・ルートの漢字リスト CSV/MD ほか）。「いま作業中」欄は空
- **下の 2026-09-19 の「待っていること」は、そのまま引きつぎます**（返事は来ていません）
- CLAUDE.md 0c に従い、**名指しの指示が来るまで着手しません**

### ★2026-09-19 いまの状態と、待っていること（引きつぎ用）
- **origin と一致。未 push なし。**今日ぶんは全部公開ずみ:
  ①理科 第2回 kind=calc（照合のみ・差分0・data.js 無変更）／②理科 第3回 改訂4（`c424c8b`）／
  ③理科 第4回 79問（`18be007`）／対戦の開始ボタンと時間切れ（`aa77e0b`）。data.js は 1545 → 1635問
- **手が空いています。**CLAUDE.md 0c に従い、**名指しされていない回には着手しません**

#### ★司令塔に預けてある「ユーザーにお願いしたいこと」2件（返事待ち）
**★ユーザーに直接お願いしないこと**（CLAUDE.md 第4節。2026-09-19 に司令塔から全担当へ再周知）。
**「実機で試してほしい」も、司令塔に送って、司令塔からユーザーに伝えてもらいます。**
1. **対戦の「二人とも押したら始まる」を実機2台で。**中継スタブでの確認までしかできていません
2. **対戦の TURN 追加（`0e3527a`・2026-09-18）が 4G/5G でつながるか。**まだ結果をいただいていません
→ **どちらも、こちらからは確かめようがありません。**返事が来たら、この欄を消してください

#### ★司令塔に出してある提案（返事待ち・急ぎではない）
- `司令塔\確認ポイント.md` の B-3b／D-17 あたりに、中継スタブの教訓を1行足してはどうか
  （「テストが通っていることは、正しい経路を通っている証拠にならない」）。**書けるのは司令塔だけ**

#### ① 第2回 kind=calc の照合（済・data.js は1バイトも触っていない）
- 宣言 sha256 `e9cd20e9…` と実物が**一致**（読む前に照合）。元データ 87問（memo 12 / calc 75）
- 道具を新しく書いた: `tools/verify_rika_pointwise.mjs`（**読んで比べるだけ。data.js を書きかえない**）。`import_rika.py --check` は使っていない
- 結果: **data.js の `r2m01`〜`r2m17` の17問すべてが元データにひもづき、`kind` の差分は0件**
  → ★**①の目的は達成**。data.js 側（`fd84841`）と元データが `kind` で一致した。**取り込みは不要**で合っています
- ★**ただし差分が3件出ました（司令塔に報告ずみ）**: **`r2m15`・`r2m16`・`r2m17` の `sol`（解説）が元データにあり、data.js は空**
  - 3問とも calc なので**アプリには出ません**（出題・一覧の両方から外れている）。**お子さんから見える影響は0**
  - `import_rika.py` の `OUT_KEYS` には `sol` が入っているので、**丸ごと取り込めば入る欄**。入れるかは司令塔の判断待ち
- 元データの calc 70問に data.js の対応が無いのは**正常**（calc は `keisan-print-app` の持ち物。data.js に残る calc 5問とは別）
- 回帰: **第1回も 73問すべてひもづいて差分0**（道具が第2回だけの作りになっていないことの確認）
- ⚠️ **道具を書いたときに踏んだ罠**: テンプレート文字列の中の `\d` が `d` になっていて（JS の決まり）、
  **1問もひもづいていないのに「✔ 差分0」と出た。**→ **r?m* が0問のとき／ひもづきが0件のときは止める**ように直した（D-17）


---

## これより前の記録

**2026-09-08〜09-19 は `作業メモ_アーカイブ_2026-09-08〜09-19.md` に移しました**
（2026-09-20・本体が 136 KB になり、起動のたびに全部読まれていたため）。
**2026-09-08 以前は `作業メモ_アーカイブ_2026-08-30〜09-08.md` にあります。**
**どちらも捨てていません。**索引を先頭に付けてあります。
