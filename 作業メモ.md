# 作業メモ

## いま作業中
（作業を始めるときに「セッション名 / 触るファイル / 内容」を1行書いてpush、終わったら消す。
　作業前に必ずここを読み、他のセッションが同じファイルを触っていたら手を出さないこと）

- （いまは誰も作業していません）

### ★2026-09-20 社会 第4回の演習年表20問を取り込んだ（`2fd83a6`・**push 前**）

ユーザー判断で**赤丸（`TIMELINE_MARKS`）を待たずに先に公開**する方針。司令塔が納品を宣言（sha256 一致）。

#### 結果
- `g4r70`〜`89` の 20問を上書き更新。**id は1つも動いていない**。総数 1635問・id重複0
- `images/kai4_01.jpg`（1189×1839）を追加。`IMG_SIZES` は **`sync_img_sizes.py` で実物から**入れた
- **121問すべて元データと照合 → 不一致0件**（★C-6 の書き戻しは**不要**。今回は元データ→data.js の一方向で、
  data.js 側で直した箇所は無く、照合で完全一致を確かめてある）

#### ★画面で解けるかを先に確かめた（B-12。今回いちばん大事なところ）
書き換え前は文章だけで解けたが、いまは **絵が無いと20問すべて解答不能**になる形。
- ★実機（390px・画素2倍）で **絵が出て、①〜⑳ が読める**（枠 312×483。押すと拡大でき、2.56倍＝幅919px）
- ★**20問すべて、実際の絵の空らんと答えが合っている**（絵を見て1つずつ確かめた。①=794 … ⑳=宋）
- ★**絵は答えを見せていない**（番号の箱はすべて空・A-2b）
- 聞いている番号は ①〜⑳ がそろっている（抜け・ダブり無し）

#### 新しい道具 `tools/mikaku/g4_timeline_probe.mjs`
絵が出るか・押して拡大できるか・拡大で実際に大きくなるかを見て、写真に残す。
★**「番号が読めるか」は判定しません**（機械では決められないので、人が写真を見る）と道具に明記した。

#### 赤丸はあと
座標が届いたら次の公開で足す。**データに触らない**ので安全（画像つき479問のうち赤丸持ちは50問だけ）。

### 2026-09-20 第4回の取り込み：届く前の下見（**データには触っていません**）

**納品前の JSON は読んでいません**（C-4c-2）。実物を見たのは画像と data.js と index.html だけです。

#### 実測した数字
| 見たもの | 実測 |
|---|---|
| `画像プレビュー\kai4_01.jpg` | **1189×1839**（司令塔の言う `[1189,1839]` と一致） |
| ★`画像プレビュー\kai4_01_master600.jpg` | **1390×2150**（**別サイズの同じ絵が並んで置いてある**） |
| `images\` の kai4 | **0件**（まだコピーされていない） |
| data.js の `g4r70`〜`89` | **20件すべて在る**（第4回は121問）。いま `file` 欄は**無い**。`a` は「（794）年です。」など |
| `TIMELINE_MARKS` の g4 | 0件 ／ `IMG_SIZES` の kai4 | 0件 |
| `IMG_SIZES` の入れる場所 | `"kai3_07.jpg"` の直後、`"kaki_r1_01.jpg"` の直前 |

#### ★★座標の受け取りで、いちばん危ないところ（社会担当に確かめること）
**同じ絵が2つ、ちがう大きさで置いてあります**（1189×1839 と 1390×2150）。**縦横比は同じ**です。
```
座標が「割合（0〜1）」で来る → ★どちらの絵で測っても同じ。そのまま使える
座標が「px」で来る          → ★どちらの絵で測ったかで、1.169倍ずれる
```
**`TIMELINE_MARKS` は割合で持ちます**（`index.html` の `drawMarks` が naturalWidth 基準で描くため）。
→ **納品時に「単位（割合か px か）」と「px なら測った絵の大きさ」を書いてもらうこと。**
　 書いていなければ、取り込む前に必ず聞く。**ここを取り違えると、赤丸が全部ずれた位置に乗ります。**

#### ★`TIMELINE_MARKS` は後から足せる（司令塔の問いに、実物で答えた）
**足せます。データには触りません。**根拠は読んだだけでなく、いま動いているものが証拠です。
```
画像つきの問 479件 のうち、赤丸を持つのは 50件だけ。
★残り 429件は、赤丸なしで今まさに配信されている（simg1・g1r41 など）
```
- `drawMarks` は `TIMELINE_MARKS[qid]` が無ければ**その場で戻る**（`index.html`）。絵はふつうに出る
- `TIMELINE_MARKS` は **`index.html` の中にあり、id を鍵にした表**。data.js とは独立
→ **画像・`q`・`img`・`IMG_SIZES` を先に出し、赤丸だけ後から足す**のは安全です。
⚠️ ただし**絵を切り直したら座標は無効**になるので、**赤丸を足すのは絵を確定させたあと**。

#### ★申し送り（司令塔から・2026-09-20）
`kai4_01_master600.jpg` は **1390×2150** で、**第3回の master600（2851×4800）ほどの原寸控えではありません。**
agy に渡した切り出しがその大きさだったため。**いまは作り直しません**（配信用 1189 幅は第3回と同じで実績があり、
絵を切り直すと座標が全部無効になるため）。
→ **将来もっと大きい控えが要るなら `社会演習.pdf` 31P から取り直す。そのときは座標も測り直し。**

#### ★訂正（私の言い方の誤り）
先に「いま `file` 欄は無い」と書きましたが、**data.js 側の鍵は `img` です**（`file` は元データJSON側の名前）。
中身の話は変わりません（**第4回 121問に画像つきは0件**）。取り込みのときに取り違えないこと。

#### 届いたらやる順番
1. **`sha256` を照合してから**元データを読む（C-4c-2）
2. 画像を `images\` へコピー（★差し替えではなく新規。旧画像の削除は無し・C-3）
3. ★**`IMG_SIZES` は手で書かず `python tools/sync_img_sizes.py` で実物から入れる**（C-4b。
   「宣言された数字」ではなく「実物の寸法」が入るので、取り違えが起きない）
4. `import_g.py` で `no` を id の番号として取り込む（★通常回に `id` 欄は無い・C-6b）
5. `TIMELINE_MARKS` に `g4r70`〜`89` を追加（**24矩形。⑥⑨⑩⑲ は2か所ずつ**）
6. 元データへ書き戻し（C-6）＋ 自前照合
7. ★**実機で見る**: `node tools/mikaku/mikaku_check2.mjs g4r70 … g4r89`（引数に id を並べる作り）。
   ⚠️ **年表は縦長で、390px 幅だと赤丸が小さい。**小さい写真で「乗っている」と判断しないこと。
   **拡大したときのずれは、拡大しないと出ません**（`drawMarks` のコメントに前例あり）。`zoom_probe.mjs` も使う

### ★2026-09-20 つながらない件：記録の直しと待ち合わせ場所の決めうち（`11fa015`・**公開ずみ**）

#### ★公開を実物で確かめた（司令塔の報告を写しただけにしない・D-9／B-12）
`8252353..698d4b3` を push。**公開サイトを取ってきて中身を数えた**:
```
版 DIAG_VERSION = "2026-09-20b"      ／ relayConfig の指定 1件
「相手を見つけたかを先に見る」はしご 1件 ／ いま開いている本数を見る箇所 3件
待ち合わせ先の行 1件
配られている待ち合わせ先 = 決めうちの6本ちょうど
  nostr.sathoarder.com / nostr-01.yakihonne.com / top.testrelay.top /
  relay.damus.io / nos.lol / relay.primal.net
`relayUrls:` の残り1件は**説明文の中だけ**（効かない書き方だと注意書きしている行）
```

⚠️ **`ROLE.md` の追記（`cbb2e75`）は未 push。**承認は `11fa015`＋`698d4b3` に対して出たもので、
そのあとに作った分なので、**次の push にまとめる**（勝手に足さない）。


司令塔の依頼2通（16:40 と 16:51 で優先順位が入れかわった）に対応。**コードはローカルにコミットずみ、push は承認待ち。**

#### ★★まず、依頼書の指定を1つ訂正しました（実物で確かめた）
> 依頼書は「`ROOM_CONFIG` に **`relayUrls`** を足す」でしたが、**trystero 0.25.4 では効きません。**
> 正しくは **`relayConfig: { urls: [...] }`**。`relayUrls` は**黙って無視**され、エラーも出ません。

`tools/mikaku/relay_probe.mjs` ③で**両方の書き方をためして**確かめています。
**09-18 の TURN（書いてあったのに一度も効いていなかった）と、まったく同じ型でした。**

#### 直したこと（`index.html`。版 `DIAG_VERSION` は `2026-09-20b`）
1. **判定の順番を入れかえた**（司令塔の新しい優先1）
   もとは待ち合わせの本数を先に見ていたため、**「相手を見つけた: 1回」と書いてある同じ紙に
   「探しに行けていません」**と出ていた。**相手を見つけたかを先に見る。**
2. **待ち合わせの数え方**を「新しく作られた本数」→「**いま開いている本数**」に。
   ライブラリは接続を appId ごとに**使い回す**ので、2回目は 0 本になっていた
3. **★つないだ待ち合わせ先の名前を出した**（2台ぶん並べれば、同じ場所にいるか一目で分かる）
4. **道づくりを通信路ごとに分けた**（司令塔の新しい優先2）。
   ライブラリは相手用のほかに**使い捨ての通信路**も作る。混ぜていたので `disconnected` が誰の話か読めなかった
5. **待ち合わせ場所を6本に決めうち**（`RELAY_URLS`）。★うち3本は**既定でも選ばれる先**にしてある。
   こうしないと、**まだ古い版を開いている端末と出会えなくなる**

#### ★実測で分かったこと（司令塔の読みとの差）
| | 司令塔の読み | 私の実測 |
|---|---|---|
| 2台は別の relay を選んでいる？ | 本命の仮説 | **選び方は appId で決まり、2台とも同じ5本**（別ページで2回引いて一致） |
| 計測がライブラリの接続を捉えていない？ | 「一度も捉えていない可能性」 | **PCでは6本すべて捉えられた**（`meet_probe` ④）。捉え損ねではなく**使い回し**で説明がつく |
| 既定の5本は使えるか | — | ★**生きていたのは3本だけ。**relay は知らせを断ると**ライブラリ側から接続が閉じられる** |

→ 「切れた7 / 失敗6」という Silk の記録は、この**閉じられ方**と一致する。

#### ★検査の穴を1つ塞いだ
**これまでの「2人ぶん」の検査は全部スタブで、待ち合わせは一度も試されていなかった。**
`test_battle_start` は本物を読むが**部屋を作るところまで**。
→ `tools/mikaku/meet_probe.mjs` を作った。**本物の relay ごしに2つの画面が実際に出会えるか**を見る。
　 実測: 決めうち・既定まかせ **どちらも2秒で出会える**／片方を別の場所にすると**出会えない**（検査が鳴る）

#### 実測（全部通過）
smoke 22 ／ resume 25 ／ start 17（本物の trystero）／ diag_panel（⑤⑥を追加）／ meet_probe ／ relay_probe

#### 次
- ★**社会・理科 第4回の取り込みが最優先**（司令塔 16:55。ユーザー「そろそろ使いたい」）。届き次第そちらへ
- 中継（TURN）の選択肢と費用の整理 → 司令塔へ上げる（**ユーザー判断**。自分で決めて入れない）
- ⚠️ **この版が届くまで、実機の記録は読み方が変わりません。**push 後にもう一度取ってもらうのが有効

### 2026-09-20 担当を引きついだ（新セッション。前任は ListAgents に不在）
- 着任時の `ListAgents` は **`あなたは司令塔です [efdd7d]` の1件だけ**。**対戦アプリ担当は他にいません**＝前任は不在。名簿を更新した
- ⚠️ 前任と同じく、**このセッションの `ListAgents` は 1行目の `This session is …` を返さない**ため、自分の ref は不明
- 実測（`git fetch origin master` 済み）:
  - `origin/master` = `8252353`、ローカル `master` = **`2e8948a`（ahead 1）**。中身は **`作業メモ.md` だけ**でコードは1バイトも違わない
  - 未コミットは `tools/mikaku/shots*` などのスクリーンショットのみ。「いま作業中」欄は空＝競合なし
- **下の引きつぎ（Silk の診断パネルの控え待ち／中継の替えはその後）は、そのまま引きつぎます。**返事は来ていません
- CLAUDE.md 0c に従い、**名指しの指示が来るまで着手しません**

### ★2026-09-20 「つながらない」の調査（司令塔 claude-33 の依頼）— 実装前の報告まで
依頼は2通。①「LTEとWi-Fiだとつながらない」→ ②途中で「**Chrome どうしはつながる。Silk だとつながらない**」に更新。
**司令塔の判断待ち。実装はまだ1行もしていません**（`index.html` は触っていない）。

#### ★★① Open Relay（TURN）は死んでいる。09-18 の `0e3527a` は**配信した日から効いていなかった**
**実機の Chrome で ICE 候補を実際に集めて確認**（新しい道具 `tools/mikaku/ice_probe.mjs`）。
```
① index.html の設定そのまま → 候補3件 {host:2, srflx:1}  ★relay 0件
     error 701 turn:openrelay.metered.ca:80  — TURN host lookup received error
     error 400 turn:openrelay.metered.ca:443 — TURN allocate error
     error 701 turns:openrelay.metered.ca:443?transport=tcp — Failed to establish connection
② TURN 1本ずつ（中継だけ使う設定）→ 生きている TURN 0 / 3
③ ★この検査が鳴るかの自己確認 → でたらめな TURN で relay 0件。**検査は鳴る**（D-17）
```
裏づけ2つ:
- `nslookup openrelay.metered.ca` → **名前が引けない**（`metered.ca` 本体は HTTP 200 で生きている）。**ホスト名ごと消えている**
- metered は**APIキー必須に変わった**（無料枠は残るが、合いことばを固定で埋め込む使い方は終了）

⚠️ **「Chrome どうしはつながる」と矛盾しません。**あれは**直接つながった**ので中継を使っていません。
**中継が要る回線（LTE・きびしいNAT）だけが、静かに落ちていました。**

#### ② `ROOM_CONFIG` は効いている（キー名も合っている）
**当て推量ではなく、配信で読み込んでいる実物のバンドルを取ってきて読んだ**（`@trystero-p2p/core@0.25.4`）。
```js
initPeer: En …… e.initPeer(false, e.config)   // e.config = joinRoom に渡した設定そのもの
En = (_, {rtcConfig: t, turnConfig: f}) => new RTCPeerConnection({iceServers: 既定.concat(f ?? []), ...t})
```
→ **`rtcConfig` は正しく届いている。**ただし `...rtcConfig` が後ろなので、**`rtcConfig.iceServers` を書くと既定も `turnConfig` も丸ごと上書き**される。
　 いまは既定の STUN 2本を自分で書いているので実害なし。**将来 `turnConfig` を併用すると黙って消えるので注意。**

#### ③ ★使っていない切り分け道具が、ライブラリ側にすでに在った
`joinRoom(config, roomId, **第3引数**)` を受け取る作りで、**いまは第3引数を渡していない**。
```
onJoinError      … 「SDPは交換できたのに道ができなかった」を文で教えてくれる
onPeerHandshake  … 「相手を見つけて話し始めた」時点が分かる
```
→ **「待ち合わせまで行けたのか」「道ができなかったのか」は、これで機械的に切り分けられる。**依頼2の2番はここが本体。

#### ④ Silk について、いま言えること／言えないこと
**言えない**: Silk 実機はここに無い。**「Silk が WebRTC に対応しているか」は、ここからは確かめられない**（D-16）。
**言える**（配信中のバンドルを走査した実測）:
- 読み込む4本が `??=`・`||=`・`Object.hasOwn`・私有フィールド・`replaceAll` を使っている
  → **おおよそ Chromium 93 以上でないと、読む前に構文で落ちる**
- **jsDelivr の `+esm` は、相手のブラウザに合わせて古い書き方に直してはくれない**（同じ1本を返す）
- つまり **Silk が古ければ、対戦に入る前の `import()` が失敗**し、いまの作りでは
  「接続に失敗しました： …」の赤い字が出る。**「相手を待っています…」のまま止まるのとは別の症状**
→ ★**ユーザーからの「つながりません」は、この2つを区別できていない。**まず診断を出すのが先。

#### ★ユーザー回答で分かった切り分け（司令塔経由・09-20）
- **症状は B**（原文「接続の失敗はつながりませんでした ふたりともおなじwifiだとつながりやすいです ってでます」）
  → **Silk でもライブラリは読めている。**案4（同梱）は急ぎでない
- **Chrome どうしなら LTE×LTE でもつながる**（原文「クロム同士なら、LTEとWIFIでもWIFIとWIFIでもLTEとLTEでもつながります」）
  → ★**中継が無くても実用上は足りている。つながらないのは Silk が絡むときだけ**
- 中継は「無料のものを探して使う」でユーザー判断が出た（★ただし Silk の切り分けより後）

#### ★できたこと（実装・検査ずみ・**ローカルにコミットのみ。push していない**）
**① 死んだ中継を外した**（`RTC_CONFIG` の TURN 3本を削除。STUN 2本は残す）
- 死んだURLを残すと、つなぎ始めに毎回そこへ問い合わせて**無駄に待つ**ため
- ⚠️ **いまは中継0本。**替えを足したら**必ず `ice_probe.mjs` で relay 候補が実際に取れることを確かめる**

**② 診断パネル**（`index.html`。ふだんは出ない。**つながらなかったときだけ**出る）
- **1行目に「どこで止まったか」の答え**を置き、以下は根拠。並びは**つながるまでの順**
  `①ブラウザの能力 → ②ライブラリ → ③待ち合わせ → ④相手 → ⑤道`。**止まった場所がそのまま出る**
- 取り方:
  - `WebSocket` を**数えるだけ**の入れ物に差し替え（中身は本物をそのまま返す）→ **待ち合わせの本数**
  - `rtcPolyfill` に**本物を作って返すだけ**の関数を渡す → **候補の種類・ICEの状態・選ばれた道**
  - `joinRoom` の**第3引数**（`onPeerHandshake`／`onJoinError`）→ **相手を見つけたか・道ができたか**
- **「この内容をコピー」ボタン**つき。★新しい書き方が無い端末でも動くよう古いやり方に落ちる。**両方だめなら、そう表示する**
- **版の名前 `DIAG_VERSION`** を入れた。**中身を変えたら必ず変える**（どの版で取った控えか言えるように）

**③ ついでに直した（先天的な穴）**: `tools/test_battle_start.mjs` だけ Chrome への切り替えが無く、
**このPCでは一度も動いていなかった**（`npx playwright install` が通らないため）。他の検査と同じ形にした → **17件全通過**

#### 実測
- `smoke-test.mjs` **22件** ／ `test_battle_resume.mjs` **25件** ／ `test_battle_start.mjs` **17件**（★本物の trystero を使う検査）
- **新しい道具2つ**:
  - `tools/mikaku/ice_probe.mjs` … 本物の Chrome に ICE 候補を集めさせる。**設定は index.html からその場で抜く**（写さない）
  - `tools/mikaku/diag_panel_probe.mjs` … **3つの止まり方を作り分けて、パネルが別の答えを出すことを見る**
    （①相手が来ない ②ライブラリが読めない ③**WebRTC が無い端末＝Silk 想定**）。**全項目 ✔**
- **どちらの道具も「自分が鳴ること」を自分で確かめる節を持つ**（D-17）
- 写真 `tools/mikaku/shots_diag/`。**390px 幅で実際に出して目で見た**（C-4d）

#### ★公開ずみ（2026-09-20・`aa77e0b..8252353`。司令塔が実行・ユーザー承認あり）
**こちらでも公開サイトの実物を取って確かめました**（司令塔の報告を写しただけにしない・D-9）:
- `master` と `origin/master` が**一致**
- `https://nambanamba.github.io/kosukequiz-battle/index.html` を取得 →
  `この内容をコピー` **3**・`DIAG_VERSION` **2**・`ROOM_CALLBACKS` **4**・`rtcPolyfill` **2**
- ★`openrelay` の残り **1件は説明文の中だけ**。`iceServers` は **STUN 2本のみ**で、**死んだ中継は本当に消えている**

#### 次（★待機中。返事待ち）
- ★**ユーザーが Silk で1回試して、パネルの内容をコピーして送ってくる**のを待っています（司令塔が転送）。**そこで原因が決まります**
- 読み方（届いたときに使う）:
  - `WebRTC: ★ない` → Silk が対戦のしくみを持っていない。**別のブラウザしかない**
  - `待ち合わせ: つながった 0` → 探しに行けていない。**strategy 変更（案6）が効く**
  - `相手を見つけた: ★なし`（待ち合わせは OK）→ 部屋は見えるが相手が来ていない
  - `相手を見つけた: 1回` かつ `選ばれた道: なし` → **見つけたが道ができない。中継（案3）の話**
- 中継の替えは、そのあと。**「無料のものを探して使う」でユーザー判断は出ている**

### 2026-09-20 担当を引きついだ（前任 `claude-1d` は ListAgents に不在）
- 名簿を更新: `司令塔\セッション名簿\kosukequiz-battle-app.md`
  - ⚠️ **このセッションの `ListAgents` は 1行目の `This session is …` を返さない**ため、**自分の ref が分かりません。**
    名簿には、他セッションから見えるであろう名前（`あなたは対戦アプリ担当です`）を書きました
- 着任時の実測: `git fetch origin master` 済み。**`master` は `origin/master` より 1コミット先（`1e1d76c`）で、
  中身は `作業メモ.md` だけ**（コードは1バイトも違わない）。**push はユーザー承認が要るので保留**
- 未コミットはコード以外だけ（`tools/mikaku/shots*`・ルートの漢字リスト CSV/MD ほか）。「いま作業中」欄は空
- **下の 2026-09-19 の「待っていること」は、そのまま引きつぎます**（返事は来ていません）
- CLAUDE.md 0c に従い、**名指しの指示が来るまで着手しません**

### ★2026-09-19 いまの状態と、待っていること（引きつぎ用）
- **origin と一致。未 push なし。**今日ぶんは全部公開ずみ:
  ①理科 第2回 kind=calc（照合のみ・差分0・data.js 無変更）／②理科 第3回 改訂4（`c424c8b`）／
  ③理科 第4回 79問（`18be007`）／対戦の開始ボタンと時間切れ（`aa77e0b`）。data.js は 1545 → 1635問
- **手が空いています。**CLAUDE.md 0c に従い、**名指しされていない回には着手しません**

#### ★司令塔に預けてある「ユーザーにお願いしたいこと」2件（返事待ち）
**★ユーザーに直接お願いしないこと**（CLAUDE.md 第4節。2026-09-19 に司令塔から全担当へ再周知）。
**「実機で試してほしい」も、司令塔に送って、司令塔からユーザーに伝えてもらいます。**
1. **対戦の「二人とも押したら始まる」を実機2台で。**中継スタブでの確認までしかできていません
2. **対戦の TURN 追加（`0e3527a`・2026-09-18）が 4G/5G でつながるか。**まだ結果をいただいていません
→ **どちらも、こちらからは確かめようがありません。**返事が来たら、この欄を消してください

#### ★司令塔に出してある提案（返事待ち・急ぎではない）
- `司令塔\確認ポイント.md` の B-3b／D-17 あたりに、中継スタブの教訓を1行足してはどうか
  （「テストが通っていることは、正しい経路を通っている証拠にならない」）。**書けるのは司令塔だけ**

#### ① 第2回 kind=calc の照合（済・data.js は1バイトも触っていない）
- 宣言 sha256 `e9cd20e9…` と実物が**一致**（読む前に照合）。元データ 87問（memo 12 / calc 75）
- 道具を新しく書いた: `tools/verify_rika_pointwise.mjs`（**読んで比べるだけ。data.js を書きかえない**）。`import_rika.py --check` は使っていない
- 結果: **data.js の `r2m01`〜`r2m17` の17問すべてが元データにひもづき、`kind` の差分は0件**
  → ★**①の目的は達成**。data.js 側（`fd84841`）と元データが `kind` で一致した。**取り込みは不要**で合っています
- ★**ただし差分が3件出ました（司令塔に報告ずみ）**: **`r2m15`・`r2m16`・`r2m17` の `sol`（解説）が元データにあり、data.js は空**
  - 3問とも calc なので**アプリには出ません**（出題・一覧の両方から外れている）。**お子さんから見える影響は0**
  - `import_rika.py` の `OUT_KEYS` には `sol` が入っているので、**丸ごと取り込めば入る欄**。入れるかは司令塔の判断待ち
- 元データの calc 70問に data.js の対応が無いのは**正常**（calc は `keisan-print-app` の持ち物。data.js に残る calc 5問とは別）
- 回帰: **第1回も 73問すべてひもづいて差分0**（道具が第2回だけの作りになっていないことの確認）
- ⚠️ **道具を書いたときに踏んだ罠**: テンプレート文字列の中の `\d` が `d` になっていて（JS の決まり）、
  **1問もひもづいていないのに「✔ 差分0」と出た。**→ **r?m* が0問のとき／ひもづきが0件のときは止める**ように直した（D-17）

### 2026-09-19 対戦アプリ: 開始ボタンと、スキップ／時間切れの切り分け（司令塔 claude-33 の依頼）
- 依頼書: `司令塔\回答\対戦アプリ_開始ボタンとスキップ_依頼_2026-09-19.md`（ユーザー原文つき）

#### ★先に調べた「いまの作り」（依頼書の「確かめてから作ること」への答え）
- ★**スキップも時間切れも、まったく同じ関数 `performSkip()` を通っている**（`hostAdvanceAndArm` の
  `pendingSendTimer = setTimeout(performSkip, headStartSec*1000)`）。だから**いまは見分けがつかない**。ここが依頼の中心
- `performSkip()` がしていること: ①答えを出す（`battle-a-block.show`）②`hostSkippedIds` に入れる
  ③その問題をラウンドの**最後に回す**（`questionIds` の末尾へ）④ゲストに「相手がこの問題をスキップしました」を送る
- 誤答になる仕組み: `judgeAction.onMessage` の
  **`forcedWrong = inMissRetryRound || (role === "host" && hostSkippedIds.has(qid))`**。
  → **`hostSkippedIds` に入れなければ、正解は正解のまま記録される**。③はここを触るだけで足りる
- ★**どちらの記録か**: 判定は「相手が自分を判定する」形なので、**`hostSkippedIds` はホスト自身の記録にだけ効く**
- ★**「あとでまた出てくる」は、いまの作りでもう成り立っている**（③で末尾に回している）。**追加の作りは要らない**
- ★**やり直しラウンドにも自然に入らない**。`myMissesThisRound` は「判定で誤答になったとき」だけ積むので、
  **時間切れで記録しなければ、やり直しの対象にもならない**（回ってきて誤答になれば入る）。**ここも作らなくてよい**
- ★**公平モード（`fairMode`）には、そもそも先読みもスキップも無い**（`hostAdvanceAndArm` が即座に配信して戻る）。
  → **②③は公平モードには関係しない**
- ★**スキップボタンはホストにしか出ない**（`showQuestion(idx, hostPrivate)` の `hostPrivate` のときだけ）。ゲストには無い
- ②「押したときは答えを見せて誤答」は、**いまの作りのまま**で合っている。③を切り出すときに壊さないようにする
- ひとり練習には `hostSkippedIds` の仕組みが無い（スキップ自体が無い）。**変えない**

#### ★① 開始ボタン: いまは「ホストだけ」。自動開始もある
- いま: 相手が入ると**ホストにだけ**「二人で開始する」が出て、**8秒（公平モードは20秒）で自動開始**
  （`START_TOGETHER_COUNTDOWN_SEC` / `startStartTogetherCountdown`）。ゲストは待つだけ（`showGuestStartCountdown` は数字を出すだけの飾り）
- ★**依頼書の「片方だけ押した状態では始まらない」を守るには、この自動開始をやめるしかない。**やめる方針で作る
#### ★できたこと（実装・検査ずみ・コミットずみ・push していない）
- **① 開始**: ゲスト側にも「開始する」を置き、`ready` の合図を1つ足した（`readyAction`）。**二人とも押したらホストが始める**。
  押した側は「相手を待っています…」、相手側は「相手は準備OK！ 開始する」。
  ★**自動開始（ホスト8秒・公平モード20秒）は外した。**「片方だけでは始まらない」と両立しないため。
  つられて `showGuestStartCountdown`（ゲスト側の秒読みの飾り）も要らなくなったので消した
- **② スキップを押したとき**: 変えていない（答えを出す・`hostSkippedIds` に入れて誤答）
- **③ 時間切れ**: `performTimeUp()` を新しく作り、`hostAdvanceAndArm` のタイマーの行き先をここに変えた（1行）。
  答えを出さない・`hostSkippedIds` に入れない（＝記録を何も変えない）・問題は末尾に回す・ゲストには「時間切れ」と伝える
- 共通部分は `moveCurrentToEndOfRound()` に切り出した

#### 実測
- `smoke-test.mjs` **22件** ／ `test_battle_resume.mjs` **25件**（もとは22件。「ホストが押しただけでは始まらない」を3シナリオに足した）
- **`tools/mikaku/battle_start_timeup_probe.mjs`（新規）で、実機の Chrome に二人ぶんの画面を出して 15件すべて確認**:
  開始ボタンが二人に出る／片方では始まらない／二人で始まる／**時間切れで答えが出ない・記録が0件のまま**／
  **時間切れの問題が回ってきて正解したら「正解」で記録される**／スキップは答えが出て、正解しても誤答のまま
- 写真: `tools/mikaku/shots_battle/`。時間切れの画面（`2_host_timeup.png`）を目で見て、答えが出ていないことを確かめた

#### ★★見つけた別の問題: 再開テストの中継スタブが嘘をついていた（直した）
- `test_battle_resume.mjs` のスタブは、部屋に入った合図を**入ってきた本人にも配っていた**。
  そのため**ホストが部屋を作った瞬間に「相手が来た」ことになり**、`gameStarted` が true に。
  ゲストが本当に入ると**2回目の onPeerJoin ＝「再接続の追いつき」の枝**に入り、
  **開始ボタンを押す前からホストが対戦画面に立っていた**
- ★**古いテストはこれで「通って」いた。**開始ボタンを押す前から対戦画面に居たので、押しても押さなくても同じに見えていた。
  今回「片方だけでは始まらない」を足して、はじめて露見した
- 直し方: 本物の trystero と同じく、**先にいた人と入ってきた人の双方に1回ずつ**配る。25件すべて通過
- **教訓**: ★**通っているテストは「正しい経路を通っている」ことの証拠にならない。**
  スタブが本物と違うと、テストは違う道を通って、それでも緑になる（B-3b の型）


#### ② 第3回 横ぐし改訂4 の取り込み（★済・ローカルにコミットずみ・**push していない**）
- **入れた元データ: `第3回_水溶液の中和.json` / sha256 `a40e79629906b4163fdff39b5e96a0702dd634a85a2ccb5bde57a8bdb7320b66`（107問・memo 89・calc 18）。読む前に照合して一致を確認**
  - ⚠️ 旧 `0066b72d…` は破棄された値。使わないこと
- 道具: **`tools/patch_r3_rev4.py`（--check / --apply）**。`import_rika.py` は使っていない（走らせると 78問すべての id が動く）
- **実測: 全1545問 → 1556問（+11）／変えた既存 11問（`r3m81`〜`r3m91` の q・note・a だけ）／増やした 11問（`r3m92`〜`r3m102`）／消えた id 0・id重複 0・欠番13はそのまま**
  - `git diff` に出た `"id"` 行は **`+` の11本（r3m92〜102）だけ。`-` は0本**＝既存の id は1つも書きかわっていない
  - `verify_rika_pointwise.mjs 3` … **89問すべてひもづいて差分0**（対応の無い18件はすべて calc＝正常）
  - 画像: **22問とも img 無し**。data.js 全体でも実物の無い画像は0件（C-4・C-4b の作業は不要だった）
  - `smoke-test.mjs` **22件通過** ／ 実機（本物のChrome・390px）で **6問（r3m81・88・91・92・98・102）を出題して目視。6/6 表示・JSエラー0**
  - 控え: `data.js.bak`
- ★**この道具が効いた理由（次の人へ）**: 書きかえる前に**往復検査**をしている。
  「触らない78問を読んで書き直したら1バイト一致するか」を先に確かめるので、**書式の思いちがいで他の行を巻きこむことがない。**
  q・note・a 以外に差があったら止まるようにもしてある
- ★**id の決め方は「`no` − それより前にある calc の数」**。改訂4の元データでも、**問題文が変わっていない67問で規則と一致**することを確かめてから使った（独立検算）

#### ★2026-09-19 14:40 公開ずみ（ユーザー承認 → 司令塔経由で指示 → push）
- `63d9495..c424c8b` を `origin/master` に push。**push 前に fetch して origin が進んでいないことを確認**（ahead 6 / behind 0）
- コミットに入ったファイルは `data.js`・`tools/patch_r3_rev4.py`・`tools/verify_rika_pointwise.mjs`・`作業メモ.md` の4つだけ。**`data.js.bak` は `.gitignore` 済みで入っていない**
- ★**公開サイトで実物を確認**（`https://nambanamba.github.io/kosukequiz-battle/data.js` をキャッシュ避けつきで取得）:
  **総数 1556／`r3m*` 89問／最大 id `r3m102`／`r3m81` の答えが新しい「①酸性　②通す」になっている**
  - ⚠️ push の直後はまだ古い版（1545）が返ってきた。**Pages の build が終わるまで約1分かかる。**「push した」で終わらせないこと（D-6b）
- ③第4回の予告: **80問・sha256 `e738c23b…`**。レビュー通過後に claude-28 から渡される

#### ★いまの状態（2026-09-19 14:38 時点・司令塔の依頼で明記）
- **どこまで終わったか**: ①第2回（照合のみ・data.js 無変更）と ②第3回（取り込み・`c424c8b`）が**完了し、公開サイトに反映ずみ**
- **次に何をするか**: **③第4回の取り込み**。元データ（`5年下\quiz_csv_理科\第4回_*.json`）**未着**。届いたら sha256 を照合 → `import_rika.py` の `FILES` に第4回を足す → `--check` → `--apply`（新規追加なので id は動かない）
- **途中のファイル**: 無し。中断中の取り込みはありません
- ✅ **司令塔判断（2026-09-19）: `r2m15`・`r2m16`・`r2m17` の `sol` は「いまは入れない」。**アプリに出ない問題なので。★次に誰かが「元データに sol があるのに data.js に無い」と見つけても、**抜けではなく決めた結果**です
- ⚠️ **sha256 `0066b72d…` はもう古い。**レビュー指摘1件（4問の主語を通称に）を claude-28 が直しているため、**新しい値で照合する**
- 段取り（受け取ったら）: ①新しい sha256 を照合 → ②`id,no` の対応表を作る（q が変わるので問題文ではひもづかない） → ③`r3m81`〜`r3m91` は q・a・note を**その場で上書き**、新規11問は `r3m92`〜`r3m102` で末尾に追加 → ④`verify_rika_pointwise.mjs --map` で差分0を確認 → ⑤smoke-test → ⑥実機で目視（B-12）
- data.js の r3m の最大は `r3m91` なので、**`r3m92`〜`r3m102` は空いている**（重複しない）
- 宣言 sha256 `0066b72d…`（14:24 の実測と一致）。107件。`r3m81`〜`r3m91` は q・a・note の上書き、新規11問は `r3m92`〜`r3m102`
- ★**q を書きかえるので、問題文でのひもづけが効かない。**`--map`（`id,no` の対応表）を作ってから照合する
- 通過したら claude-28 が直接渡してくる。**そのときもう一度 sha256 を照合する**

#### ③ 第4回（元データ未着）
- `5年下\quiz_csv_理科\` に `第4回_*.json` はまだ無い。道具側はファイル名を**決め打ちせず**、`第4回_*.json` を1つだけ見つけたら使う形にした（推測で名前を書かない・A-10）

### 2026-09-19 司令塔 claude-33（`四谷大塚` [0e8455]）の依頼: 理科の取り込みだけ。①第2回 kind=calc ②第3回 横ぐし改訂4 ③第4回 新規
- 作成担当 `claude-28` [fb7c3b] ／ レビュー担当 `claude-30` [7cd256] から、**レビューを通ったもの（sha256 宣言つき）**が順に来る
- 取り込みは **id キーでピンポイント**。★丸ごと再取り込みは禁止。取り込むたびにローカルにコミットして司令塔へ一報。**push はしない**（ユーザー承認は司令塔が取る）
- いまは**待機**。下は、待つあいだに data.js 側だけを読んで確かめた事実（元データは書きかえていない）

#### ★★着手前に分かった危険: `tools/import_rika.py --apply 2` / `--apply 3` は、いまの data.js では id を壊す
**どちらも「丸ごと再取り込み」の道具で、今回の①②では使えない。**理由を残す（C-5: id は履歴のキー）。

- **`import_rika.py` の採番は「memo だけの並び順の連番」**（`convert()` 112行 `rec["id"] = "r%dm%02d" % (kai, i)`）。
  **必ず `r?m01` から歯抜けなしに振り直す。**
- **第2回**: data.js はいま **memo 12・calc 5 の計17問**（`r2m01`〜`r2m17`。calc は `r2m12・13・15・16・17`）。
  `import_rika.py` は **calc を取り込まない**ので、走らせると **calc の5問が data.js から消え、`r2m14` が `r2m12` に付けかわる**。
  → ★**①の「差分0の確認」に `import_rika.py --check 2` を使ってはいけない。**差分0にはならないし、`--apply` は事故になる。
  **確認は id キーで1問ずつ突き合わせる**（data.js の `r2m*` ⇔ 元データの該当行）
- **第3回**: data.js はいま **78問なのに id は `r3m01`〜`r3m91`、13個の欠番つき**
  （欠番 = 4・5・6・8・15・35・36・37・39・55・56・57・58。2026-09-13 の横ぐし改訂3で消した13問）。
  **`import_rika.py` は歯抜けを作れない**ので、走らせると **78問すべてが `r3m01`〜`r3m78` に振り直される**。
  → **data.js だけから言える事実**（元データが何であっても成り立つ）
- ★**いまの data.js の r3m の採番則は「`no` − それより前にある calc の数」**。
  改訂3 の元データ（96件）と問題文で照合して **67/67 一致**。
  「memo だけの連番」の仮説は **3/67 しか合わない**（消した13問の `no` の穴を、id 側も欠番として残しているため）
  - ⚠️ `import_rika.py` の冒頭コメントと、`quiz_csv_理科\司令塔への報告_第3回横ぐし_20260913.md` は
    どちらも「id は memo の並び順の連番」と書いている。**取り込み当時は正しかったが、13問を消したいまは成り立たない**
- **第4回**は data.js に0問なので、**新規追加の経路なら `import_rika.py` が使える**。ただし `FILES`（48〜52行）に第4回のファイル名を足す必要がある

#### ⚠️ いま元データ2本は「編集中」。読むだけにして、取り込まない（C-4c / C-4c-2）
- `第2回_てこ滑車輪軸.json` … 14:18 更新／`第3回_水溶液の中和.json` … 14:19 更新（いずれも私の確認時刻 14:22 の直前）
- ★**第3回は、私が2回読むあいだに中身が変わった**: 14:19 の読みで memo 78 → 14:21 の読みで memo 89。
  **C-4c-2 の事故（検査したものと取り込んだものが別物）と同じ状況。**
- 第3回のいまの sha256 は `0066b72d…`。改訂3の宣言値 `29dd2555…` とも違う（＝改訂4 が書きこみ中）
- → **sha256 の宣言が来るまで、元データを取り込みの入力として読まない**

### 2026-09-19 着任: claude-1d [1b7496]（前任 claude-9e [daea42] は ListAgents に不在）
- 起動時の実測: `git fetch origin master` → **`master` と `origin/master` は一致**（HEAD `63d9495`。ahead/behind なし）
  → 前任が「司令塔が push する」としていた `fd84841`・`bad4386`・`63d9495` などは**すべて origin に入っている**
- 未コミットはコード以外だけ（`tools/mikaku/shots*` の PNG と、ルートの漢字リスト CSV/MD）。index.html・data.js は未変更
- 名簿 `司令塔\セッション名簿\kosukequiz-battle-app.md` を自分に更新した
- 司令塔（`四谷大塚` [0e8455]）に着任を報告。**名指しの指示待ち（CLAUDE.md 0c）**
- 引き継いだ保留: 理科 第4回の取り込み（元データ待ち・`import_rika.py` の FILES に第4回を足す必要あり）
### 2026-09-18 社会 第4回: no.116・117 の note と sol をピンポイントで取り込み（済・未コミット→コミットずみ）
- 依頼: 社会クイズ作成担当（`四谷大塚` [ea158b]）。元データ `5年下\quiz_csv\第4回_平安時代.json`（121問・sha256 959d50c5…）。★丸ごと再取り込みは禁止
- 道具: `tools/patch_g4_sol_note.py`（--check / --apply）。その2行のその欄だけ書きかえ、ほかは1バイトも触らない
- 実測: 変わった行は g4r116・g4r117 の2行だけ／行数1545のまま／ほかの119問は q・a・note とも元データと一致／sha256 は前後で一致／smoke-test 22件／実機で2問とも「解説」が出ることを確認
- ★**見つかった別の問題（未対応・司令塔へ報告）**: **社会の sol は data.js に1件も入っていない**。`tools/import_g.py` の `DROP` に `sol` が入っていて捨てているため
  - 第4回の元データには sol が7問ある（no.98・103・109・112・113・116・117。作成担当が原本と照合ずみ・誤り0件）。今回入れたのは依頼どおり **116・117 の2問だけ**
  - ⚠️ **このままだと、次に第4回を丸ごと取り込んだときに、いま入れた2問の sol も消える**
  - ✅ **司令塔判断で案①を実施**（2026-09-18）: `import_g.py` の DROP から sol を外し、OUT_KEYS の最後に足した。残り5問（g4r98・103・109・112・113）の sol も `patch_g4_sol_note.py --nos` でピンポイントに入れた
    - 実測: 変わったのは5行だけ（欄は sol のみ）・行数1545のまま・第4回は sol 7問
    - ★`import_g.py --check 1 2 3 4` で **全4回とも「増える0・中身が変わる0・消えた0」**＝第1〜3回に影響なし、第4回は元データと完全一致
    - 実機で g4r98・g4r113 の「解説」が出ることを確認。smoke-test 22件
    - ⚠️ 踏んだ罠: パッチの道具の最後の検算が「指定した問題は必ず変わる」前提だったので、**もう同じだった2問を混ぜたら、書いたあとに落ちた**（書きこみ自体は正しかった）。「直す予定だった行」と突き合わせる形に直した

### 2026-09-18 理科の「計算が混ざっている問題」の洗い出し（調査のみ・実装は指示待ち）
- ユーザー原文「理科の問題なんですが、1問1答に計算が混ざっていて、外してほしいです。一問一答では答えられません」
- 理科は全168問（第1回73・第2回17・第3回78）。★**kind はすべて memo**（calc は0件＝取り込んでいない）
- ★該当は**5問、すべて第2回**（てこ・滑車・輪軸）。第1回・第3回は0件
  - A 計算・グラフが要る（一問一答にできない）: **r2m16**（ばねばかりの値と距離の関係のグラフを選ぶ。★「問3のあと」とアプリに無い前問を指す）・**r2m17**（水の重さと台ばかりの値のグラフを選ぶ。★装置の説明が問題文に無い）
  - B 計算問題の一部が残ったもの（問題文の数字を読み返すだけ）: **r2m12**（答え4）・**r2m13**（答え12）
  - C 判断待ち: **r2m15**（定滑車の力。選択肢つき・図が要るが計算はしない。書き直せば残せる）
- 対象外と考えた: 図・グラフの問題21問は**すべて画像つき**／第3回の数字入り（r3m51〜54・68〜71）は答えが「酸性/中性/アルカリ性」「食塩」で暗算20秒以内
- 案: ①消す（記録は消える）②`kind` を calc にして出題から外す（記録は残る・元データとの整合は理科作成担当と分担）③問題文を書き直して一問一答にする（09-12 の長い四択と同じ手口）④絞りこみに「計算を出さない」を足す
- ✅ **ユーザー判断: 5問とも外す**（消さない・書き直さない）。`kind` を memo → calc にした（`fd84841`）
  - 実測: ひとり練習は第2回 17問→12問、5問は出ない。ホームの「全問題数」も12問。対戦・復習ミックス・公平モードも同じ仕組みで外れる
- ✅ **ユーザー判断: 問題一覧からも消す** → `unitIndices` で calc を外した（一覧・単元えらび・公平モードの単元一覧の「◯問」で使っている）
  - ★おまけで、単元の「◯問」が実際に出る数と一致するようになった（第2回 17→12）
  - ⚠️ **引きかえに、この5問の記録は一覧で見られない・直せない。**記録は消えていないので、CSVの書き出し・読み込みでは見られる
- 元データ（quiz_csv_理科 第2回）も同じ印にするかは司令塔から理科作成担当へ。★**丸ごと再取り込みは禁止**（memo が減ると r2m14 以降の id がずれる）→ 司令塔が理科作成担当（オフライン）に依頼ずみ
- ✅ `fd84841`・`bad4386` はユーザー承認ずみ。**司令塔が push する（私は push しない）**

#### 次に来る予定（2026-09-18 時点・まだ着手しない）
- ユーザー指示「社会・理科を第4回まで完璧に」→ **理科 第4回**（data.js にいまは0問）の作成が進行中。できたら取り込み依頼が来る
- 取り込みの段取り: 元データ（`5年下\quiz_csv_理科\`）→ **sha256 を照合** → ピンポイント取り込み
  - ★第4回は**新規の単元で、既存の r4m* が無い**。既存 id を動かす心配が無いので、`tools/import_rika.py --check 4 → --apply 4`（新規追加の経路）が使えるはず。ただし **FILES に第4回のファイル名を足す必要がある**（いまは1〜3回だけ）
  - calc は取り込まない（計算プリント担当の持ち物）。取り込み後は id キーで照合し、実機で数問を目視

### 2026-09-18 対戦がつながらない（中継 TURN を足す＋20秒で知らせる）
- ユーザー報告「対戦が、インターネットにつながっていてもつながらないときはつながらない」「リトライがきいていないのか、少しゲスト側がつなぐのが遅いとつながらない」。実機は2台とも4G/5G、ホストが「相手の参加を待っています…」のまま
- ★調べて分かったこと（trystero 0.25.4 の中身を CDN から読んで確認）
  - `joinRoom({appId}, code)` だけだったので、**つなぎ方の設定は既定のまま＝STUN だけ**（core の既定は `stun.cloudflare.com:3478`）。**中継（TURN）は無し**
  - 2台とも携帯回線だと直接つながれないことが多く（CGNAT・対称NAT）、**部屋は見つかるのに道ができない**→ 待ち続ける
  - ★**部屋の告知は約5.3秒ごとに出し直している**（nostr strategy の定数 5333ms）。だから「ゲストが遅れて入ると見つからない」のは考えにくく、**遅れて入った場合も道ができないほうが疑わしい**
  - Trystero 自身に「TURN を設定せよ」と促す文言があり、`rtcConfig.iceServers` か `turnConfig` で渡せる
- 直したこと: `ROOM_CONFIG`（`appId` ＋ `rtcConfig`）を3か所（作る・再開・入る）で使う。STUN 2つ＋ Open Relay の TURN 3つ（80・443・TLS443）。★直接つながるときは中継は使われない
- 20秒たっても相手が来なければ、「つながりませんでした／同じ Wi-Fi をためす／もう一度つなぐ」を出す（`CONNECT_TIMEOUT_MS`・`create-retry-btn`・`join-retry-btn`）
- ⚠️ **実際につながるかは実機2台（4G/5G）でしか確かめられない**

### 2026-09-16 クラウド側の変更の取り込み（済）
- ★**GitHub の PR #7・#8 がマージされ、origin/master は 1a35194。**クラウドの `01754f9`（1段目を「まだ一度も正解していない」に広げた）も、今朝の3本（スキップ拡大・0.3秒遅れ）も入っている
- 私がいちど cherry-pick した `c2f9bfe` は**二重**だった（index.html・smoke-test.mjs が origin/master と同じ中身）→ 落として、`origin/master + 3d52e7f`（作業メモと tools/mikaku の道具だけ）の形に載せ直した。**force push はしていない**
- 載せ直したあとに確認: コード2ファイルは origin/master と1バイトも違わない／作業メモはクラウド側の記録と私の記録が両方残っている
- ⚠️ 教訓: **クラウドと同時に動くときは、着手前に `git fetch` して origin/master を見る。**PR がマージされていれば cherry-pick は要らない（私は取り込み中に #7・#8 が入って二重を作った）

- ⛔ ~~2026-09-16: index.html・data.js に手を出さない~~ → **解除ずみ（取り込み完了）**。当時の指示（司令塔 claude-35 [eb5585]）:
  ユーザーが GitHub のクラウドセッション `kosukequiz-battle-app_cloud` にも対戦アプリの修正を頼んでいる。取り合いを防ぐため、**次の指示が出るまで編集もコミットもしない。**
  再開するときは、**作業を始める前に `git fetch`（クラウド側の push を取り込む）** から。★私の未push 3本（9da9b3e・f49e82a・6e1889d）が残っているので、取り込んだあとの食いちがいに注意

### 2026-09-16 対戦の押しまちがい対策（司令塔 claude-35 [eb5585] の依頼・仕様の整理「対戦の押しまちがい対策」）
- ユーザー原文「スキップのボタンが小さいそうです。あと、わかったを押した瞬間に画面が切り替わって、間違いを押してしまうことがあります。改善したいです。」
- やること: スキップを大きく／「わかった」の直後に同じ場所を押しても判定が入らない（判定ボタンが出てから1秒は反応しない）／800×1280 の写真
- ユーザー方針「テストの優先度を上げなくていい。言われたことを実装してほしい」→ 壊して鳴る確認はしない。ふだんのテストが通れば十分
- ⚠️ `claude-35` が2つある（司令塔 [eb5585]／漢検アプリ担当 [51e20c]）。宛先は番号つきで
- ✅ 実装・コミットずみ（push していない）
  - スキップ: 高さ68px以上・文字18px（800×1280 で 688×71。前は padding 12px・文字13px）
  - 判定ボタンは `revealAnswer` で出た時刻から `JUDGE_GUARD_MS`（1000ms）のあいだ、手で押しても無視（`judgeGuarded()`）。`.judge-row.guarded` で薄く出す。自動で押されるカウントダウンは効く。revealAnswer はホスト・ゲスト共通なので両方に効く
- ★実測（`tools/mikaku/battle_buttons_probe.mjs`・800×1280 ホスト）: 「わかった！」の中心 (400,637) … 押した直後はチーズのカウントダウン、**二人とも「答えを見る」を押すと判定ボタンの列（✕ 相手はまちがい）がちょうどそこに来る**。ユーザーの症状と合う
  - 直後に✕を押す → 入らない（薄く表示）／1.2秒後に✕ → 入る。smoke-test 21件
- ⚠️ 確かめていないこと:
  - ゲスト側で「直後に押しても入らない」ことは、試していない（同じ関数を通るので効くはず、止まり）
  - **ひとり練習**（ボタンの名前が「〇 わかった」「✕ まちがえた」）: 「〇 わかった」を押すと、その場所に次の問題の「こたえを見る」が来る。同じ場所をもう一度押したあとの位置では、どちらの判定ボタンも見つからず、**「✕ まちがえた」が指の下に来るかは分からなかった**。仕様の対象は対戦なので、ひとり練習は変えていない。司令塔に報告する
- ⚠️ **再開テストが一度落ちた（直した）**: 押しまちがい対策のコミット `9da9b3e` を、再開テストの結果を見る**前に**作ってしまった（並べて走らせたため）。結果は3シナリオが30秒待ちで失敗
  - 原因: テストが判定ボタンの出た瞬間に押していた（ゲスト0秒・ホスト0.3秒）→ アプリが1秒の押しまちがい対策どおり無視 →「つぎの問題へ」が出ず待ち続けた。**アプリの不具合ではなく、テストの押し方が人より速すぎた**
  - 直し方: `test_battle_resume.mjs` の `reveal()` で判定ボタンが出たあと1.1秒待つ。確かめる中身は変えていない → 22件すべて通過。別のコミットにした
  - 教訓: **コミットは、そのコミットに関わるテストの結果を見てから作る。**並べて走らせると、落ちたものを先にコミットしてしまう
- いまの状態: **コミット2本（push していない）。司令塔に報告して待機**

#### 2026-09-16 ユーザー判断で「1秒反応しない」をやめ、「0.3秒遅れて出す」に変えた（コミットずみ・push していない・司令塔に報告ずみ）
- 実測: 「答えを見る」を押した直後は判定ボタンがまだ無く、約325msで出る／出てすぐ✕を押すと入る。smoke-test 21件・再開テスト 22件 通過
- 未push: 9da9b3e（スキップ拡大＋1秒ガード）・f49e82a（テストの待ち）・今回の1本（0.3秒遅れに置きかえ）。いまの状態: **待機**
- ユーザー原文「一秒は長いです。反応しないのはいやなので、0.3秒くらいボタンが出るのが遅いとかがいいです」
- 対象は**対戦だけ**。ひとり練習は変えない。スキップを大きくしたのはそのまま
- `revealAnswer`: 答えはすぐ出し、判定ボタンの列は `JUDGE_SHOW_DELAY_MS`（300ms）後に出す。**判定のカウントダウンは出たときから**（時間は短くならない）。0.3秒のあいだに次の問題へ進んだ・判定ずみなら出さない
- 1秒のガード（`judgeGuarded`・`.judge-row.guarded`）は消した。ボタンは出たらすぐ押せる
- 再開テストの1.1秒待ちを外した（`visible(judge-row)` が0.3秒遅れも待つ）。probe は「押した直後はまだ出ていない／出てすぐ押すと入る」を見る形に直した

### 2026-09-13 着任: claude-3a [ab77cf]（前任 claude-03 は不在）
- 起動時の実測: `master...origin/master [ahead 1]`（749971d・push 未承認）。index.html / data.js は未変更、`tools/mikaku/` の調査用だけ未コミット
- 司令塔 claude-4f に着任を報告。**名指しの指示待ち（CLAUDE.md 0c）**。下の保留はどれも未着手

### 2026-09-13 司令塔の指示（順番どおり）: ①やり直しラウンド途中のリロード再開 ②beforeunload ③749971d を別の目で確認（push しない）
判断待ち（手を出さない）: ⓐ段が尽きたら補うか ⓑランダム順で3段が崩れる件

#### ①の原因（実物で確認・司令塔に案を送って返事待ち）
- ✕ 推測の「`inMissRetryRound` が保存されていない」は外れ。**保存 3186・復元 5483 はある**
- ★**`myMissesThisRound` が保存されていない。**再開すると `[]`（5484）になり:
  - ラウンドの最後に「もう一勝負」が出ない（5865 の `length>0`）
  - ★`endGame` の `saveLastMiss([])` がホームの「前回まちがえた問題」を**空で上書きする**
  - ふつうのラウンドでも同じことが起きる
- ★二重記録の穴: 採点が済んだあと次の問題へ進む前にリロードすると、同じ問題に戻って `recordResult` がもう一度走る（保存が番号だけで「採点ずみか」を持たない）
- 案: 保存に `myMissesThisRound`・`recordedIdx` を足す／採点ずみなら記録を飛ばす／BroadcastChannel で2ページをつなぐスタブで再現テストを作る

#### ①の結果（ローカルにコミット・push はしていない・司令塔は claude-fb に交代）
- ★**原因がもう1つあった**: 再開する経路で `#room-code-display` を埋めていない → 次の保存でコードが「----」になり、**2回目のリロードでゲストのいない部屋に入る。**ユーザーの症状に一番合うのはこれ
- 直したこと: 保存に `misses`・`recordedIdx`・`scoredIdx` を足した／再開時にこの3つとコードを戻す／記録ずみの問題は二重に記録しない／採点ずみの問題には戻らない／まちがいリストを持たない古い保存からの再開では、`saveLastMiss` しない
- テスト: `tools/test_battle_resume.mjs`（node が2つのブラウザ文脈を中継するスタブ。文脈を分けて localStorage も分けている）。**直す前 6件NG → 直したあと 15/15**。smoke-test 15/15
- ⚠️ 自分で見ていないもの: 「----」の検査が直す前に落ちること（その手前で止まっていたため）／古い保存からの再開
- ⚠️ テストで踏んだ罠: **playwright の `click` は、塗りのカウントダウンで動き続けるボタンに対して「止まるまで待つ」で30秒固まる。**DOM の `click()` を直接呼ぶ
- 次: ②beforeunload

#### ②beforeunload（司令塔 claude-fb が承認・実装とテストずみ・push はしていない）
- `index.html` の `disarmBackGuard` のすぐ下に `beforeunload` を1つ置いた。**守る画面は「戻る」と同じ `GUARDED_SCREENS`（対戦・ひとり練習）。**離れる瞬間の画面を見るので、「おわる」「中断」で結果・ホームへ移ったあとは出ない（付け外しの処理は無い）
- ★**ひとり練習も守る対象に入れた**（「戻る」の守りと同じ範囲にそろえた）。対戦だけにしたい場合は条件を1行変えるだけ
- ⚠️ 止められないもの（コードのコメントにも書いた）: 電源／アプリの切りかえ・タスク終了／ブラウザの強制終了／Android Chrome のタブ一覧からのスワイプ／iPhone Safari（確認を出さない）／一度も触っていないページ
- テスト `tools/test_battle_resume.mjs` を **22件**にした（[4] 対戦中は出る・中断のあとは出ない・結果画面では出ない ／ [5] ひとり練習は出る・ホームでは出ない ／ [6] 修正前の古い保存から再開しても「前回まちがえた問題」を上書きしない）
- ★**壊して鳴るかを確かめた**: `tools/break_test_battle_resume.mjs` で5通りに壊し、5通りとも、落ちるべき確認がちゃんと落ちた（「----」の検査も、今回初めて落ちるのを見た）
- ⚠️ 罠: **playwright は、一度も画面を触っていないページでは beforeunload の確認を出さない**（Chrome の決まり）。「出ない」の確認が何を直しても通ってしまうので、リロードの前に `mouse.click` で実際に触る
- ⚠️ 罠: **「前回まちがえた問題」に存在しない id を入れると、ホームを描くときに取りのぞかれる**（3109 `refreshLastMissButton`）。仮の文字でテストすると、修正の有無にかかわらず `[]` になる

#### ★★2026-09-13 私の知らない push があった（司令塔に報告）
- `git reflog show origin/master`: **`14ab7eb` 11:46:07 `update by push`**（＝**このPCのリポジトリから** push されている）
- ★**私（claude-3a）は `git push` を一度も打っていない。**司令塔は「749971d と①は push 未承認」と言っていた
- → **`749971d`（3段）と `14ab7eb`（①）は origin に入っている。**`63d2d31`（②）はまだローカルだけ
- 誰が押したかは未確認（同じPCの別セッション、または VS Code の「同期」の可能性）。**決めつけない**
- ✅ **解決**: 司令塔 claude-fb が、ユーザーの明示の承認（「PUSHお願いします」）を得て push したもの。通知が遅れただけ
- 待ち: ②`63d2d31` の push は、守る範囲（対戦だけ／対戦＋ひとり練習）のユーザー判断待ち。決まったら司令塔から指示が来る
- ✅ `63d2d31` も司令塔が push ずみ（範囲は「対戦＋ひとり練習」で確定）。実機 a〜d は「大丈夫みたい」（b の詳しい結果はまだ）

### 2026-09-13 司令塔 claude-fb の次の指示（順番どおり・push しない・1つ終わるごとに報告）
1. ⓑ ランダム順ON: **3段は崩さず、段の中だけ混ぜる**（選ぶ問題は変えない。出す順番だけ段の中で混ぜる）。トグルOFFの通常側で迷ったら、実装する前に案を送る
2. 一覧の「未解答のみ」→「未実施」に呼び名をそろえる ／ 古いコメント（3725〜3735 付近）を消す
3. 一覧: 押した行だけ描き直す（1.8秒・318pxのずれ）／開いた時点では全部を描かない（5.9秒）
4. フィルターのパネル化
5. 一覧の手動編集の作り直しは案だけ
- 手を出さない: ⓐ（苦手0問のとき補うか）／「あと1回正解で卒業」の一言

#### 1.ⓑ 判断待ち（司令塔 claude-fb に案を送った・実装していない）
- 変える場所: `buildFinalPool` 3769 の `if(currentShuffle) pool = shuffleArray(pool)`（通常側＋ミックスをつないだ全体を混ぜている）
- 案A（推奨）: 選び終わった全体を段ごとに分け、段の中だけ混ぜてつなぐ。トグルON/OFFで分けない（トグルOFFの通常側も段に振り分けるので、未実施がいつも前に来る）
- 案B: ミックスだけを段の中で混ぜて後ろにつなぐ（通常側はいまと同じ。ミックスは最後にまとまる）
- 案C: トグルONのときだけ案A、OFFのときはいまのまま全体を混ぜる
- ✅ **ユーザー判断: 案A**（いつも段の中だけ混ぜる。トグルOFFでも未実施が前に来るのは了承ずみ）
- ✅ **ⓐもユーザー判断: 補う。**「よく間違える」「未クリア」がONで、絞りこんだ問題が問題数に足りないとき、選んだ単元の残りから段の順（未実施→苦手→それ以外・各段は正解日の古い順）で補う。苦手0問でもボタンを押せるようにする。★**トグルの問題が必ず先。**迷ったら（例: 苦手だけONで未実施を先に補うか）実装前に案を送る。「補う仕組みは無い」のコメントも直す
- 順番: 5・7 → ⓑ（案A）→ ⓐ → 1・2 → 3 → 4（案のみ）

#### 2.（5・7）終わり
- 一覧のボタン「未解答のみ」→「**未実施のみ**」（1636）。3段のコメントの説明もそろえた
- ★中身を確かめてから変えた: 一覧の「未」は `statusOf` が苦手でも〇でもない問題（＝一度も答えていない）。「未実施」で合っている
- 古いコメント（「未クリア→苦手の2段」「段ごとにシャッフル」。3725〜3735 にあった）を消した
- コミット `a4bc1ae`（push していない）

#### 3. ⓑ 案A: 実装したが未コミット・司令塔に確認中
- `shuffleWithinTiers` を足し、`buildFinalPool` の最後で `if(currentShuffle) pool = shuffleWithinTiers(pool)`（ミックスの中にあった全体シャッフルは消した）
- ★**ぶつかった点**: `currentShuffle` は**はじめから true**（3265）。そのため「よく間違える」ONの出す順番が、段の中でランダムになる（前は「正解日の古い順・ランダムにしない」がユーザー指定）。smoke-test が4件落ちた
- 司令塔に A1（このまま・テストを書き直す）／A2（トグルONのときは混ぜない）を聞いている。**テストの期待値は、まだ書き換えていない**

#### ★ⓑの結論（最新）: 変更しない
- ★**stash@{0}（ⓑ案A/A2）は適用しない。ランダム順の削除も取り消し**（ユーザー判断: ランダム順はそのまま・OFFなら混ぜない。ユーザーはOFFで使っている）。stash の説明文「ランダム順は削除へ」は古い
- stash@{1}（漢字プリントの WIP）は無関係。触らない
- 点検: ランダム順OFFで動くランダムは、出題の経路では0件（shuffleArray の呼び出し6か所はすべて `if(currentShuffle)`。ほかに乱数を使うのは部屋コードの genCode だけ）
- ⓐは `7cfc6ea` でコミットずみ（push していない）

#### 年表の大きさ（ユーザー依頼「横に隙間はいらない」「横幅はデバイスに合わせて」）: 実装した・未コミット
- 原因は①アプリの枠: `.img-wrap img` の `max-height:220px`（縦長は400px）＋`object-fit:contain` で、**箱の中で絵が縮み、左右に白い帯**が出ていた
- ②画像ファイルの左右の白は年表3枚とも1〜4%で、原因ではない（地図 kai1_28 は10〜16%白。報告だけ）
- 直し方: 幅は枠いっぱい（`width:min(100%, 85svh×縦横比)`・`height:auto`）。縦横比 `--img-ratio` は `setImgWithSize` が入れる。縦長の400pxの規則は消した（`.tall` は一覧でまだ使う）
- 実測（`tools/mikaku/timeline_size_probe.mjs`・直す前は KQ_ROOT に HEAD の index.html を置いて測った）:
  - 390px: kai2_05 絵 228×398・左右43px → **312×545・左右1px**／768px: 左右108px → **1px**
  - 赤丸の位置（絵に対する％）は前後で0.3%以内
  - PC 1280×800: 縦長は画面の高さの85%で止まるので、左右21〜29px残り、絵の下端が画面から約120pxはみ出す（スクロールで見る）
- ⚠️ 罠: **contain の画像は、箱（getBoundingClientRect）を測っても隙間0と出る。**描いた絵の四角を計算して測ること（1回外した）
- 判断待ち: 画面全体の最大幅 520px（`.wrap`）を、タブレット・PCで広げるか（ボタン・文字にも効くので司令塔に聞いた）
- ✅ コミット `49cd11f`（push していない）

#### 画面全体の幅を720pxに（ユーザー判断）: 実装・コミットずみ
- ★**対戦で使っている端末は Android タブレット（Chrome）。**向き（縦・横）はまだ分からない
- 768px以上で `.wrap` と `.selection-bar` を720px（`body .selection-bar` にしたのは、本体の規則が下にあって上書きされるため）
- `tools/mikaku/wide_layout_probe.mjs`: ホーム・一覧・ひとり練習は 390／768／1024／1280 で横スクロールなし。写真は `shots_wide/`
- ★**対戦の画面では、画像の上端がどの大きさでも406px**（題名・中断/おわる・キャラ・得点の箱・単元名）。問題文は画像の下、「こたえを見る」はさらに約250px下
  - Android 縦 800×1280: 縦長の年表は問題文もボタンも隠れる。入れるには画像を画面の44%まで
  - Android 横 1280×800: 3枚とも隠れる。入れるには画像を83px（10%）まで → **上限を下げるだけでは解決しない**
  - ★直す前の版でも、横 1280×800 はもともと全部隠れていた。縦は「ボタンだけ隠れる」→「問題文も隠れる」に悪化
- 司令塔に案①問題文を画像の上に／②横向きは左右に並べる／③キャラと得点を縮める／④縦長だけ縦向きで44%上限、を送った。**向き次第（縦なら④、横なら②を推奨）。まだ直していない**
- 次: 「1・2（一覧）」
- ✅ コミット `f828fc5`（push していない）

#### 一覧 1・2（押した行だけ描き直す／開いた時点で全部を描かない）: 実装した・未コミット
- ★**ずれの原因**: 〇✕・正解数・日付を1つ変えるたびに `renderListItems()` で全行を作り直していた。画面の外の行は `content-visibility` の仮置き400pxに戻るので、押した行が318pxずれた
- 直し方:
  - 行を作る部分を `buildListRow(i)` に分け、`renderListRow(qid)` でその行だけ差しかえる（〇✕・＋−・日付・文章を直す・キャンセル・保存・元にもどす）
  - ★絞りこみの条件から外れた行も、その場では消さない（押した指の下から行が消えないように）。絞りこみを押し直すと反映される。**ここはユーザーに確認していない。司令塔に報告する**
  - `renderListItems` は最初の20行だけすぐ描き、残りは60行ずつ `setTimeout` で足す。途中で描き直しが始まったら `listRenderGen` で古い続きを止める
  - 編集中の行は `data-edit-qid`（`data-qid` は振り分けの対象外にしたいので付けない、は前のまま）
- 実測（CPU 4倍遅く・`tools/mikaku/jump3.mjs`／`list_perf.mjs`。直す前は HEAD の index.html を一時フォルダに置いて測った）:
  - ずれ 318px → **0px**（高さが変わった行 7件 → 0件）
  - 1件編集したときの描き直し 797ms → **44ms**
  - 最初の行が出るまで 3455ms → 530〜1733ms
  - ★ページ内で測り直した値（800×1280・1377行）: 最初の行 1492ms → **23ms**／⚠️**全行そろうまで 1493ms → 3416ms に延びた**（60行ずつ足すあいだにレイアウトが挟まるため）。司令塔に報告ずみ。気になるなら1回に足す行を増やす
- ✅ コミット `2666b85`（push していない）
- smoke-test に1件足した（全18件通過）。★2通りに壊して鳴ることを確かめた（「続きを止めない」→ 0行のはずが1117行／「＋で全体を作り直す」→ となりの行が作り直される）
- ⚠️ 罠: **`Start-Job` の出力は文字化けする**（CP932）。NG の行数と「期待/実際」の数字で読んだ
- ⚠️ 罠: PowerShell のヒアドキュメントに `"/index.html"` を含むと、コマンド全体が安全確認で止められた。測定の道具はファイルに書いてから走らせる
- 確認待ち（司令塔）: 絞りこみから外れた行をその場で消さない、でよいか

#### 年表: 縦向きのタブレットで問題文とボタンが入るようにした（ユーザー回答「タブレットは縦向き」→ 案④）
- `@media (orientation:portrait)` で `.img-wrap img.tall` の幅を `min(100%, 42svh×縦横比)` にした（44%がぎりぎりだったので42%）。横向き・横長の画像は「幅いっぱい」のまま
- 実測（`wide_layout_probe.mjs`・対戦のホスト）: **800×1280 で年表3枚とも問題文○・ボタン○**（ボタンの下端 1245／画面1280）
  - 390×844（スマホ）: 縦長2枚は絵が約203px幅・左右に約55pxの背景。問題文もボタンも隠れる（入れるには11%まで必要。スマホで対戦はしないので、このまま）
  - 768×1024・820×1180: 問題文○・ボタン✕（30%・39%まで要る）。ユーザーの端末ではないのでこのまま
  - 横向き 1280×800: 変わらず全部隠れる（変更の対象外）
- 赤丸の位置（絵に対する％）は前と0.1〜0.2%以内。smoke-test 18件すべて通過
- 司令塔の判断: 一覧の「全行がそろうまで 3.4秒」はこのままでよい

#### ✅ 3 フィルターのパネル化: 案Aで実装・コミットずみ（push していない）
- ユーザー判断: 案A（B・Cはやらない）。絞りこみから外れた行をその場で消さないのも、今のままでOK
- 単元の選択と「絞りこみ ▾（N件）」を1行に（`#list-filter-open`）。中身は `#list-filter-panel`（`hidden`）。閉じているときは `#list-filter-summary` に条件を1行
- 開閉は `kq_battle_list_filter_open_v1` に保存。はじめは閉じている。768px以上は優先度と難易度を横並び（`.list-meta-cols`）
- ボタンと要約は `refreshListFilterSummary()`。★絞りこみを変えると必ず `renderListItems` を通るので、その先頭で呼んでいる
- smoke-test 19件通過。壊し方2通り（開閉を覚えない／件数を出さない）でそれぞれ落ちることを確認
- 写真: `shots_wide/after_*_list.png`・`after_*_list_open.png`。スマホで閉じると1問目が約190pxから（前は絞りこみだけで約350px）

#### ★★正解数＝連続正解数（ユーザー判断・2026-09-13）: 段階Aを実装中・未コミット
- ユーザーの選択は「正解数そのものを連続正解数にする」（まちがえると0。累計で苦手を決める 09-06 の逆戻りではない）。苦手の判定・nextDue の仕組みは変えない
- ★**保存データは書きかえない。**box はふだん答えるたびに保存ずみ。読む値を変えるだけ。累計の correct は残すが使わない
- 段階A（済・テスト前）:
  - `isMastered` → `box>0`（ホームの未クリア・未クリアのトグル・一覧の未クリア・状態・CSVの状態に効く）
  - `adjustStat`（ひとり練習の＋−と「ひとつ前にもどる」）: 正解数の＋−は box（0〜7）。＋は累計と正解日も進める。誤答＋で box を0。nextDue を決め直す。box0・誤答0で記録を消す
  - ひとり練習と一覧の「正解数」の表示を box に
  - CSV: 「正解した回数」は累計のまま（元アプリ・古いファイルと互換）。最後に「連続正解数」列を足した。読み込みは**既存の記録の box を触らない**（古いファイルで連続が上がって苦手が消えないように）。新しくできる記録だけ列の値を使う
  - ⚠️ ひとり練習の「ひとつ前にもどる」で✕を取り消しても、切れる前の連続は戻らない（前からそう）
- 司令塔に確認中: 一覧の「未／〇／✕」ボタンを残すか消すか（おすすめは消す）。**返事で一覧の行の作りが変わるので、案2のフォームはまだ作っていない**
- ✅ 段階Aはコミットずみ（push していない）。smoke-test に「正解数＝連続正解数」の1件（全20件通過）
  - ★**コミットずみの版に同じ検査をかけると4件落ちた**（ホームの正解済み数が 1 ではなく 3）＝検査が効いている
  - 例（ユーザーに伝える用）: ①累計5回正解→まちがえ（連続0）②3回続けて正解 ③1回正解→まちがえ ④未実施 → **正解済み数 3→1、未クリア 1→3、苦手 2→2**
- 司令塔に報告ずみ: CSV の扱い／例の数字／「ひとつ前にもどる」で切れた連続は戻らない（前から）
- 次: 〇✕ボタンの返事が来たら案2（記録を直す: 正解数＝連続／誤答数／最後に正解した日／最後に解いた日、保存まで変えない）
- ⚠️ 前から残っている（今回は直さない・司令塔判断）: ひとり練習の「ひとつ前にもどる」で✕を取り消しても、切れる前の連続正解数は戻らない

#### 案2「記録を直す」: 実装した・未コミット（ユーザー判断: 〇✕ボタンは今の動きのまま残す）
- 行の＋−と日付の欄をやめ、`recordBlockHtml` で「正解数（連続）n・誤答数 n・最後に正解 …・最後に解いた …」の1行に。「✎ 記録を直す」で入力欄4つが開く
- ★**「保存する」を押したときだけ `saveRecordEdit` が書く。**キャンセルは何も書かない。連続は0〜7の外なら保存しない
- 累計の correct は連続より小さくしない／連続を変えたときだけ nextDue を決め直す／4つとも空なら記録を消す（未実施）
- `setStatDate` と行の＋−（一覧側）は消した。ひとり練習の＋−（`adjustStat`）は残っている
- 開いている行は `recordEditingId`（1行だけ）。文章を直す `editingId` とは別
- smoke-test: 日付の検査を「記録を直す」の検査に書きかえた（保存まで変わらない／キャンセル／範囲外／4つ保存）。自分の一覧の検査も＋1ではなく〇ボタンに。20件通過。test_battle_resume 22件通過
- `tools/mikaku/jump3.mjs` も〇ボタンに直した（消えた＋ボタンを押していた）。ずれ0pxのまま
- ★**写真で見つけて直した**: 開いた形で「保存する」のすぐ下に「✕ 不正解」が並んでいた（少し下を押すと記録がまるごと置きかわる）→ **「記録を直す」を開いている行だけ〇✕ボタンを出さない**。司令塔に「指示に足したこと」として報告ずみ
- 入力欄に枠を付けた。★前から: `.list-stat-date input` の `--line`・`--card` は定義されておらず、以前の日付の欄も枠が見えていなかった
- 壊し方4通り（範囲外でも保存／次に出す日を決め直さない／累計をそろえない／開いても〇✕を出す）がそれぞれ落ちることを確認
- ✅ コミットずみ（push していない）。未push: パネル化 `c8e7537`・正解数＝連続 `df4b35e`・記録を直す
- いまの状態: **司令塔 claude-fb が「今回の作業はすべて完了」と確認（2026-09-13）。待機中（CLAUDE.md 0c・新しい作業は探さない）**。公開（push）はユーザー承認のうえ司令塔が行う

### ★★2026-09-13 夕: 公開後にユーザー「社会はだいぶ意図と違います」→ 直した（コミットずみ・push していない）
★**仕様の唯一の正は `司令塔\回答\対戦アプリ_仕様の整理_2026-09-13.md`**（メッセージ・依頼一覧と食い違ったらこちら）
- 1 **メイン画面**の「出題タイプ・優先度・難易度」を「絞りこみ ▾」でたたむ（`#setup-filter-open`・`kq_battle_setup_filter_open_v1`・`refreshSetupFilterSummary` は `refreshCreateButton` から）。★c8e7537 は一覧のほうをたたんでいた＝**取り違え**
- 2 一覧の絞りこみはたたまない形に戻した（今日の前と同じ）
- 3 一覧は `listHasCondition()`（単元・状態・解いた日）が無いと何も描かず「単元か絞りこみを選んでください。」。★優先度・難易度だけでは出さない（司令塔に一報ずみ）
- 4 一覧の行の上で ＋−（正解数は連続＝box）と2つの日付を直す形に戻した。「記録を直す」（a6ae483）は外した。`setStatDate` は box を見る形で復活。日付の欄の枠（未定義 `--line`・`--card`）を直した
- 5 画像を高さで縮める決まりを全部外した（対戦・ひとり練習の85%と縦向き42%、一覧の160/280px）。すべての画面で幅いっぱい（ユーザー原文「なんで年表は横に余白を作って縮小してるんですか？大きく出してほしいです」）
- 検査: smoke-test 20件／test_battle_resume 22件／jump3 ずれ0px／赤丸0.2%以内。写真 `tools/mikaku/shots_wide/before_*`（公開中の版）・`after_*`
- ⚠️ 教訓: **「どの画面の話か」を確かめずに作った**（メイン画面の依頼を一覧に入れた）。案を送るときは画面名と、いまの見た目を並べて確認すること
- いまの状態: **待機（司令塔に完了を報告ずみ）**
- ✅ e37dffd はユーザー承認のうえ司令塔が push する（私は push しない）
- 次の予告（まだ着手しない）: 理科第3回の「すべて答えなさい」6問を横ぐしの1問ずつに切り直したJSONが、理科作成担当から **sha256 つき**で届いたら取り込む（依頼書 `司令塔\回答\理科第3回_水溶液の横ぐし問題_依頼_2026-09-13.md`）。★届くまで待機。ほかの回・教科には手を付けない。取り込むときは `tools/import_rika.py`・C-4c-2（sha256 照合）・calc は入れない

### 2026-09-13 夜: トップ画面の並べ替え（コミットずみ・push していない・司令塔に報告ずみ）＋ 理科第3回の取り込み（新しい sha256 待ち）
- トップ画面: 仕様の整理「トップ画面の並び順」どおり。id はすべて前のまま。公平モード・チェックのみは `.check-toggle`（見た目だけチェック）。区切り `hr.home-divider` 3本。復習ミックスの絞りこみ（`review-filter-*`・鍵 `kq_battle_review_filter_open_v1`）と二人対戦の時間・出題順（`battle-settings-*`・鍵 `kq_battle_battle_settings_open_v1`）を `makeFoldPanel` でたたむ
  - ★時間設定は `row.nextElementSibling` で自由入力の欄を探すので、行と欄を離さないこと（HTML にコメントあり）
  - smoke-test 21件（並び順の検査を追加・`#order-toggle` はパネルの中なので DOM click に）／再開テスト22件
- ★理科第3回: 削除は**ユーザー了承ずみ**（原文「こういうきき方ですでに知識を確認できている問題は削除してください」「においの個別の問題も削除してください」）
  - 削除は**13個**: r3m04・05・06・08・15・35・36・37・39・55・56・57・**58**。残り（r3m59・63・75 ほか）の id は1つも動かさない。新11問を r3m81〜91 で末尾に
  - ★`import_rika.py` の丸ごと入れ直しは使わない（memo の並び順で id を振り直すので、途中を消すと r3m04 以降が全部ずれる）
  - ★**いま届いている sha256 376c914a… は古い。取り込まない。**作成担当が「r3m58 削除・⑤においを あり／なし」に直した新しい sha256 を待つ
  - 取り込み後: id キーで「消えたのは13個だけ」「残りの q・a は同じ」を照合（B-8）／新11問を対戦とひとり練習で出して写真／note 3件（no.24・27・60）も入れる
  - ✅ 新しい sha256 29dd2555…（75406バイト）が届き、一致を確認。道具は `tools/import_rika3_yokogushi.py`（ピンポイント。--check で計画を出してから --apply）
  - ★**note 3件は、書きかえる必要がなかった。**data.js の r3m24（no.24）・r3m27（no.27）・r3m59（no.60）の note は、元データと1文字も違わなかった（読んで確認）。作成担当の「元データのほうが新しい」は古い見立て。司令塔からも「差分0件と聞いている、なぜ違うのか書け」と聞かれていた → **違いは無かった、と報告する**
  - 残す数は **67問**（80−13）。司令塔の「65問」は書き間違いと確認ずみ
  - ✅ **取り込みずみ（未コミット）**: `--verify`（data.js.bak＝HEAD と書いたあとを比べる）で **1547→1545問、消えた id は13個だけ、増えたのは r3m81〜91、残した1534問は1バイトも変わらず並び順も同じ、note の書きかえ0件、id重複0**。r3m は78問
  - ⚠️ 罠: **`--apply` の書いたあとの照合が JSON 読み込みで落ちた**（data.js の古い行に「最後のキーのあとのカンマ」がある）。書きこみは済んでいた。★落ちても書いたかどうかを決めつけず、`git hash-object data.js.bak` と HEAD の一致・r3m の数を見てから `--verify` で照合し直した。道具は直した（カンマを外して読む／照合は行の文字列で比べる）
  - smoke-test 21件／test_battle_resume 22件 通過。ひとり練習で r3m81・r3m88 を出して、①〜⑤の答えが2行で読めることを写真で確認（`tools/mikaku/shots/r3m81.png`）
  - ⚠️ 罠: `wide_layout_probe.mjs` の対戦の画面は「画像の読み込み」を待っていたので、**画像の無い問題では「対戦の画面が出なかった」と誤って出る**。直した
  - ✅ 対戦の画面でも r3m81・r3m88 が出る（800×1280 は問題文とボタンが1画面／390 はボタンの下端が1px はみ出す＝画像の無い長い問題文のため）。写真 `tools/mikaku/shots_wide/after_battle_*_r3m8*.png`
  - ✅ **コミットずみ（push していない）**。いまの状態: 司令塔に報告して待機
  - ✅ 理科作成担当も独立に照合して差分0件。ユーザーが公開を承認 → **司令塔が push する（私は push しない）**
  - 題名「社会 一問一答」が理科でも出る件は、司令塔からユーザーに伝えてある。**指示が出るまで触らない**
  - いまの状態: **依頼はすべて完了。待機（CLAUDE.md 0c・新しい作業は探さない）**

### 2026-09-13 夜: 題名を「恒介クイズ」に（ユーザー原文「題名は恒介クイズにしてください。」）
- 変えた: `<title>`（12行）と画面上の題名 `.brand`（1495行）。下の小さい「早押し対戦」はそのまま
- ★変えていない（司令塔の指示）: CSV のファイル名 `社会一問一答_対戦記録_…`（4875行付近）・`社会一問一答_直した問題_…`（4907行付近）
- ✅ smoke-test 21件通過／390px でホーム・一覧・ひとり練習・対戦の題名を写真で確認（`tools/mikaku/shots_wide/after_390x844_*`）／コミットずみ（push していない）／司令塔に報告ずみ
- いまの状態: **待機**（push は司令塔がユーザー承認のうえで行う）

#### 4 手動編集: ユーザー判断は案2（「記録を直す」を開いて保存まで変えない）。★実装はまだ
- ユーザーの回答「今の正解数をそのまま連続正解数として使いたい」の意味を司令塔が確認中。**確認が来るまで入らない**
- 司令塔に回答ずみ: 正解数を box として使うと ①苦手判定（累計は減らないので苦手に入らなくなる・09-06に直した型の逆戻り）②次に出す日（まちがえても縮まない）③ふだんの記録（まちがいで連続が切れなくなる）の3つ全部に効く。「画面に box の欄を出さない」という意味なら仕組みは変えずに済む

#### （済・記録）3 フィルターのパネル化: 仕様が無いので案を送った
- 資料は「フィルターのパネル化」の一言だけ。一覧の上の絞りこみ（単元・状態3・優先度5・難易度5・解いた日）が約350px使っている（index.html 1645〜1677）
- 案A（推奨）: 単元＋「絞りこみ ▾（n件）」の1行にまとめ、押すと開く。閉じても条件を1行で出す／案B: 下から出る引き出し／案C: 1行を画面の上に貼りつける（Aに足す）
- 返事待ちのあいだに「4 手動編集の作り直し（案のみ）」を書く

#### 4 手動編集の作り直し: 案を書いた（`案_一覧の手動編集の作り直し_2026-09-13.md`・実装しない）
- ★〇✕ボタン（`setStatus`）は記録をまるごと置きかえる／＋−（`adjustStat`）は box・次に出す日を動かさず、`lastAnswered` を今日にする／box は一覧に出ていない
- おすすめ: 案1（〇✕を `recordResult` にして記録を残す）＋案3（直前を1回戻せる）。案2（「記録を直す」を開いて保存まで変えない）は親がよく直すなら
- ★**訂正**: 前に書いた「正解・誤答を両方0にすると記録が残り3段目」は誤り。`adjustStat` は両方0で記録を消す。3段目に回るのは日付だけ残った記録（`setStatDate`）
- いまの状態: **3（パネル化）の案の返事待ちで止まっている**

#### （以下は途中経過・取り消しずみ）ⓑ は取り消し → 「ランダム順」を削除する（ユーザー判断「ランダムにする機能は要らない」）
- 案A/A2 の変更（`shuffleWithinTiers`・smoke-test の1件）は **`git stash` に退避**した（消していない）。壊して鳴ることは確かめてあった
- やること（ⓐのあと）: ランダム順ボタンを消す／混ぜる処理をやめる（絞りこみなしは data.js の番号順、トグルON・ミックスは3段、ミックスは通常側のあとにつなぐ）／科目別の保存に残った `shuffle:true` を無視する／`shuffleArray` の使い道を洗い、**出題順以外の使い道は消す前に一覧で報告**／消える・書き換える検査は何を守っていたか添えて報告／ユーザー原文コメントの近くに1行
- 順番: ⓐ → ランダム順の削除 → 1・2 → 3 → 4案

#### 4. ⓐ 補う: 決定ずみ（①数を決めたら補う ②「全部」は案X: 0問のときだけ残り全部を段の順で）
- ★出題数の初期値は「全部」（3263 `questionCount = "all"`）。「足りないとき補う」は数を決めたときしか意味がない
- 案: 数を決めたとき → トグルの問題を先に、足りない分を選んだ単元の残りから段の順で補う（種別・優先度・レベルの絞りこみは補う側にもかける）
- 「全部」のとき → 案X（推奨: 0問のときだけ残り全部を段の順で）／案Y（補わない）を司令塔に聞いた
- スタートボタンの数（`refreshCreateButton` 3916 は `buildPool` しか見ていない）も補った数にそろえる必要がある
- 開始する経路は `buildFinalPool` を呼ぶ2か所（4743 ひとり・5480 対戦）

### ★★2026-09-16 出題順の1段目を「まだ一度も正解していない」に広げた（★master 反映ずみ・配信ずみ / PR #7・266a240）

**ユーザー報告:**「未クリアでフィルタして、今日やった。単元は一つ。
一覧画面で単元の1問目が未クリアなのに出題されなかった」

**原因は仕様の穴で、コードのバグではありませんでした。**
09-13 の1段目は「未実施＝記録が1件も無い」だけでした。そのため
**✕がついていて一度も正解していない問題は2段目に落ち、手つかずの問題すべての後ろ**に回ります。
単元1つ＋「未クリアの問題」ONで出題数を10問にすると、その問題は永久に出てきません。

実測（`tools/mikaku` と同じやり方で本物のブラウザで確認）:

```
仕込み: 第1回 88問すべて未クリア。うち g1r1 だけ ✕3回・正解0回。残り87問は手つかず
直す前: 10問 → g1r2,g1r3,…  ❌ g1r1 は出題されない ／ 全部 → 88問目（最後）
直した後: 10問 → g1r1,g1r2,… ✅ g1r1 が1問目
```

**ユーザーの言葉:**「一度も答えていない、と一度も正解していない、は、同じ扱いでいいと思ってました」

```
1段目 まだ一度も正解していない（未実施 ＋ 答えたが未正解）   ← ここを広げた
2段目 一度は正解した ∧ 間違えた ∧ まだ2連続正解していない    ← そのぶん狭くなった
3段目 それ以外
各段の中は 最後に正解した日が古い順
```

★**段の中の並びは元から `lastCorrectOf`（最後に正解した日。無ければ0＝いちばん古い）**なので、
この2つは**並べ替えの基準の上ではもともと同じ値**でした。段の判定だけがずれていた、という形です。
1段目は全員が0で同点なので、安定ソートで **data.js の並び＝一覧に見えている並び**になります。
「一覧の1問目がそのまま1問目に出る」のはこのためです。

- 直した場所: `index.html` の `tierOf` 1行（`if(!s)` → `if(!s || (!s.lastCorrectAt && !((s.correct||0) > 0)))`）
- `correct` も見るのは、`lastCorrectAt` を入れる前の記録や CSV インポートの記録に
  「正解しているのに正解日が無い」ものがあるため（`backfillLastCorrectAt` は `lastAnswered` が
  無いと届かないので、そこを取りこぼさない）
- テスト: `tools/smoke-test.mjs` に1件追加（22件に増えた）。
  ★**旧判定に戻して鳴ることを確認ずみ**（4件NG・`s19` が出ず `s20` が1問目になる＝報告そのもの）
  ★仕込みは「並べ替えを壊すと鳴る」ように、未正解を**先頭と途中**に、2段目を**先頭寄り**に置いた

#### ★次の担当へ: まだ直していない、確かな不具合が2つあります（別件・未着手）

1. **`index.html` の `buildFinalPool`** … 復習ミックスが1問でも入ると
   `if(currentShuffle) pool = shuffleArray(pool)` が**メイン側の3段の並びごと混ぜ直します。**
   すぐ上のコメントの「ランダムにはしない（ユーザー指定）」と食いちがっている。
   ★「ランダム順」は初期値でONなので、復習ミックスを使うと必ず踏みます。
   実測: 苦手+未クリアON・復習5問 → `r1 2 1 2 2 2 r1 r1 r1 1 …` と段が崩壊
   → **ユーザーは「ランダム順のときはどうでもいい」と判断ずみ。**直すなら出題順どおり側だけでよい
2. **`index.html` の公平モード（ゲスト参加時）** … `questionIds.concat(extraIds)` のあと
   同じ `shuffleArray` で**対戦開始前に作った並びを混ぜ直し**、足す問題を3段に通していない

#### ★ついでに分かった、出題順どおりの単元の並び（未判断・ユーザーに保留された）

「出題順どおり」は data.js の物理的な並びをそのまま出すので、**画面の単元の並びと一致しません。**
- `simg1`(公民5)・`simg2`(公民7)・`simg3`(公民8) が data.js で公民8の後ろにあり、
  複数単元を選ぶと**その単元のかたまりから離れて1問だけ後に出ます**
- **`第4回.平安時代` が data.js の末尾**なので、社会を全部選ぶと夏期講習8のあとに来ます
- 単元の**中**の並びは data.js 順で正しい（`kaki1_94` などの「追加分」は元の問にとなり合うよう
  意図して差しこまれている）。**崩してよいのは単元と単元の順番だけ**

### ★★2026-09-13 出題順を3段にした（未コミット→コミットずみ・push は未承認）

**ユーザーが確定させた仕様。**`index.html` の `tierOf` / `orderByTiers` に、
**ユーザーの原文つき**でコメントを置いた（順序は取り違えが続いたため）。

```
1段目 未実施（記録が1件も無い）          ← 優先度 最高
2段目 一度でも間違えた ∧ まだ2回連続正解していない（＝苦手）
3段目 それ以外
各段の中は「最後に正解した日」の古い順
```

#### ★実装して分かった、**仕様の読み方に関わる大事なこと**

⚠️ **3段すべてが実際に並ぶのは「復習ミックス」だけです。**
トグルが**先に**絞りこむため、メイン側には3段目が残りません。

| 設定 | 対象に残る段 |
|---|---|
| 「よく間違える」だけON | **2段目のみ**（`isWeak` で絞るため） |
| 「未クリアの問題」だけON | **1段目と2段目**（`correct` 0 で絞るため） |
| 両方ON（OR） | **1段目と2段目** |
| 復習ミックス | ★**1段目・2段目・3段目すべて**（`buildReviewPool` は絞りこまない） |

**実測（`tools/mikaku/tier_order_probe.mjs`・本物のChrome・390px）:**
```
経路1 メイン側（両方ON）    1段目28問 / 2段目27問 / 3段目 0問  ← 3段目は対象外
経路2 復習ミックス          1段目28問 / 2段目27問 / 3段目27問  ← 全3段が並ぶ
```

⚠️ ★**「対象が尽きたら次の段から補う」仕組みは、まだありません。**
苦手が0問になると、ボタンが「問題がありません」で**押せなくなります**
（`tools/mikaku/empty_probe.mjs` で実測）。**受け皿を作るかは未判断・司令塔へ報告ずみ。**
★**2026-09-13 に私はコードのコメントに「尽きたら3段目が受け皿になる」と書きましたが、
コードはそうしていませんでした。**テストを書いていて自分で気づき、コメントを直した。
**「こう動くはず」を確かめずにコメントに書くと、次の担当がそれを前提に組みます。**

⚠️ ★**「ランダム順」がONで復習ミックスを使うと、3段の並びが最後に崩れます。**
`buildFinalPool` の末尾に `if(currentShuffle) pool = shuffleArray(pool)` があり、
**メイン側の3段の並びまで混ぜ直します。**トグルON時はメイン側だけなら混ざりません。
→ **これは仕様かバグか未判断。司令塔に確認中。**

#### テスト（`tools/smoke-test.mjs`・15件に増えた）
- 「出題順の3段（復習ミックス経路・全3段が順に出る）」… 各段が出る／段の中が正解日の
  古い順／段をまたいで混ざらない／**1段目が尽きたら2段目へ行く**
- 「出題順の3段（よく間違える と 復習ミックス で同じ並びになる）」…
  ★**3段目を作らない仕込み**にしてトグルで1問も落ちない状態を作り、**同じ集合**で
  両経路の並びを直接くらべる（ふだんは集合が違うのでくらべられない）

★**5通りに壊して、全部が鳴ることを確かめた**（`置換が当たったこと自体を assert` してから実行）:
並べ替えを丸ごとやめる／段の中の並べ替えをやめる／段の順を逆にする／
苦手判定から `box` の条件を外す／復習ミックスだけ並べ替えをやめる。

⚠️ ★**テストの仕込みでやってはいけないこと（2回踏んだ）**
1. **段を data.js の並び順どおりに仕込むと、並べ替えを壊してもテストが通ります。**
   → **とびとびに割りあて、各段の中の日付を data.js の並びと逆**にする
2. **3段目を `wrong: 0` だけで作ると、苦手判定から `box` を外しても鳴りません。**
   → ★**「一度間違えたが2連続正解して卒業した」（`wrong>0` かつ `box>=2`）を混ぜる。**
   ここが `box<=1` の境目そのもの

### ★★2026-09-13 次の担当が踏むところ（実際に踏んだもの）

- ★**トグルと出題数は科目別に保存されます。**リロード後に「クリックする」と、
  ONではなく**OFFになります。**テストは必ず「この状態にする」（今の状態を見て、
  ちがっていたら押す）と書くこと。**「押す」と書いて3回はまりました**
- ★**アコーディオンも「開く」ではなく「開いた状態にする」。**2回押すと閉じて、
  次の単元クリックが「要素が見えない」で30秒待って落ちます
- ★**復習側のアコーディオンは `data-review-group`、メイン側は `data-group`。**
  別属性です（`querySelector` の取り違えを防ぐ作り）。取り違えて落ちました
- ★**`[...a].sort().join()` は引数なしなのでカンマ区切りになります。**
  `join("")` と比べると必ず不一致になり、**検査そのものが壊れているのにアプリを疑いました**

### ★★2026-09-13 似た名前で中身がちがう2つ（取り違え注意）

| どこ | 表示 | 実際の条件 |
|---|---|---|
| ホーム | 「未クリアの問題」 | **`correct` が 0**（★まちがえた問題も入る） |
| 問題一覧 | 「未解答のみ」 | **記録が1件も無い**（★一度も答えていない） |
| 出題順 | 「1段目・未実施」 | **記録が1件も無い**（＝一覧側と同じ） |

★**候補の直し方: 一覧側の呼び名を「未実施」にそろえる。**ホーム側は `isMastered`
（`correct > 0`）を使っており、こちらは名前を変えるほうが影響が小さい。**未着手。**

### ★★2026-09-13 ユーザー判断: お子さんが見る画面に、進み具合の数字を出さない

> **「子供には見せなくていいです。気になって怒ったりするので、めんどくさいです」**

★**理由＝気になって本来の目的から外れ、実際に怒るため。**
**漢検アプリ 09-09 の「切りかえ画面に正解数を出さない（C案）」と同じ趣旨**
（`司令塔\依頼一覧.md` の完了記録）。**今回はさらに「怒る」という実害が出ている。**

⚠️ **書いておかないと、次の担当が「あと何回で卒業か見えたほうが親切では」と足します。**
★**2026-09-13 に司令塔が実際にその案を出し、この判断で取り下げました。**

**あわせて前提（ユーザー確認ずみ）:**
```
★お子さんが触るのは「対戦」の画面だけ
★問題一覧・設定は「親御さんが見る画面」
```
→ **一覧に数字を出すのは問題ありません。**隠す細工も不要。**出さないのは対戦の画面だけ。**
→ ⚠️ ただし**いま足す必要もありません**（ユーザーの問いは「正解数って使ってます？」で、
　 表示を増やす依頼ではなかった）。

★**`refreshSoloCountRow`（ひとり練習の出題中）には、いま正解数・誤答数が出ています。**
ひとり練習は親が使う画面という整理なので**そのままにしてあります。**対戦側には出ていません。

### ★★2026-09-13 どの値が何を決めているか（司令塔 fba1985d への回答・調査ずみ）

| 値 | 一覧に出ているか | 何を決めているか |
|---|---|---|
| `correct` 正解数 | ★**出ている**（増減できる） | `isMastered`（`correct>0`）→ **ホームの「未クリア」数・「未クリアの問題」トグル・一覧の「未解答のみ」絞りこみ・行の〇✕表示・CSVの「状態」欄**。★**3段の並び順には使っていない** |
| `wrong` 誤答数 | ★**出ている**（増減できる） | `isWeak` の前半 → **「よく間違える」トグル・2段目の判定・CSVの「状態」欄** |
| `box` 連続正解数 | ⚠️ ★**出ていない** | `isWeak` の後半（`box<=1`）→ **2段目の判定**。かつ `nextDue = 今 + BOX_INTERVAL_DAYS[box]` → ★**次にいつ出すか** |
| `nextDue` 次に出す日 | ⚠️ ★**出ていない** | `isDue` → **復習ミックスの対象かどうか** |
| `lastCorrectAt` 最後に正解した日 | ★**出ている**（日付で編集できる） | ★**3段すべての中の並び順**（古い順） |
| `lastAnswered` 最後に解いた日 | ★**出ている**（日付で編集できる） | **一覧の日付での絞りこみ**のみ |

★**ユーザーの指摘は当たっています。**
**`box` と `nextDue` は一覧に出ていないのに、苦手判定と出題間隔の両方を決めています。**
→ **「なぜこの問題が出ているのか」が一覧を見ても説明できない状態です。**
→ **私の見立て: 数字（0/2）より「あと1回正解すれば卒業」のような一言のほうがよい。**
　 ただし**上の判断どおり、対戦の画面には出さない。**一覧だけ。**実装は未着手・要判断。**

⚠️ ★**一覧に出ているのに判定に使っていない値は `lastAnswered` だけ**（絞りこみ専用）。

### ★★2026-09-13 記録が壊れうる入口（報告のみ・直していない）

1. ★**CSV取り込みは `Math.max` で「増える方向にしか動かない」**（`index.html` 4664付近）。
   **古いCSVを読ませても減らないので、取り違えを取り消せません。**
   ★ただし **`box`・`nextDue` はCSVに列が無く、触られません**（`4666` のコメントどおり）。
   → **「古いファイルで box が上がって早く卒業してしまう」心配は起きません。**確認ずみ
2. ★**一覧で「最後に正解した日」を直接編集できます。**ユーザー自身の依頼による機能ですが、
   ★**3段すべての並び順を決める値**なので、ここを触ると出題順が変わります

### ★★2026-09-13 意図せぬ中断（司令塔 → 調査中・未実装）

★**`beforeunload` は `index.html` に 0件です。**入れたのは `popstate` だけ。
→ ★**リフレッシュ・タブを閉じる・URL移動は、確認なしで通り抜けます。**
（`visibilitychange`・`pagehide`・`unload` も 0件）
**次にやること: 経路を洗って、止められるものと止められないものを分けて報告する。**

### ★★2026-09-12 配信完了（公開URLで反映を目視した）

**ユーザーが push を許可してくれた。**`b16e347..50e13a1`（コミット10本）。
**公開URL（`https://nambanamba.github.io/kosukequiz-battle/`）を実際に取得して中身を確認ずみ。**

| 中身 | 問数 |
|---|---|
| 長い四択→一問一答（前任 claude-dc 作・型1〜型4） | 23 |
| 理科 第3回「水溶液の中和」（新規単元 r3m01〜r3m80） | 80 |
| 社会 記号答えの解消（g1r84・g3r109 含む） | 12 |
| 社会 末尾追加（隋と唐の前後関係） | 3 |
| 夏期講習 地図9問の反転（kaki2_02 ほか） | 9 |

＋ 未クリア優先の出題順 ／ favicon の404止め ／ 理科 r3m53・r3m54 の短縮 ／
　 g3r109 の写真差し替え ／ `tools/sync_img_sizes.py`・`import_g.py`・`import_rika.py`

**公開側の実測: 1425問（社会1255／理科170）・id重複0。**
- ★**ユーザーが2回訴えた `kaki6_104` は 667字 → 55字**になっている（公開側で確認）
- `kaki8_118` 34字 ／ `kaki2_02`「地図1の『エ』は、何という工業地帯・地域ですか」→（中京工業地帯）
- index.html 側も、未クリア優先のコード・favicon 対策・`kai3_06` の寸法登録すべて反映ずみ

### ★★新しく見つけた問題: kaki2_75・76・77 は解答不能（未対応・司令塔に報告ずみ）

**`kaki_r2_graph06.jpg` に ア・イ・ウ の記号が印刷されていない。**帯グラフ3本が無印なのに、
問題文は「ア〜ウから選んで記号で答えなさい」、答えは（ア）（イ）（ウ）。
**子どもはどの帯がアなのか判断できない。**

- 元データ（`quiz_csv_夏期講習/kaki_r2_graph06.jpg`）と配信中の画像は**ハッシュ一致**。
  つまりコピー事故ではなく、**元の切り出しで記号が切り落とされている**
- `verify_data.mjs` は捕まえられない。**「図を指すことばがあって画像もある」ので条件を満たしてしまう**
- 中身から対応は判明する（上から順に 京浜・中京・阪神 で、graph01 の お・く・う と同じ数値）。
  つまり **ア=上・イ=中・ウ=下**。しかし問題文にその断りは無い
- ★**この3問は「三大工業地帯」だけなので、特徴での書き換えがきれいに決まる**（下記）

### グラフ問題の現状（★私の実測）

**`kaki_r2_graph01.jpg`（あ〜け・9地域）の実際の割合**（画像を開いて書き出した）:

| 記号 | 地域 | 金属 | 機械 | 化学 | 食料品 | その他 |
|---|---|---|---|---|---|---|
| あ | 京葉 | 22.2 | 9.1 | **45.6** | 13.8 | 9.3 |
| い | 瀬戸内 | 21.6 | 30.1 | 26.0 | 7.6 | 14.7 |
| う | 阪神 | 22.2 | 38.1 | 16.6 | 10.4 | 12.7 |
| え | 北九州 | 20.5 | 44.1 | 7.1 | 12.9 | 15.4 |
| お | 京浜 | 10.5 | 46.1 | 19.0 | 11.2 | 13.2 |
| か | 関東内陸 | 13.1 | 43.4 | 9.7 | **15.9** | 17.9 |
| き | 東海 | **7.6** | 49.6 | 13.7 | 13.6 | 15.5 |
| く | 中京 | 10.6 | **68.0** | 7.2 | 4.7 | 9.5 |
| け | 北陸 | 19.2 | 39.2 | 14.0 | 8.7 | **18.9** |

★**「特徴→地域名」で断定できるのは4つだけ**: く中京=機械68.0最大 ／ か関東内陸=食料品15.9最大 ／
き東海=金属7.6最小 ／ け北陸=その他18.9最大。
✕ **う阪神22.2 と あ京葉22.2 は同値**（「金属が最も高い」と書くと誤り）。
✕ **え北九州7.1 と く中京7.2 は0.1差**（目で区別できない）。
✕ お京浜は突出した特徴なし。
△ あ京葉=化学45.6は断然だが、**kaki2_25・kaki7_25・kaki7_68 で既に3回聞いている**。
い瀬戸内=化学26.0も kaki4_26 と重複。

★**graph06 の3問（三大工業地帯だけ）なら、3つの中での比較なのできれいに決まる**:
金属最大=阪神(22.2 vs 10.5/10.6) ／ 機械最大=中京(68.0 vs 46.1/38.1) ／ 化学最大=京浜(19.0 vs 7.2/16.6)。
ただし**「京浜は化学が最大」は、この3つの中での話**で、京浜の特色として教えると誤解を生む。要相談。

### ★「反転」が効く条件（司令塔が A-12 に登録）
**記号が「読み取るべき中身」を指しているときだけ効く。**
- 地図の記号 → **位置**を指している。答えるには位置を知る必要がある → **効く**（9問とも反転ずみ）
- グラフの記号 → **ただの行番号**。反転しても「く=中京」の丸暗記になるだけ → **効かない**


### 待ちになっているもの（2026-09-12 時点）
1. **★push のユーザー許可** … コミット5本が積まれている。23問の書き換え＋理科80問＋社会15問が一度に届く
2. **社会 第3回の再取り込み** … claude-34 が凍結を一時解除した。`g3r109` の画像を切り直し（画像に
   「記号で答えなさい」と印刷されているのに問題文を「名前で答えなさい」に変えてしまった食いちがい。
   私が実機で見つけた）＋ `g3r111` に記述問題の札。**新しいハッシュが来るまで第3回・第4回は読まない**
3. **社会 第4回** … `g4r11`・`g4r12` の分割後に取り込む。レビュー前なのでまだ入れない
4. **第2回の sol 3件**（`r2m15`〜`17`）… 次に理科第2回を再取り込みする機会に入れる
5. **`g1r85` の【発展問題】の札** … 次に社会第1回を再取り込みする機会に戻す

★**第1回・第2回（社会）のハッシュは有効のまま**: `8732547bc9216524…` / `f871177f3948bd83…`

### ★2026-09-12 取り込み完了: 理科第3回80問＋社会15問（1342→1425問）

**すべてコミットずみ。push だけがユーザーの許可待ち。**

| 中身 | 問数 | id |
|---|---|---|
| 理科 第3回「水溶液の中和」（新規単元） | **80** | `r3m01`〜`r3m80` |
| 社会 末尾追加（隋と唐の前後関係） | 3 | `g2r114`・`g2r115`・`g3r113` |
| 社会 記号答えの解消（q・a だけ） | 12 | `g1r9`・`g1r73`・`g1r83`・`g1r84`・`g2r4`・`g2r86`・`g2r94`・`g2r103`・`g3r98`・`g3r99`・`g3r100`・`g3r109` |

- **1342 → 1425問。既存 id は1つも動いていない。id重複0。空のq/a 0**
- 科目別: 社会1255 / 理科170
- ★**calc は取り込んでいない**（第3回の calc 18問は `keisan-print-app` の持ち物）
- 照合: 第1〜3回とも「増える0・変わる0・消えた0」＝元データと完全一致。理科も80/80一致

#### ★★元データが作業中に動く事故を、1日で2回起こしかけた（C-4c）
**1度目（理科）**: 10:26 に読んだ55行版で安全検査を全部通したのに、10:34:01 に98行へ
書き換わり、46秒後の `--apply` が98行版を取り込んだ。**検査ずみの版と取り込んだ版が別物。**
→ data.js を HEAD まで戻して取り消した。
**2度目（社会）**: `--check`（変わる0問）と `--apply` の間に、claude-34 が11問を書き換えた。

**★テストも verify_data も、これは絶対に捕まえられない。**
どちらも「元データと data.js が一致するか」しか見ないので、**元データ自体が動いていれば一致してしまう。**
実機目視でも、80問が出ること自体は正常に見える。**B-3b の実例。**

→ **対処: 取り込みの前と後で元データの sha256 を照合する。**今回はすべて一致を確認した。
```
理科第3回      730227ea49af1232（前後で一致）
第1回 8732547bc9216524… / 第2回 f871177f3948bd83… / 第3回 732eaadc07072393…（凍結表と一致）
```
→ 運用も変わった（司令塔決定）: **作成担当が sha256 つきで凍結を宣言し、それまで触らない。**

#### ★道具で踏んだ罠（`tools/import_g.py` に書き残した）
1. **文字列で比べると、既存313問を無用に書きかえる。**data.js には取り込んだ時期ごとに
   **2つの書式が混ざっている**（最後のキーにカンマが付く行と付かない行）。
   実測: `g1r`・`g2r`・`kaki1_`・`s`・`simg` は付く、ほかは付かない。
   → **行を辞書に直して中身で比べ、中身が同じ行は1バイトも触らない。**
   これをしないと差分が1057行に膨らみ、競合の温床になる（ROLE.md 5）
2. **空の欄を落とさずに比べると、全問が「変わった」と判定される**（`img=""` と `img`無しのちがい）
3. **★`--apply` を2回打ってしまった**（head と tail を見るために同じコマンドを2回書いた）。
   結果は無事だった（`at = その接頭辞の最小位置` なので2回目は同じ位置に上書きされる）が、
   **危なかった。`--apply` は1回だけ打って、出力は変数に取ること**

#### 実機で目視した12問（B-12・`tools/mikaku/shots/`）
`r3m01`・`r3m53`・`r3m54`・`r3m80`・`g2r114`・`g2r115`・`g3r113`・`g1r83`・`g1r84`・`g3r109`・`g1r9`・`g3r100`
- 地図つき（`g1r83`・`g1r84`）は**画像・問題文・答えがそろって出ることを目で見た**
- `tools/mikaku/mikaku_check2.mjs` を作った。前任の `mikaku_check.mjs` は社会専用で、
  **理科の単元は科目を切りかえないと出てこない**（`#subject-science` を押す）

#### ★既存の小さな問題を1件見つけた（私の変更とは無関係・未対応）
実機で 404 が1件出る。原因は `index.html` の **`<img src="">` が3か所**あること
（1642・1746・1816行）。空の src はブラウザがページ自身を取りに行って404になる。
**害はないが、コンソールが汚れて本当のエラーが埋もれる。**1行で直せる。index.html は今回触っていない。

### 【済】2026-09-12 理科第3回の取り込み保留（解決）

**司令塔 claude-fe の依頼「第3回 Part1・55問（memo 54／calc 1）を今日中に」に着手したが、
元データが作業中に書き換わったため取り込みを取り消した。**

| 時刻 | 起きたこと |
|---|---|
| 10:26 | 元データを読む。**55行（memo 54／calc 1）**、no 1〜55、note は「要点チェック」だけ |
| 〜10:33 | この55行版で安全検査を全部通す（図表参照0・丸数字0・120字以上0・q重複0・既存との重複0） |
| **10:34:01** | **元データが書き換わる。98行（memo 80／calc 18）**に増加。note に「演習問題集」＝Part2 が追加された |
| 10:34:47 | 私の `--apply 3` が走り、**80問を取り込んでしまった**（依頼は54問） |
| 直後 | **data.js を HEAD と完全一致まで戻した。**1342問・id重複0・`git diff` 0行。コミットもしていない |

**★教訓: 他のセッションが編集中のファイルは、読んだ瞬間の内容で取り込んではいけない。**
検査は55行版に対して通したのに、取り込んだのは98行版だった。
**つまり「検査ずみ」と「取り込んだもの」が別物になっていた。**
テストも verify も、これは捕まえられない（どちらも「元データと data.js が一致するか」しか見ないため、
元データが変わっていれば一致してしまう）。
→ **取り込み前に元データの更新時刻を見て、作成担当に「書き終わったか」を確かめる。**

claude-64 [c69d87] に3点を聞いて返事待ち:
1. いまの98行は確定版か、まだ追記中か
2. 取り込むのは Part1 の54問か、Part2 を含む80問（memo）か
3. 書き終わったら一声ほしい

### ★理科の取り込みで分かったこと（道具にも書いた: `tools/import_rika.py`）

- **`import_quiz.py` は夏期講習専用。**元データの `id` を使い `kai` を落とすので理科には使えない。
  理科用に `tools/import_rika.py` を新規作成した
- **★calc は対戦アプリに入れない。**計算問題は 2026-09-07 にユーザーの依頼で `keisan-print-app` へ
  分離ずみ。第1回の calc 7問・第2回の calc 70問は向こうに r1c01〜07・r2c01〜70 として入っている。
  **第3回の calc も計算プリント担当の持ち物。**入れると二重になる
- **★★理科の id は「memo だけを取り出した並び順」の連番。`no` の値ではない。**
  実測で確認（「id番号=no」の仮説は第1回 16/73・第2回 4/17 しか合わない）。
  calc がはさまると番号がずれる（98行版なら28件）。
  **差し替えは no ではなく memo の並び順で対応を取ること。**no から組み立てると中身が
  別の問題に入れかわり、解答履歴が別の問題に紐づく（夏期講習で踏んだ事故と同じ型）
- **道具が正しいことを既存データで確かめた**（D-17）: `import_rika.py` の出力が
  data.js の既存行と**第1回 73/73 バイト一致**。第2回は 14/17 で、
  **ちがう3件は元データに `sol` があるのに data.js に入っていない分**（下記）

### ★既存のずれを1件見つけた（私の変更とは無関係・未対応）
**第2回の元データには `r2m15`・`r2m16`・`r2m17` に `sol`（解説）があるが、data.js に入っていない。**
あとから足された解説が取り込まれなかったと見られる。claude-64 に伝えた。急ぎではない。

### ★★2026-09-12 push が私のセッションの権限で止まっています（ユーザーの判断待ち）

**23問の配信はユーザー承認ずみ**（司令塔 claude-fe 経由）。テストも全部通し、
コミットも済んでいますが、**`git push origin master` がこのセッションの
権限設定（auto mode classifier）で拒否されました。**

```
1688fd3 長い四択23問を「知識を聞く一問一答」に書き換え
a5ddcd2 ブラウザ本体が無いPCでもテストが通るようにし、引き継ぎを記録
## master...origin/master [ahead 2]   ← ローカルには入っている。まだ公開されていない
```

- **他のセッションに代わりに push させることはしません。**権限の迂回になるため
  （共通ルール: permission laundering の禁止）。**ユーザーが自分で許可を出すまで待ちます**
- ユーザーが `git push` を許可すれば、そのまま出せます。やり直しの作業はありません
- 配信前の確認は**すべて終わっています**（下記）。残るのは push と公開URLの目視だけ

配信前チェック（司令塔の指定4項目のうち1・2は完了）:
- テスト: test_migration 22件成功0失敗 ／ smoke-test 11件成功0失敗 ／
  node だけで動く5件成功 ／ verify_data 全8編765問 不一致0件
- data.js: 1342問・id重複0・patch23件すべて一致
- 公開URLでの目視（kaki6_104）は **push 後にやる。まだできていない**


## 担当（2026-09-06 ユーザー決定）
- **masterへ直接pushするのは claude-ed（自宅PC）だけ。**
- クラウドセッション（スマホからの窓口、例: `kosukequiz-battle-e1`）は**ブランチ＋プルリクで出す**。
- 詳しいルールは `ROLE.md` の「担当とルール」を参照。

## 2026-09-12 引き継ぎ（claude-03 [e15b1d]）★前任の23問は全部終わっていた。未コミットなのが問題

**前任 claude-dc [beafe0] から交代。**司令塔 claude-fe [0133ca] からの引き継ぎ指示では
「型1の4問だけ完了、型2以降が残っている」と伝えられたが、**実測したら23問すべて完了していた。**

### 私が実測して確かめたこと（司令塔の理解との食い違い）

| 項目 | 司令塔の理解 | 私の実測 |
|---|---|---|
| 書き換えずみ | 型1の4問だけ | **型1〜型4の23問すべて**（patch の23 id が data.js と一致） |
| 元データ書き戻し（C-6） | 不明 | **23/23 完了**（夏期講習15件は id で、通常回8件は `no` で照合。食い違い0） |
| 実機目視（B-12） | 不明 | **やってある。**`tools/rewrite/shots/` に23枚。kaki6_109 を開いて中身を確認した |
| data.js の健全性 | 壊れていない | **1342問・id重複0**（一致） |

- 通常回の元データ（`quiz_csv/第N回_*.json`）に **`id` 欄は無い。**`no` 欄で照合する
  （`g2r110` → `第2回_古墳飛鳥.json` の `no=110`）。id で探すと「見つからない」と誤判定する
- `shots/kaki6_109.png` を目で見た: 地図画像・短い問題文・「（長野）です。位置は地図の4です。」・解説が
  すべて出ている。**型2の狙い（選択肢一覧を消す／位置は答えに添える）が実機で成立している**

### ★★本当の問題：全部コミットされていない（配信されていない）

```
M  data.js            ← 23問の書き換えが staged のまま
A  tools/rewrite/...  ← 道具と写真23枚も staged のまま
M  作業メモ.md
M  ROLE.md            ← 第0節の見出しを追記（未staged・内容は妥当）
## master...origin/master  ← origin と差はゼロ = 一度も push されていない
```

**つまり、23問の書き換えは子どもの端末にまだ1問も届いていません。**
ユーザーが今日2回訴えた `kaki6_104` も、実機ではまだ667字の旧版が出ます。
前任は作業を終えた直後（10:00過ぎ）に落ちたため、コミットまで届かなかったと見られる。

→ **push は本番配信なので、司令塔に承認を上げた**（共通 CLAUDE.md 第4節）。承認待ち。
→ **承認が来たら、まず この23問を先に配信する。**残りの作業と束ねない（届くのが遅れるため）

### 次にやること（司令塔指定の順）
1. **【承認待ち】23問の commit & push** ← これが最優先。もう出来上がっているものが届いていない
2. ②上位2枚20問の書き換え ＋ 同20問の `sol`（工業地帯・地域。一度で済ませる）
3. ①選択肢の並べ替え30問（除外15問＝すでに書き換えた分）
4. `index.html`: 未クリア→苦手 の優先順（段ごとにシャッフルして連結）
5. 年表モードは**保留**（ユーザーの判断でクイズ担当へ移った。着手しない）

### 引き継いだ未解決（前任から）
- `g1r63`・`g1r85` … data.js と元データが食い違う2件。**HEAD 時点で既にずれ**ており今回の23問とは無関係。
  どちらが正か分からないので触っていない。司令塔に報告ずみ
- `kaki2_74` … patch の `holds` に入っている唯一の保留

## 2026-09-12 長い四択23問を「知識を聞く一問一答」に書き換えた（claude-dc [beafe0]）

**ユーザーの訴え「まだ、長い四択が残っています。知識は聞きたいのでうまく問題を直してほしいです」**
指示書: `司令塔\回答\対戦アプリ_長い四択を知識の問いに直す_指示_2026-09-12.md`

### 結果
**120字以上の長い四択 23問 → 0問。** 1342問のまま、id は1つも動いていない。

| 型 | 問数 | 直し方 |
|---|---|---|
| 型1（説明文A〜Gの読み比べ・400字超） | 4 | 説明文を全部外して1文で聞く |
| 型2（説明文＋選択肢20個＋地図番号） | 7 | 選択肢を消して都市名を直接。位置は**答えに添えた** |
| 型3（長文4つの正誤判定） | 8 | 正解文の知識を1問に。誤答の知識は `a` の補足へ |
| 型4（表・グラフの2段階） | 4 | 表を外す／県名を問う形へ |

字数の実例: `kaki8_118` 667→35字 / `kaki8_116` 662→39字 / `kaki6_104` 496→55字 / `kaki6_95` 420→65字。

### ★数え直しは司令塔の概算と完全一致した（食い違い0）
見出し3つ以上=37問 / 120字以上=23問 / 400字以上=4問。道具は `tools/rewrite/` に置いた。
書き換え後は 37→14問（残り14はすべて120字未満）。

### ★重複確認（A-9 手順3）で、指示書の案が既存問と衝突したもの
**「答えが同じ」だけでは重複ではないが、「問い方まで同じ」は重複。**ここを分けて判定した。

- **`kaki7_68`** … 指示書の案「千葉・君津をふくむ工業地域の名は？」は **`kaki7_67` とほぼ同一文**だった。
  司令塔の判断「**重複を避けるより20秒で解けるが優先**」により保留にせず、
  「関東の工業地帯・地域のうち化学の割合が特に高いのは」と**比べる相手がいる聞き方**にした。
  `find_same_answer.mjs` には kaki7_67 との組が出るが、**これは承知のうえ**（問い方が別）。
- **`g2r110`** … 「仏教を伝えたのは百済」は g2r4/32/33/69/103 の**5問と重複**。→ **経典**を問う形にした。
- **`g3r92`** … 正解の「大宰府」は **`g3r108` と完全重複**。誤答の国司・郡司も g3r18〜21 で既出。
  → 語ではなく「**郡司にはどのような人が任命されたか**」＝性格を問う形にした。
- **`kaki4_81`〜`83`** … 都市名を答えさせると kaki4_34/36/43 と重複する。→ **県名**を問う形に変えた。
- **`g2r112`** … ★司令塔から「大友皇子が未出題ならそちらを優先」と言われたが、
  **大友皇子・大海人皇子は全1342問に一度も出てこない。教材が使っていない語**なので、
  そのまま出すと子どもが答えられない（A-2）。教材が使う「弟」の表現で確定した。
  **「未出題だから使える」ではない。未出題の理由が「教材に無いから」のことがある。**

### ★説明文A〜Gを外しても知識は宙に浮かなかった（確認ずみ）
- 復習編8のA〜G（7道県）… `kaki8_98`〜`115` が**1つずつ全部を個別に出題ずみ**
- 復習編6のA〜G（7都市）… 型2の `kaki6_105`〜`111` がそのまま受け持つ

### 元データへの書き戻し（C-6）
6ファイルに書き戻した（復習編4/6/7/8・第2回・第3回）。`verify_data.mjs` で**全8編765問 不一致0件**。
通常回（g*）は verify_data.mjs の対象外なので**別に自前で照合し、313問中の該当分すべて一致**を確認した。

### ★既存のずれを2件見つけた（私の変更とは無関係・未対応）
通常回の自前照合で、**data.js と元データJSONが食い違う問が2件**あった。
```
g1r63  q  「紀元前1世紀ごろの日本のようすが…」（data.js） vs 「…倭のようすが…」（元データ）
g1r85  q  data.js は短い言いかえ版、元データは発展問題の長文のまま
```
**どちらも HEAD の時点で既にずれており、今回の23問には含まれていない**（`git diff` で0行）。
第1回の元データか data.js のどちらが正なのか分からないので、**触っていない。司令塔に報告ずみ。**

### 実機で目視した（B-12）
`tools/rewrite/mikaku_check.mjs` を作った。**本物の Chrome（スマホ幅390px）で実際に出題し、
23問すべてを画面から読み取って写真に残した**（`tools/rewrite/shots/`）。JSエラー0。
- Playwright のブラウザは**ダウンロードが通らなかった**ので、`channel: "chrome"` で
  **PCに入っている Chrome をそのまま使った**。次に同じ壁に当たった人はこれで動く
- `#solo-q-id` は `No.kaki6_109` の形。`#` を剥がすつもりのコードだと**永久に一致しない**
- 地図つき（kaki6_109・kaki4_82）は、**画像・短い問題文・答えの位置表示**がそろって出ることを目で見た

### 判定の速さ（対戦の〇✕10秒）への影響
`g3r111` だけ記述式にした（1語にできないため）。**答えに「言えていればよい要点」を書いてある**ので、
親が10秒で〇✕を付けられる。ほかの22問はすべて短い語で答える形。

### 次にやること
1. ②上位2枚20問の書き換え ＋ **同じ20問の `sol` に「グラフのどこを見るか」を書く**（司令塔の追加依頼・合流ずみ）
2. ①選択肢の並べ替え30問 ← **最後**（書き換えた問からは選択肢が消えるため）
3. `index.html`: 年表モード（種別に「年表だけ」を追加）
4. `index.html`: 未クリア→苦手 の優先順（段ごとにシャッフルして連結）

---

## これより前の記録

**2026-09-08 以前は `作業メモ_アーカイブ_2026-08-30〜09-08.md` に移しました**（2026-09-12・本体が1662行になり直近の進捗が埋もれたため）。
**捨てていません。**索引と「いまも効く教訓」をアーカイブの先頭に付けてあります。
